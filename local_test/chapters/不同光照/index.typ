= 低光图像的合成实验

== 摘要

夜间农业无人机（UAV）影像在作物表型监测、田间巡检与精准管理中具有重要应用潜力，但受低照度条件下曝光不足、噪声显著及颜色不稳定等成像退化因素影响，其图像质量往往难以稳定支撑后续农业视觉分析任务。现有低光增强方法在农业场景中普遍存在模型复杂度高、对真实夜间田间环境泛化不足，或仅改善主观亮度而难以保持结构与颜色稳定性等问题。

针对上述挑战，本文提出一种面向夜间农业 UAV 场景的轻量化低光增强网络 $L^3$-AgriUAVNet。在缺乏严格昼夜配准样本的现实约束下，本文首先构建了一套物理与 ISP 启发的低光退化模型，在 patch 级别合成低光—正常曝光成对数据，用于可控的监督训练。通过轻量残差特征蒸馏与跨尺度空间注意力机制，在有限参数量与计算预算下增强对作物纹理与行间结构等关键细节的建模能力；同时引入全局调制与细节门控策略，以稳定夜间条件下的曝光与白平衡响应，并抑制噪声主导区域的过度增强。此外，本文设计了由鲁棒重建、梯度一致性与颜色比例约束组成的低光损失函数，以缓解低光增强中常见的“变亮但发糊”与全局色偏问题。

实验在合成成对数据与真实夜间农业 UAV 图像上进行系统评估。结果表明，$L^3$-AgriUAVNet 在 PSNR、SSIM、MAE 与 LPIPS 等指标上取得了具有竞争力的性能，并在模型参数量与推理开销方面显著优于多种代表性方法。进一步的统计分布分析与设备级推理测试验证了该方法在真实夜间场景下的结构保真性、颜色稳定性与部署可行性。最后，通过一个轻量级下游作物分类任务的案例研究，证明所提出的低光增强方法能够恢复对农业语义判别具有意义的视觉表征，从而为夜间农业 UAV 视觉分析提供更加稳定、可复用的输入。

*关键词：* 农业；表型监测；机器视觉；低光增强

== 引言

无人机平台能够在大尺度农田上快速获取高分辨率可见光影像，为作物长势评估、病虫害巡查与表型分析提供高时空分辨率数据@guebsi2024drones @aierken2024review。然而在夜间或弱光条件下，UAV 影像常出现整体曝光不足、噪声显著、对比度下降与颜色偏移等问题@widartono2025aerial @fu2022highlightnet，直接影响后续检测、分割与计数等视觉任务的稳定性与可用性@silwal2021robust。与白天成像相比，夜间农业场景还具有背景复杂、作物纹理细密等特点，使得低光增强需要同时兼顾“可见度提升”和“结构细节保真”。

在农业生产中，夜间影像获取并非边缘需求。一方面，部分田间作业（如夜间巡田、应急病虫害排查、阶段性长势核查等）对“及时性”有更高要求；另一方面，实际作业安排往往受白天温度、风速、人员与机械调度等因素影响，夜间与清晨时段反而更利于持续、稳定地开展航测与巡检@flynn2026assessing。以棉花等大田作物为例，生长中后期的群体结构变化较快，且田块跨度大、地形地貌差异显著@bendig2015combining @yang2017unmanned，夜间 UAV 影像一旦质量不稳，容易造成下游任务在田块间、批次间表现波动@kamilaris2018deep @xu2019multispectral，影响农业决策的可解释性与可复用性。

从成像链路角度看，夜间 UAV 影像退化通常是多因素叠加的结果：低照度导致曝光时间延长或增益提升，使传感器噪声更显著@kurimo2009effect；机体震动与飞行速度进一步放大运动模糊风险@tsekhmystro2025uav；相机 ISP 的降噪与锐化策略在弱光下更容易产生纹理涂抹或伪影@li2021low；同时田间存在复杂、非均匀的反射光，使白平衡与色彩一致性更难保持@lu2025unsupervised。上述因素共同导致“变亮但发糊”“细节恢复伴随噪声放大”“颜色偏移影响作物判读”等问题，限制了夜间 UAV 影像在农业场景中的有效利用。

因此，面向农业夜间 UAV 低光增强需要在以下方面取得平衡：（i）在显著提升可见性的同时尽量抑制噪声与伪影，避免对纹理和叶缘等关键结构造成不可逆的平滑；（ii）保持颜色与亮度层次的稳定性，降低跨田块、跨批次的外观漂移；（iii）兼顾边端部署约束，在有限参数量与算力预算下实现可用的推理速度与鲁棒性，为后续检测、分割与计数等任务提供更可靠的输入。

现有低光增强方法大体可分为基于 Retinex 物理先验的方法与基于深度学习的端到端方法。前者依赖对照明与反射分解的假设（例如经典 Retinex 理论及其后续算法@land1971retinex），面对田间复杂反射与非均匀光照时容易产生过增强或色偏；后者虽然在公开数据集上表现良好，但常伴随较高计算量，且公开低光数据与农业 UAV 夜景存在明显域差异，导致在真实田间夜景上出现纹理涂抹、边缘不稳定等问题。近年来，一些基于深度 Retinex 分解的监督方法通过成对数据（如 LOL）学习照明与反射的联合建模@wei2018retinex，以及零参考、无配对训练的曲线估计方法（如 Zero-DCE++）在效率与可用性上具备优势@li2021learning。同时，Transformer 与生成式建模逐渐被引入低光增强以提升长程依赖建模与极暗区域恢复能力，例如 Retinexformer、LLFormer 与 Diff-Retinex 等方法@cai2023retinexformer @wang2023llformer @yi2023diffretinex；此外，也有更强调端侧效率与稳健性的轻量方案（如 SCI）@ma2022sci，以及结合多重先验约束的近期工作@wang2024quadprior。因此，面向夜间农业 UAV 影像的低光增强方法，需要在显著提升可见性的同时，尽量保持作物纹理、行间结构与颜色关系的稳定性，并在有限参数量与算力预算下具备良好的可部署性，以稳定服务于后续的检测、分割与表型分析等下游任务。

#figure(
  image("overview.png", width: 100%),
  caption: [
    夜间农业 UAV 低光增强方法的整体框架示意图。整体流程包括数据采集、数据集构建、模型开发、指标验证与下游任务评估五个阶段。首先，从露天大田场景采集无人机可见光影像，并通过基于物理与 ISP 启发的低光退化模型构建合成成对数据集。随后，在合成数据上训练所提出的 L³-AgriUAVNet，并在无配对的真实夜间 UAV 图像上进行测试。通过与多种代表性方法的对比实验、模块消融分析及设备级推理测试，对方法的增强性能与部署可行性进行系统评估。最后，引入一个轻量级下游作物分类任务作为受控验证，用以分析低光增强是否能够恢复对农业语义判别具有意义的视觉表征。
  ],
) <fig:overview>

基于上述分析，本文面向夜间农业无人机影像在低照度、噪声与颜色不稳定条件下难以稳定支撑下游农业视觉任务的问题，提出一种兼顾增强质量与边端部署约束的轻量化低光增强方案 L³-AgriUAVNet。如@fig:overview 所示，本文从数据构建、模型设计到评价验证，构建了一套完整且工程可行的研究框架。

具体而言，首先从露天大田场景采集正常曝光的无人机可见光影像，并在缺乏严格昼夜配准样本的现实约束下，引入一种物理与 ISP 启发的低光退化模型，在 patch 级别合成低光—正常曝光成对数据，以支持可控的监督学习训练。随后，在合成数据上训练所提出的轻量化低光增强网络 $L^3$-AgriUAVNet，并将其直接应用于真实夜间农业 UAV 图像，以验证模型在无配对参考条件下的泛化能力与稳健性。在方法层面，通过轻量残差特征蒸馏块与跨尺度空间注意力机制，在有限参数量与计算预算下增强对作物纹理、行间结构等关键高频信息的建模能力；同时引入全局调制与细节门控机制，以稳定夜间条件下的曝光与白平衡响应，并抑制噪声主导区域的过度增强。在评价层面，本文不仅在合成数据上进行定量对比与消融分析，还进一步通过真实夜间图像的统计分布分析、设备级推理测试以及一个轻量级下游作物分类任务，对增强结果的结构保真性、颜色稳定性与任务相关性进行系统验证。

通过将低光增强置于“数据构建—模型设计—指标验证—下游任务评估”的统一框架中，本文旨在证明：面向夜间农业 UAV 场景的低光增强方法，不仅应提升图像的主观可见性，更应在统计特性与语义判别层面为后续农业视觉任务提供稳定、可复用的输入表示。本文围绕夜间农业无人机影像在低照度、噪声与偏色条件下难以稳定服务下游表型任务的问题，提出一种兼顾增强质量与边端部署约束的轻量化低光增强方案。主要贡献如下：

+ *UAV 低光成对数据构建与验证框架：* 针对真实夜间—白天严格配准样本难以获取的现实约束，提出一套面向农业 UAV 的成对数据生成流程。该流程从正常曝光影像裁剪多尺度 patch，并通过曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角等退化因素合成低光输入，从而构建可控的监督训练对；同时引入真实夜间数据进行外部统计验证，以检验合成数据与真实夜景在亮度、饱和度及通道比例等分布上的一致性。
+ *提出 $L^3$-AgriUAVNet：面向夜间农业场景的轻量化增强网络设计：* 提出端到端的轻量低光增强网络 L³-AgriUAVNet，在特征蒸馏框架下设计轻量残差特征蒸馏块（L-RFDB）与跨尺度空间注意力模块（SCSA），以较低参数量增强纹理与结构细节的恢复能力；进一步引入全局调制分支（GMod）以稳定曝光与白平衡，并通过细节门控机制实现“结构区域更积极、平坦/噪声区域更保守”的自适应增强策略，从而提升真实田间夜景下的稳健性与可部署性。
+ *面向“图像质量”的低光优化目标：* 提出由鲁棒重建（Charbonnier）、梯度一致性与颜色比例约束构成的低光损失函数，在提升亮度与可见度的同时抑制纹理涂抹与全局色偏，使增强结果更接近正常曝光外观分布，为后续农业视觉任务提供更稳定的输入。

== 方法与数据

#figure(
  image("dataset_aug.png", width: 100%),
  caption: [
    用于低光增强的训练对生成流程。将正常曝光的无人机图像裁剪成多尺度补丁，然后通过曝光衰减、噪声注入和可选的轻微模糊进行降质，以合成对应的低光输入，从而生成用于监督训练的成对样本 $(x, t)$。
  ],
) <fig:dataset_aug>

如@fig:dataset_aug 所示，UAV 影像中裁剪多尺度图像块（patch），并基于低光退化模型合成对应的低光输入，以构建监督学习所需的成对样本 $(x, t)$（其中 $x$ 为低光输入，$t$ 为正常曝光目标）。具体而言：1）在每张图像的四角与中心区域进行带抖动采样，并随机选择缩放因子以模拟不同飞行高度带来的细节软化；2）对 patch 进行随机旋转与水平翻转等数据增强；3）对目标 patch 施加曝光衰减（如增益衰减与 gamma 变换），并叠加轻微噪声与可选模糊，以近似夜间成像中的欠曝、噪声与 ISP 平滑效应@wei2018retinex。最终将数据集划分为训练集、验证集与测试集三部分，其中训练集占 80%，验证集占 10%，测试集占 10%，并在此划分下完成模型训练与评测。

=== 研究区域

#figure(
  image("area.png", width: 90%),
  caption: [
    研究区域示意图 (Overview of the study area) (a) 研究区域在新疆沙湾市的地理位置；(b) 开阔大田实验区的无人机正射影像，其中 L1、L2 和 L3 分别表示不同作物地块；(c)–(e) 分别为 L1（小麦）、L2（棉花）和 L3（玉米）的地面实景图像。研究区域均为露天大田环境，具有典型的农业种植结构与自然光照条件。
  ],
) <fig:area>

本次实验在新疆沙湾市周边的大田农业区开展，如@fig:area 所示。@fig:area(a) 给出了研究区在新疆的地理位置；研究区属温带大陆性气候，年均降水量较低（125.9 至 207.0 毫米），水资源相对匮乏且昼夜温差显著。在该类环境下，作物冠层形态、土壤背景以及光照条件在不同地块间可能存在差异，从而对弱光成像的亮度分布与噪声水平产生影响。实验数据采集于实际生产田块（农户自耕地），而非专门试验田，因此能够更真实地反映大田场景中常见的地表背景变化、作物行间结构与管理差异，提升所提出方法在真实农业应用中的可迁移性与参考价值。@fig:area(b) 为无人机获取并拼接得到的正射影像，可见研究区包含多个相邻地块（L1、L2、L3），地块边界清晰且田间纹理呈现典型的行列结构。为评估模型在不同作物纹理与冠层结构下的适用性，我们选择了三类代表性作物地块：L1 为小麦，L2 为棉花，L3 为玉米；相应的地面实景如@fig:area(c)--(e) 所示。

其中，正常曝光数据采集于 2025 年 5 月 6 日至 6 月 12 日，覆盖作物生长的不同时期与田间外观变化。为增强数据多样性与代表性，我们从多期航测数据中均匀选取三种典型大田作物（小麦、棉花与玉米）的无人机影像，最终获得 1306 张原始 UAV 图像用于低光成对数据集的构建。此外，我们进一步选取 26 张在真实田间场景下采集的夜间低光图像，用于低光退化模型的参数标定与统计分布对齐分析，以验证合成低光数据在亮度、饱和度及颜色比例等特征上的合理性。

=== 低光退化模型

为在缺乏严格配准的夜昼成对样本条件下构建可控的训练与评估数据，我们在 RGB 图像域中设计一种物理与 ISP（Image Signal Processing）启发的低光退化模型（如@fig:dataset_aug（b））。给定正常曝光图像块（8-bit RGB）$bold(I) in [0, 255]^(H times W times 3)$，先归一化得到强度表示：

$ bold(X) = frac(bold(I), 255) in [0, 1]^(H times W times 3). $

给定归一化后的正常曝光图像块 $bold(X) in [0, 1]^(H times W times 3)$，模型通过依次模拟欠曝、白平衡漂移、Poisson--Gaussian 噪声，以及轻度 ISP/光学伪影（模糊与暗角）生成低光观测 $bold(Y)$。整体前向过程表示为：

$ bold(Y) = cal(V)(cal(B)(Gamma_(gamma)(T_(tau)(cal(N)(cal(W)(alpha bold(X)))))))) $

其中 $cal(W)$ 表示通道增益（白平衡）、$cal(N)$ 表示噪声注入、$T_(tau)$ 与 $Gamma_(gamma)$ 分别表示 tone 映射与 gamma 映射、$cal(B)$ 表示轻度模糊、$cal(V)$ 表示暗角（径向照度衰减）。下文对各算子给出具体定义及参数采样范围。

*曝光衰减（Exposure reduction）*

在低照度条件下，相机往往通过降低曝光或增益调整导致整体信号幅度下降。我们使用线性缩放模拟欠曝：

$ bold(X)_e = alpha bold(X), alpha ∼ cal(U)(0.08, 0.30). $

*通道增益（White balance / ISP color bias）*

为刻画弱光条件下 ISP 白平衡与通道增益的漂移，引入通道独立增益向量 $bold(g) = (g_r, g_g, g_b)$，并对三通道逐元素缩放：

$ bold(X)_("wb") = cal(W)(bold(X)_e) = bold(X)_e ⊙ bold(g), g_c ∼ cal(U)(g_(c, "min"), g_(c, "max")), c in {r, g, b}. $

本文取 $g_r ∼ cal(U)(1.05, 1.18)$，$g_g ∼ cal(U)(0.95, 1.03)$，$g_b ∼ cal(U)(0.98, 1.06)$，并进行裁剪以保证数值稳定：

$ bold(X)_("wb") ← op("clip")(bold(X)_("wb"), 0, 1). $

*Poisson--Gaussian 噪声（Shot + read noise）*

夜间噪声通常由信号相关的散粒噪声（shot noise）与信号无关的读出噪声（read noise）共同构成。我们采用 Poisson--Gaussian 近似进行建模：

$ bold(X)_p = frac(1, k) · op("Poisson")(k · bold(X)_("wb")), k ∼ cal(U)(30, 100). $

较小的 $k$ 对应更强的相对散粒噪声强度。随后叠加零均值高斯噪声以模拟读出噪声：

$ bold(X)_n = bold(X)_p + epsilon, epsilon ∼ cal(N)(0, sigma_r^2), sigma_r ∼ cal(U)(0.004, 0.020). $

最终结果被裁剪至 $[0, 1]$ 以满足图像强度范围约束。

*Tone 映射与 gamma（Tone mapping + gamma）*

为在 RGB 域近似 ISP 的 tone curve 以及显示域非线性响应，我们采用幂律形式的 tone 与 gamma 变换：

$ bold(X)_t = T_(tau)(bold(X)_n) = bold(X)_n^tau, tau ∼ cal(U)(0.90, 1.05). $

$ bold(X)_g = Gamma_(gamma)(bold(X)_t) = bold(X)_t^gamma, gamma ∼ cal(U)(1.2, 1.8). $

*轻度模糊（Mild blur）*

考虑到弱光条件下 ISP 降噪、轻微失焦或运动引起的细节软化，使用高斯核近似点扩散函数（PSF）：

$ bold(X)_b = cal(B)(bold(X)_g) = bold(X)_g * cal(G)(sigma_b), sigma_b ∼ cal(U)(0.00, 0.60). $

当 $sigma_b = 0$ 时跳过该步骤。

*暗角（Vignetting / illumination falloff）*

为模拟镜头暗角或空间非均匀照度，引入径向衰减掩膜 $M(bold(p))$。令归一化坐标 $tilde(x), tilde(y) in [-1, 1]$，$r^2 = tilde(x)^2 + tilde(y)^2$，则：

$ M(bold(p)) = 1 - s · op("clip")(r^2, 0, 1), s ∼ cal(U)(0.00, 0.15). $

并得到：

$ bold(Y) = cal(V)(bold(X)_b) = bold(X)_b ⊙ M(bold(p)). $

*量化输出（8-bit quantization）*

最后将结果裁剪并映射回 8-bit 空间：

$ bold(Y)_8 = op("round")(255 · op("clip")(bold(Y), 0, 1)). $

需要指出的是，本模型在 RGB 域实现，未显式模拟 RAW 与去马赛克过程，因此属于 image-domain approximation；但其包含低光退化的关键统计因素（曝光降低、通道偏置、Poisson--Gaussian 噪声、轻度模糊与暗角），可用于构造训练数据并支持后续的消融分析。所有参数均在对应区间内均匀采样，以增强退化多样性并提升模型鲁棒性。

=== 数据集构建

#figure(
  image("dataset_dis.png", width: 80%),
  caption: [
    合成低光退化图像与真实夜间 UAV 图像的统计一致性分析。(a) 多维图像统计指标（亮度、对比度、饱和度、RGB 通道比例等）的均值及标准差对比，(b) 合成数据与真实数据在各统计指标上的绝对差异，(c) 亮度（luma）直方图分布对比。结果表明，所构建的合成低光退化在多项关键统计特性上与真实夜间 UAV 图像具有良好一致性，为后续低光增强模型的训练与评估提供了合理的数据基础。
  ],
) <fig:dataset_dis>

针对真实夜间农业 UAV 影像难以获取严格配准的昼夜成对样本的问题，本文采用“合成成对训练 + 真实夜间外部验证”的策略构建数据集，以在可控监督学习条件下提升模型对真实场景的适用性与鲁棒性。

正常曝光数据采集于 2025 年 5 月 6 日至 6 月 12 日，覆盖作物生长的不同时期与田间外观变化。为增强数据多样性与代表性，我们从多期航测任务中均匀选取三种典型露天大田作物（小麦、棉花与玉米）的无人机影像，最终获得 1306 张原始 UAV 图像作为数据源。所有影像均在实际生产田块条件下采集，能够真实反映大田场景中常见的作物行列结构、土壤背景差异及光照变化。

在此基础上，我们对原始影像进行多尺度裁剪以生成训练 patch，并结合随机旋转与翻转等数据增强操作，以模拟不同飞行高度、航向与视角条件下的成像差异。随后，基于第 2.2 节所述的物理与 ISP 启发低光退化模型，对正常曝光的图像施加曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角等退化因素，从而合成对应的低光输入，构建监督学习所需的低光—正常曝光成对样本 $(x, t)$。最终数据按 train/val/test = 0.8/0.1/0.1 的比例划分用于模型训练与评测。

除合成数据外，本文进一步选取 26 张在真实田间场景下采集的夜间低光 UAV 图像，用于低光退化模型的参数标定与统计分布对齐分析。需要强调的是，这部分真实夜间图像不参与网络训练或定量指标评测，而仅用于验证所构建退化模型在亮度分布、饱和度及 RGB 通道比例等统计特征上对真实夜间农业场景的逼近程度。

基于上述真实夜间数据，我们对合成低光图像与真实夜间 UAV 图像在亮度均值、亮度累积分布函数（CDF）、饱和度分布以及通道比例等统计特征上进行了对比分析（见@fig:dataset_dis）。结果表明，二者在主要统计特性上呈现出较高一致性，说明所提出的低光退化模型能够在统计层面有效近似真实夜间农业 UAV 成像分布。尽管在极暗像素比例及亮度分布尾部仍存在一定差异，但该差异主要源于真实夜间成像条件下低照度环境的复杂性，例如天空背景与月相变化引起的整体照度波动、传感器在高增益条件下噪声统计特性的非稳定性，以及 ISP 对暗部信号的黑电平抬升与压缩处理等因素。上述差异均具有明确的物理与成像机理解释。整体而言，该数据构建策略仍能够在统计层面有效逼近真实夜间农业 UAV 图像分布，为后续低光增强网络的训练与真实场景评估提供可靠的数据基础。

=== 网络结构设计

#figure(
  image("network.png", width: 100%),
  caption: [
    所提出的 $L^3$-AgriUAVNet 网络结构示意图。(a) 整体网络架构，由 Head 模块、L-RFDB、SCSA 和 GM 组成，并通过多阶段特征融合实现低光图像重建；(b) L-RFDB 模块结构，采用深度可分离卷积与残差连接以提升特征表达能力并降低计算复杂度；(c) SCSA 模块结构，通过空间上下文建模与注意力机制增强对低光区域的判别能力；(d) GM 模块结构，通过全局统计信息预测仿射参数（gain 和 bias），用于自适应调节特征分布；(e) Head 模块结构，用于特征映射与输出重建。
  ],
) <fig:backbone>

综合来看，夜间农业无人机影像的低光增强面临多重挑战，包括整体亮度不足、噪声显著放大、局部结构模糊以及不同地物区域亮度分布差异较大等问题。针对上述特点，本文在网络结构设计上未采用大规模深层模型，而是以轻量化和可解释性为目标，构建了 $L^3$-AgriUAVNet（如@fig:backbone 所示）。具体而言，L-RFDB 模块通过残差特征聚合与深度可分离卷积，在保持较低计算复杂度的同时增强了局部纹理与边缘特征表达能力，适用于夜间低对比度场景下的细节恢复；SCSA 模块引入空间上下文与注意力机制，有助于在复杂农田场景中区分不同地物区域，从而缓解低光条件下植被、土壤与背景之间的混淆；同时，GM 模块基于全局统计信息对特征进行自适应调制，以应对夜间 UAV 图像中普遍存在的非均匀照明与亮度分布偏移问题。通过上述模块的协同作用，所提出的网络在有限参数规模下实现了对夜间农业无人机影像的有效增强，为后续作物识别等视觉任务提供了更具判别性的输入特征。这种兼顾增强性能与计算效率的设计，使 $L^3$-AgriUAVNet 更适合在农业无人机等资源受限平台上的实际部署。

网络首先用一个 $3 × 3$ 卷积提取浅层特征 $f_0$，默认通道数 $C = 72$，卷积步幅为 1、padding 为 1，因此空间分辨率保持不变。随后堆叠 $N$ 个 L-RFDB（默认 $N = 4$）得到深层特征 $f_N$。为稳定曝光与白平衡，网络提供全局调制分支 GM，其从浅层特征中预测逐通道的仿射参数并调制深层特征：

$ f_m = f_N ⊙ g + b $

其中 $g, b in RR^(C times 1 times 1)$，$⊙$ 表示逐元素乘法。GM 先通过全局平均池化将 $f_0$ 压缩为通道描述向量，再用两层 MLP 预测 $(g, b)$。为保证训练初期行为接近恒等映射，我们采用 $g = 1 + Delta g$ 的输出形式，使初始阶段 $g ≈ 1, b ≈ 0$，从而稳定优化过程并减少颜色/曝光的震荡。

调制后的特征 $f_m$ 与浅层特征 $f_0$ 在通道维拼接并通过 $1 × 1$ 卷积融合，随后由重建头预测 RGB 残差 $r$。与此同时，细节分支直接从输入 $x$ 估计细节门控图 $r_d$（用于突出纹理与边缘区域的增强幅度）。最终输出采用“输入 + 残差”的形式：

$ y = x + alpha (r ⊙ r_d) $

其中 $alpha$ 为可学习标量（用于自适应控制残差强度）。为避免负亮度与过曝光值，输出端将结果裁剪到 $[0, 1]$。

=== L-RFDB

L-RFDB 是一个轻量残差特征蒸馏块，旨在以较低计算开销增强局部纹理与结构细节的表达能力。其核心思想是在块内逐阶段“蒸馏”出信息更集中的通道子集，同时对剩余通道进行轻量变换以逐步提炼特征。具体地，L-RFDB 共 4 个阶段：前三个阶段从输入特征中蒸馏出 $d$ 个通道，并对剩余通道做卷积变换；第 4 阶段不再蒸馏，直接将剩余通道映射为 $d$ 个通道。四个阶段得到的蒸馏特征在通道维拼接形成 $4d$ 通道特征，再通过 $1 × 1$ 卷积融合回 $C$ 通道。

为降低计算量并提升部署友好性，块内的卷积变换默认采用深度可分离卷积（DWConv + PWConv）替代部分标准 $3 × 3$ 卷积，并在非线性处使用 LeakyReLU。块末端引入轻量空间注意力 SCSA（见下一节）以强调纹理/边缘等高频区域，最终与块输入做残差相加以稳定训练并保留低频结构信息。

=== 轻量空间注意力

SCSA（Strided Context Spatial Attention）用于强化纹理与边缘区域的响应，其结构为“降维 → 下采样 → 局部变换 → 上采样 → 升维 → Sigmoid 门控”。给定输入特征 $u in RR^(C times H times W)$，SCSA 先用 $1 × 1$ 卷积将通道降到 $C/4$ 以降低计算量；随后用 stride=2 的 $3 × 3$ 卷积进行下采样以扩大有效感受野，并通过轻量卷积在低分辨率上进行局部上下文建模；最后通过上采样恢复空间分辨率，并用 $1 × 1$ 卷积升维回 $C$ 通道，得到空间注意力图 $a in [0, 1]^(C times H times W)$。输出采用逐元素门控：

$ u' = u ⊙ a $

从而在保持主干计算开销可控的同时，提升对关键结构区域（叶缘、行间纹理等）的响应强度。

=== 损失函数设计

为缓解低光增强中“变亮但发糊偏色”的问题，我们采用由鲁棒重建、结构梯度与颜色比例一致性组成的加权损失：

$ "LLLoss" = w_("char") L_("char") + w_("grad") L_("grad") + w_("color") L_("color") $

*Charbonnier（鲁棒像素重建）*

$ L_("char") = frac(1, |Omega|) sum_(p in Omega) sqrt((y_p - t_p)^2 + epsilon^2) $

与 $L_1$ 相比，Charbonnier 对噪声与局部异常更鲁棒且梯度更平滑，可提升低光数据上的训练稳定性。

*Gradient loss（梯度一致性 / 结构细节）*

$ L_("grad") = frac(1, |Omega|) sum_(p in Omega) (‖ ∇_x y_p - ∇_x t_p ‖_1 + ‖ ∇_y y_p - ∇_y t_p ‖_1) $

其中 $∇_x, ∇_y$ 为 Sobel 算子在水平方向与垂直方向的梯度响应，对每个 RGB 通道分别计算。该项鼓励边缘与纹理的梯度形状一致，从而减少“变亮但发糊”的现象。

*Color consistency（颜色比例一致性 / 抗偏色）*

先计算每张图像的通道均值向量 $m(y) in RR^3$，再归一化得到通道占比向量 $r(y)$，并采用 $L_1$ 距离约束：

$ r(y) = frac(m(y), sum_(c) m_c(y) + delta) $

$ L_("color") = frac(1, B) sum_(i=1)^B ‖ r(y_i) - r(t_i) ‖_1 $

其中 $m(y)$ 表示每张图像在空间维的通道均值向量，$r(y)$ 表示归一化后的通道占比向量（$delta$ 为稳定项）。该约束旨在抑制增强过程中出现的整体色偏，使增强结果的全局颜色比例更接近正常曝光目标。

== 结果

实验在一台配备 NVIDIA GeForce RTX 4090（24 GB 显存）的工作站上完成，软件环境为 Python 3.12（Ubuntu 22.04）与 PyTorch 2.8.0，CUDA 版本为 12.8。硬件平台包含 16 vCPU 的 Intel Xeon Gold 6430 处理器与 120 GB 系统内存。所有模型在相同的成对数据上训练与评测，输入输出均为 RGB，像素范围归一化到 [0,1]。我们报告 PSNR、SSIM、MAE 与 LPIPS 作为评价指标，并与 RFDN、IMDN、Zero-DCE 等方法进行对比。

=== 综合性能分析

#figure(
  table(
    columns: 7,
    table.header([骨干], [参数量(k)], [FLOPs(G)], [PSNR(dB)↑], [SSIM(%)↑], [MAE(%)↓], [LPIPS(%)↓]),
    [Zero-DEC++], [11.6], [1.01], [11.41], [47.36], [22.89], [41.46],
    [RFDN], [371], [35.23], [23.45], [84.16], [5.32], [15.75],
    [IMDN], [689], [68.67], [24.88], [85.05], [4.53], [11.04],
    [$L^3$-AgriUAVNet], [138], [11.94], [24.71], [84.62], [4.58], [9.80],
  ),
  caption: [
    不同低光图像增强方法在夜间 UAV 场景下的定量性能对比。表中比较了各方法的模型参数量、计算复杂度（FLOPs）以及客观图像质量指标，包括 PSNR、SSIM、MAE 和 LPIPS。其中，PSNR 和 SSIM 的数值越大表示性能越好，MAE 和 LPIPS 的数值越小表示性能越优。该表用于分析不同方法在增强性能与计算效率之间的权衡关系。
  ],
  kind: table,
) <tab:overall>

为全面评估所提出方法的性能，我们选取了具有代表性的无监督曲线增强方法与轻量 CNN 重建方法作为对比基线，重点评估增强质量与计算效率的权衡，以匹配农业无人机端侧部署场景。Zero-DCE++@li2021learning 是一种无需成对数据训练的低光增强方法，通过学习像素级的曝光调节曲线实现亮度增强，具有较高的计算效率。RFDN@liu2020residual 和 IMDN@hui2019lightweight 均为轻量化卷积神经网络模型，最初用于图像超分辨率任务，近年来被广泛应用于低光增强场景，能够在较低参数规模下实现较好的重建效果。

上述方法涵盖了基于曲线调整的无监督方法以及基于卷积神经网络的轻量化重建模型，代表了当前低光图像增强领域中常见且具有实用价值的技术路线。通过与这些方法进行对比，可以更全面地分析所提出 $L^3$-AgriUAVNet 在增强性能与计算效率方面的相对优势。

@tab:overall 在 PSNR 与 SSIM 等像素级指标上取得了最高数值，但其参数量与 FLOPs 显著高于其他方法；RFDN 在复杂度上有所降低，但在预测质量与感知一致性方面仍存在一定差距。相比之下，本文提出的 L³-AgriUAVNet 在仅 138k 参数量与 11.94 G FLOPs 的条件下，实现了与重型模型相近的重建质量（PSNR 24.71 dB，SSIM 84.62%），并在 LPIPS 指标上取得最优结果。较低的 LPIPS 值表明，本文方法在感知一致性与结构保真性方面具有优势，这对于降低下游农业视觉模型在不同田块与不同时期之间的特征漂移尤为重要。Zero-DCE++ 虽具有极低的模型复杂度，但在 SSIM、MAE 与 LPIPS 等指标上明显落后，说明仅依赖曲线映射的轻量方法难以在夜间农业场景中同时兼顾结构细节与颜色稳定性。总体而言，L³-AgriUAVNet 在增强质量与计算开销之间取得了更为均衡的权衡，表现出良好的 *“精度–效率”帕累托特性*，更适合资源受限或需要较高推理吞吐的夜间农业 UAV 应用场景。

=== 运行消耗

#figure(
  image("inference_time.png", width: 80%),
  caption: [
    该图以气泡图形式展示了不同低光图像增强方法在预测质量、推理效率与模型规模之间的综合权衡关系。横轴表示预测相似性指标 LPIPS（数值越小表示预测质量越好，坐标轴由左至右递减），纵轴表示单张图像的推理时间（毫秒），气泡大小对应模型参数量（千）。结果表明，所提出的方法在保持较低 LPIPS 的同时显著降低了推理时间和模型规模，在预测质量与计算效率之间实现了更优的帕累托权衡。Zero-DCE 作为基于曲线映射的参考方法，主要用于展示极端高效率条件下的性能边界，其预测质量相对较低。
  ],
) <fig:time>

如@fig:time 所示，为系统评估不同低光增强方法在实际应用中的可部署性，本文从预测质量、推理效率与模型规模三个维度对代表性方法进行了综合比较，如图 5 所示。预测质量采用 LPIPS 衡量，推理效率以单张图像的平均推理时间表示，模型复杂度则通过参数量进行表征。

从结果可以看出，IMDN 在预测质量方面具有一定优势，但其推理时间与模型规模较大，限制了在实时或边端场景中的应用；RFDN 在计算效率方面有所改善，但在预测质量与模型复杂度之间仍未达到理想平衡。相比之下，L³-AgriUAVNet 在保持较低 LPIPS 的同时显著降低了推理时间与参数量，其整体性能分布位于质量–效率–复杂度空间的帕累托前沿，体现出更优的综合权衡能力。

此外，Zero-DCE 作为基于曲线映射的参考方法，在计算开销上具有明显优势，但其预测质量显著低于基于深度学习的方法。该结果表明，在夜间农业 UAV 场景中，适度的网络建模对于在增强质量与部署效率之间取得平衡仍然是必要的。

=== 消融实验

#figure(
  table(
    columns: 10,
    table.header([], [L-RFDB], [GMod], [SCSA], [LLLoss], [PSNR↑], [SSIM(%)↑], [MAE(%)↓], [LPIPS(%)↓], [参数量(k)]),
    [Baseline], [✓], [], [], [], [21.84], [81.56], [6.14], [19.46], [113],
    [--], [✓], [✓], [], [], [23.39], [81.83], [5.12], [19.28], [122],
    [--], [✓], [✓], [✓], [], [23.60], [82.49], [5.04], [16.76], [138],
    [$L^3$-AgriUAVNet], [✓], [✓], [✓], [✓], [24.71], [84.62], [4.58], [9.80], [138],
  ),
  caption: [
    $L^3$-AgriUAVNet 的消融实验结果。通过逐步引入 L-RFDB、全局调制模块（GMod）、SCSA 注意力模块以及低光重建损失（LLLoss），分析各组件对低光图像增强性能的影响。表中符号 ✓ 表示对应模块或损失被启用，“–”表示未使用。评价指标包括 PSNR、SSIM、MAE、LPIPS 以及模型参数量，用于综合评估各组件在性能提升与模型复杂度之间的贡献。
  ],
  kind: table,
) <tab:ablation>

Baseline（仅使用 L-RFDB）作为起点，取得 21.84 PSNR / 81.56 SSIM，且误差与预测指标相对较弱，参数量为 113k。在 Baseline 上引入 GMod 与 SCSA 后，性能提升最明显：PSNR 提升了 +1.76，SSIM 提升至了 0.93，MAE 降至 5.04，LPIPS 降至 16.76，表明结构建模与注意力机制对重建质量贡献显著；参数量小幅增加到 122k。在此基础上进一步加入 LLLoss，可继续改善像素级重建（PSNR 24.66，MAE 4.66），但 LPIPS 略有回升（10.02），说明该损失更偏向提高客观保真度而不一定同步提升预测相似性；参数量增加至 138k。完整模型（$L^3$-AgriUAVNet：L-RFDB + GMod + SCSA + LLLoss）取得了所有指标上的最佳性能，特别是 LPIPS 大幅提升。这一结果体现了不同模块与改进的有效性，在略微参数量上升的情况下，性能取得了较大提升。说明了 $L^3$-AgriUAVNet 的性能来自高效模块直接的组合，而非简单的参数堆叠。

=== 真实数据测试

#figure(
  image("res_dis.png", width: 100%),
  caption: [
    面向农业场景的夜间 UAV 图像低光增强结果的统计分布对比。每一行依次展示对应方法的输入或增强图像，以及其亮度直方图、亮度累积分布函数（CDF）、饱和度直方图和 RGB 通道比例。图中 (a) 为真实夜间农田图像，(b) 为真实白天农田参考图像，(c)–(f) 分别为 L³-AgriUAVNet、Zero-DEC++、RFDN 和 IMDN 的增强结果。可以看出，真实夜间图像在亮度与饱和度分布上均呈现明显退化特征，而真实白天图像具有更合理的亮度动态范围和中低饱和度分布，符合农业场景的实际成像特性。相比现有方法，L³-AgriUAVNet 在提升整体亮度的同时，其亮度与饱和度分布更接近真实白天参考，且保持稳定的 RGB 通道比例，有效避免了过饱和和色偏问题，从而为后续农业视觉预测与分析任务提供更加稳定、可靠的图像输入。
  ],
) <fig:real>

如@fig:real 所示，为系统评估不同低光增强方法在亮度恢复与颜色保真性方面的表现，本文从亮度分布、亮度累积分布函数（CDF）、饱和度分布以及 RGB 通道比例四个维度，对真实夜间图像、真实白天参考图像及多种增强结果进行了统计分析。如图所示，真实夜间农田图像的亮度分布高度集中于低亮度区间，其 CDF 在极低亮度范围内迅速饱和，表明大部分像素处于接近黑电平区域，信噪比较低。同时，其饱和度分布几乎塌陷在零附近，说明夜间低光条件下不仅亮度严重退化，颜色信息亦显著丢失。

相比之下，真实白天图像呈现出更为合理的亮度统计特性，其亮度直方图呈现出以中低亮度为主的单峰分布，并伴随自然衰减的长尾特征，CDF 曲线平滑且动态范围合理，符合真实农田场景在自然光照条件下的亮度分布特性。此外，其饱和度主要分布于中低区间，未出现高饱和聚集现象，反映出农田场景在自然光照条件下以柔和、真实色彩为主的固有特性。RGB 通道比例中绿色通道略占优势，符合植被场景的光谱响应特征。

从增强结果来看，所提出的 $L^3$-AgriUAVNet 在亮度与颜色统计特性上均表现出与真实白天图像高度一致的分布趋势。其亮度直方图与 CDF 曲线整体形态明显向白天参考靠拢，表明模型能够在有效提升整体亮度的同时保持合理的动态范围。更为重要的是，其饱和度分布在整体右移的同时未产生异常尖峰或过度拉伸，饱和度区间与真实白天图像高度重合，说明该方法在增强颜色信息的同时避免了过饱和问题，具有良好的颜色保真性。此外，RGB 通道比例保持稳定，未引入明显色偏。值得注意的是，其饱和度分布区间与真实白天农田图像高度一致，进一步表明该方法并非通过简单提高饱和度来改善视觉效果，而是实现了对低光条件下退化颜色信息的有效恢复。

相比之下，Zero-DEC++ 方法虽然在一定程度上提升了图像亮度，但其亮度 CDF 呈现出明显的阶梯状特征，反映出非平滑的亮度映射过程，易引入量化伪影；其饱和度分布高度集中且异常尖锐，表明像素级曲线拟合易导致颜色分布受限并引入失真。同时，其 RGB 通道比例出现明显偏移，存在较为严重的色偏现象。RFDN 与 IMDN 方法在亮度恢复方面取得了一定效果，但其饱和度分布仍明显偏低且范围受限，未能充分恢复夜间图像中退化的色彩信息。

综上所述，统计分布分析表明，L³-AgriUAVNet 不仅能够有效提升夜间农田图像的亮度水平，还能够在不引入过饱和和色偏的前提下恢复接近真实白天场景的颜色分布特性，为后续农业视觉任务提供更加稳定且可靠的输入。

#figure(
  image("res_detail.png", width: 100%),
  caption: [
    实际夜间农业无人机图像的局部细节恢复视觉对比。(a) 原始夜间低光图像；(b) 采用 IMDN 方法的增强结果；(c) 采用所提出 $L^3$-AgriUAVNet 的增强结果。红色实框标示对比区域，虚线连接对应的局部放大细节。可以观察到，与 IMDN 相比，$L^3$-AgriUAVNet 在植被区域的纹理结构连续性、边缘清晰度以及噪声抑制方面表现更为稳定，有效缓解了低光条件下的细节模糊与噪声干扰。
  ],
) <fig:res_detail>

如@fig:res_detail 所示，为进一步分析不同低光增强方法在真实夜间农业 UAV 场景下的细节恢复能力，本文选取冠层结构较为复杂的局部区域进行放大对比。可以观察到，IMDN 在整体亮度提升方面效果显著，但在极低照度条件下，其增强结果在局部区域呈现出明显的过度平滑现象，冠层纹理被压缩为低频斑块，叶缘及冠层内部的空间结构可分性下降。相比之下，所提出的 $L^3$-AgriUAVNet 在获得与 IMDN 相当亮度水平的同时，能够更好地保留冠层颗粒结构与空间纹理连续性，局部区域中作物冠层与背景之间的结构边界更加清晰。该结果表明，$L^3$-AgriUAVNet 并非通过简单增强对比度或锐化操作改善视觉效果，而是在结构感知的前提下实现细节恢复。这一优势与网络中引入的轻量特征蒸馏机制及跨尺度空间注意力设计相一致，使模型能够在弱光条件下优先强化结构显著区域，同时避免在噪声主导区域引入过度增强，从而获得更具结构一致性的增强结果。

=== 下游任务增强实验

#figure(
  image("res_task.png", width: 100%),
  caption: [
    用于评估低光增强的下游作物类型分类案例研究概述。一个区域级别标注的农业场景被划分为不重叠的带有作物真实标签的图像块。使用一个轻量级分类器（SimpleNet）对图像块进行分类任务。在测试阶段，将原始低光图像和由 L³-AgriUAVNet 增强的图像在相同设置下输入到训练后的分类器中。通过对分类性能进行评估，从而实现对增强前端的受控比较。混淆矩阵显示，在原始低光照条件下，分类器几乎只预测一个主要类别，表明可区分的视觉特征严重退化。经过增强后，类别可分性显著恢复，说明 L³-AgriUAVNet 为下游农业分析提供了更具任务相关性的表征。
  ],
) <fig:res_task>

#figure(
  table(
    columns: 5,
    table.header([Input], [Accuracy], [Macro-F1], [Macro-Precision], [Macro-Recall]),
    [Raw low-light], [0.250], [0.069], [0.040], [0.250],
    [Enhanced ($L^3$-AgriUAVNet)], [*0.355*], [*0.250*], [*0.214*], [*0.355*],
  ),
  caption: [
    在低光照条件下的下游植物分类性能。SimpleNet 在相同设置下分别在原始低光照图像和增强图像上进行训练和评估。
  ],
  kind: table,
) <tab:downstream_classification>

为评估所提出低光图像增强方法在实际农业感知任务中的潜在价值，我们设计了一个下游作物类型分类的案例研究（case study）。需要强调的是，该实验的目标并非构建高精度分类模型，而是通过对比不同输入条件下分类性能的变化，分析低光图像增强是否能够提升对农业语义判别有意义的视觉表征质量，用于评估低光增强是否能够恢复对农业语义判别有意义的视觉表征。为避免分类模型本身的容量掩盖输入质量差异，本文刻意选用结构极简、表达能力受限的 SimpleNet 作为分类器。当输入图像中可区分的纹理与结构信息严重退化时，该类模型更容易出现判别塌缩；反之，若增强结果能够恢复关键结构与颜色线索，即便是轻量分类器亦可学习到更具区分性的特征表示。

实验基于一幅分辨率为 5280 × 3956 的无人机农业场景图像。该场景涵盖小麦（wheat）、玉米（maize）、林地（tree）和草地（grass）等典型地物类型。首先对原始场景进行区域级人工标注，获得作物类型的 ground truth，随后将整幅图像裁剪为不重叠的 224 × 224 图像块（patch）。每个 patch 根据其中心点所对应的标注区域被赋予唯一的类别标签。最终共获得 391 个有效 patch，包含 144 个小麦样本、136 个林地样本、60 个草地样本和 51 个玉米样本。该样本分布反映了真实农业场景中不同地物类型的空间占比特征。考虑到类别分布并非完全均衡，实验结果中除总体准确率外，进一步报告了 macro-F1 指标及混淆矩阵，以便更全面地分析各类别的识别行为。

下游任务采用一个结构极简的卷积神经网络（SimpleNet）作为分类模型。该网络由两层 $3 × 3$ 卷积层、全局平均池化层以及一个全连接分类层构成，参数规模较小，为保证对比的公平性，在不同输入条件下均采用相同的网络结构、优化器设置及训练策略，仅输入数据分布存在差异。具体而言，SimpleNet 分别在以下两种数据集上独立训练并测试：1）原始低光图像 patch；2）经所提出 $L^3$-AgriUAVNet 增强后的低光图像 patch。通过这种设置，可以在保持分类模型和训练协议一致的前提下，分析低光增强对下游分类任务学习过程和最终判别能力的影响。

@fig:res_task 展示了在原始低光输入和增强后输入条件下训练得到的 SimpleNet 分类结果的混淆矩阵。可以观察到，在原始低光图像上训练和测试时，分类模型表现出明显的性能退化，预测结果高度集中于单一主导类别（grass），呈现出典型的分类塌缩（collapse）现象。这表明在严重低光条件下，图像中与作物类型区分相关的纹理和结构特征难以被有效学习，从而限制了分类模型的判别能力。需要注意的是，增强后 Macro-F1 与 Accuracy 同步提升，且 Precision 与 Recall 均显著改善，表明性能提升并非来自单一类别的预测偏置，而是多类别判别能力的整体恢复。

相比之下，当使用经 $L^3$-AgriUAVNet 增强后的图像进行训练和测试时，分类结果得到显著改善（见@tab:downstream_classification）。混淆矩阵显示，不同作物类别之间逐渐形成可区分的预测结构，小麦、林地和草地等类别的识别能力明显提升，整体分类行为不再退化为单一类别预测。尽管部分类别（如玉米）仍存在一定程度的混淆，但这种误差模式更加符合实际农业场景中不同作物外观相似性的特征，而非由低光退化引起的系统性失败。

上述结果表明，所提出的低光增强方法不仅改善了图像的整体可视性，而且在一定程度上增强了对农业语义判别具有关键作用的视觉信息，使得即便是结构极简的分类模型，也能够在增强后的数据上学习到更具区分性的特征表示。

== 讨论

本文针对夜间农业无人机影像在低光条件下面临的亮度不足、噪声显著、结构细节退化以及非均匀照明等问题，提出了一种轻量化的低光增强网络 $L^3$-AgriUAVNet。与追求大规模模型容量不同，本文从农业无人机实际应用出发，强调在有限计算资源下实现稳定、可解释且具有实际增益的图像增强效果。

从定量结果来看，所提出方法在 PSNR、SSIM、MAE 和 LPIPS 等指标上均取得了较为稳定的提升，且在参数规模和计算复杂度方面显著低于部分现有方法。这表明，通过合理的结构设计与模块协同，即使在轻量化约束下，仍可有效改善夜间低光影像的整体视觉质量与结构一致性。消融实验进一步验证了各组成模块在性能提升中的作用，其中 L-RFDB 有助于增强局部纹理与边缘表达，SCSA 模块提升了复杂农田场景中不同地物区域的区分能力，而 GM 模块则有效缓解了夜间 UAV 图像中常见的亮度分布偏移问题。

在真实夜间农业无人机影像上的定性对比结果显示，$L^3$-AgriUAVNet 在植被区域的纹理连续性与结构可辨性方面表现更为稳定，能够在抑制噪声的同时避免过度平滑现象。这对于夜间农业场景尤为重要，因为作物冠层结构、行距纹理以及地物边界信息往往是后续农业视觉任务（如作物识别、长势评估和作业决策）的关键判别依据。

此外，通过引入最小成本的下游分类任务作为功能性验证（sanity check），实验结果表明，基于增强图像训练的简单分类模型在整体性能上优于直接使用原始低光图像的情况。尽管该分类实验并非旨在构建最优识别模型，但其结果从侧面验证了低光增强在提升图像判别性方面的实际价值，说明所提出方法不仅在像素层面改善视觉质量，也能够为后续农业智能感知任务提供更具信息量的输入。

从农业应用角度看，$L^3$-AgriUAVNet 的轻量化设计使其更适合部署于计算资源受限的无人机平台或边缘设备，有助于扩展农业无人机在夜间巡检、作业监测以及全天候农业感知等应用场景。这种兼顾增强效果与计算效率的设计思路，为农业领域中低光视觉感知问题提供了一种具有实际可行性的解决方案。

需要指出的是，本文的研究仍存在一定局限性。例如，低光退化模型虽在统计分布层面与真实夜间影像具有较高一致性，但仍难以完全覆盖复杂环境光源变化和极端照明条件；此外，下游任务验证仅采用了简单分类模型，尚未在更复杂的农业视觉任务中进行系统评估。未来工作将考虑引入更丰富的真实夜间数据，并在目标检测、语义分割等农业关键任务中进一步验证所提出方法的泛化能力与实用价值。

== 总结

本文针对夜间农业无人机影像在低光条件下面临的可视性差、噪声显著以及结构信息退化等问题，提出了一种轻量化低光图像增强方法 $L^3$-AgriUAVNet。该方法从农业无人机实际应用需求出发，在网络结构设计中兼顾增强性能与计算效率，通过多模块协同提升夜间低光影像的整体可视性与结构一致性。

实验结果表明，所提出的方法在多项客观图像质量指标上取得了稳定提升，并在真实夜间农业 UAV 场景下展现出良好的视觉效果。进一步的消融实验和下游任务验证表明，各组成模块在增强低光影像判别性方面具有明确作用，使增强结果更有利于后续农业智能感知任务的开展。

得益于其轻量化设计，$L^3$-AgriUAVNet 具有较好的部署潜力，可应用于夜间农业巡检、作业监测以及全天候农业感知等场景。未来工作将进一步扩展真实夜间农业数据规模，并在目标检测、语义分割等关键农业视觉任务中系统评估所提出方法的泛化能力与实用价值。
