= 低光图像的合成实验

== 摘要

夜间农业无人机（UAV）影像在作物表型监测、田间巡检与精准管理中具有重要应用潜力，但受低照度条件下曝光不足、噪声显著及颜色不稳定等成像退化因素影响，图像质量往往难以稳定支撑后续视觉分析任务。与此同时，真实场景中严格配准的“夜间低光—白天正常曝光”成对样本获取成本高、周期长，成为农业低光视觉研究的关键瓶颈。

针对上述问题，本文聚焦“低光数据合成”这一基础环节，构建了一套物理与 ISP 启发的低光退化建模流程，在 patch 级别生成可控的低光—正常曝光成对样本。该流程在 RGB 域联合模拟曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角，以覆盖夜间农业 UAV 成像中的主要退化因素。

此外，本文引入真实夜间 UAV 图像进行外部统计对齐，从亮度、饱和度与 RGB 通道比例等维度评估合成数据与真实分布的一致性。结果表明，所构建的合成数据在关键统计特性上能够有效逼近真实夜间场景，可作为后续低光增强与全天候识别任务的可靠训练数据基础。

*关键词：* 农业；无人机遥感；低光数据合成；统计对齐

== 引言

无人机平台能够在大尺度农田上快速获取高分辨率可见光影像，为作物长势评估、病虫害巡查与表型分析提供高时空分辨率数据@guebsi2024drones @aierken2024review。然而在夜间或弱光条件下，UAV 影像常出现整体曝光不足、噪声显著、对比度下降与颜色偏移等问题@widartono2025aerial @fu2022highlightnet，直接影响后续检测、分割与计数等视觉任务的稳定性与可用性@silwal2021robust。

在农业生产中，夜间影像获取并非边缘需求。受白天温度、风速、人员与机械调度影响，夜间与清晨时段反而更利于持续开展航测与巡检@flynn2026assessing。以棉花等大田作物为例，生长中后期群体结构变化快、田块跨度大，影像质量一旦不稳，易造成下游任务在田块间和批次间表现波动@bendig2015combining @yang2017unmanned @kamilaris2018deep @xu2019multispectral。

从成像链路看，夜间 UAV 退化通常是多因素叠加：低照度引起曝光与增益变化，使噪声显著增强@kurimo2009effect；机体振动与飞行速度放大运动模糊风险@tsekhmystro2025uav；ISP 在弱光下更易产生纹理涂抹与伪影@li2021low；复杂反射光又进一步扰动白平衡与色彩一致性@lu2025unsupervised。上述因素共同导致夜间图像在亮度、纹理和颜色三个层面同步退化。

现有低光增强研究虽然在公开数据集上进展迅速，但农业夜间场景缺少高质量配准训练对，导致“方法有效但数据支撑不足”的矛盾长期存在。相比直接依赖难以获取的真实配准数据，构建可解释、可控且可校验的合成退化流程，是提升农业低光研究可复现性与工程可用性的关键路径。

围绕这一目标，本文工作聚焦数据层面，主要贡献如下：

+ *提出面向农业 UAV 的低光成对数据合成流程：* 从正常曝光影像裁剪多尺度 patch，并按成像机理注入曝光衰减、通道偏移、噪声、轻度模糊与暗角等退化，构建可控监督训练对。
+ *建立物理与 ISP 启发的退化参数体系：* 在统一数学表达下给出各退化算子的采样区间与组合方式，使数据生成过程具备可解释性与可复现性。
+ *引入真实夜间样本进行统计一致性验证：* 从亮度、饱和度和通道比例等统计维度评估合成数据与真实分布的一致性，为后续增强与识别模型提供可靠的数据基础。

== 方法与数据

本章涉及的研究区域与低光成对数据集构建流程统一归档在@chap:dataset，本章以下重点给出低光退化模型的数学定义与参数设置。

=== 低光退化模型

为在缺乏严格配准的夜昼成对样本条件下构建可控训练数据，我们在 RGB 图像域设计一种物理与 ISP（Image Signal Processing）启发的低光退化模型。给定正常曝光图像块（8-bit RGB）$bold(I) in [0, 255]^(H times W times 3)$，先归一化得到强度表示：

$ bold(X) = frac(bold(I), 255) in [0, 1]^(H times W times 3). $

给定归一化后的正常曝光图像块 $bold(X) in [0, 1]^(H times W times 3)$，模型通过依次模拟欠曝、白平衡漂移、Poisson--Gaussian 噪声，以及轻度 ISP/光学伪影（模糊与暗角）生成低光观测 $bold(Y)$。整体前向过程表示为：

$ bold(Y) = cal(V)(cal(B)(Gamma_(gamma)(T_(tau)(cal(N)(cal(W)(alpha bold(X)))))))) $

其中 $cal(W)$ 表示通道增益（白平衡）、$cal(N)$ 表示噪声注入、$T_(tau)$ 与 $Gamma_(gamma)$ 分别表示 tone 映射与 gamma 映射、$cal(B)$ 表示轻度模糊、$cal(V)$ 表示暗角（径向照度衰减）。下文对各算子给出具体定义及参数采样范围。

*曝光衰减（Exposure reduction）*

在低照度条件下，相机往往通过降低曝光或增益调整导致整体信号幅度下降。我们使用线性缩放模拟欠曝：

$ bold(X)_e = alpha bold(X), alpha ∼ cal(U)(0.08, 0.30). $

*通道增益（White balance / ISP color bias）*

为刻画弱光条件下 ISP 白平衡与通道增益的漂移，引入通道独立增益向量 $bold(g) = (g_r, g_g, g_b)$，并对三通道逐元素缩放：

$ bold(X)_("wb") = cal(W)(bold(X)_e) = bold(X)_e ⊙ bold(g), g_c ∼ cal(U)(g_(c, "min"), g_(c, "max")), c in {r, g, b}. $

本文取 $g_r ∼ cal(U)(1.05, 1.18)$，$g_g ∼ cal(U)(0.95, 1.03)$，$g_b ∼ cal(U)(0.98, 1.06)$，并进行裁剪以保证数值稳定：

$ bold(X)_("wb") ← op("clip")(bold(X)_("wb"), 0, 1). $

*Poisson--Gaussian 噪声（Shot + read noise）*

夜间噪声通常由信号相关的散粒噪声（shot noise）与信号无关的读出噪声（read noise）共同构成。我们采用 Poisson--Gaussian 近似进行建模：

$ bold(X)_p = frac(1, k) · op("Poisson")(k · bold(X)_("wb")), k ∼ cal(U)(30, 100). $

较小的 $k$ 对应更强的相对散粒噪声强度。随后叠加零均值高斯噪声以模拟读出噪声：

$ bold(X)_n = bold(X)_p + epsilon, epsilon ∼ cal(N)(0, sigma_r^2), sigma_r ∼ cal(U)(0.004, 0.020). $

最终结果被裁剪至 $[0, 1]$ 以满足图像强度范围约束。

*Tone 映射与 gamma（Tone mapping + gamma）*

为在 RGB 域近似 ISP 的 tone curve 以及显示域非线性响应，我们采用幂律形式的 tone 与 gamma 变换：

$ bold(X)_t = T_(tau)(bold(X)_n) = bold(X)_n^tau, tau ∼ cal(U)(0.90, 1.05). $

$ bold(X)_g = Gamma_(gamma)(bold(X)_t) = bold(X)_t^gamma, gamma ∼ cal(U)(1.2, 1.8). $

*轻度模糊（Mild blur）*

考虑到弱光条件下 ISP 降噪、轻微失焦或运动引起的细节软化，使用高斯核近似点扩散函数（PSF）：

$ bold(X)_b = cal(B)(bold(X)_g) = bold(X)_g * cal(G)(sigma_b), sigma_b ∼ cal(U)(0.00, 0.60). $

当 $sigma_b = 0$ 时跳过该步骤。

*暗角（Vignetting / illumination falloff）*

为模拟镜头暗角或空间非均匀照度，引入径向衰减掩膜 $M(bold(p))$。令归一化坐标 $tilde(x), tilde(y) in [-1, 1]$，$r^2 = tilde(x)^2 + tilde(y)^2$，则：

$ M(bold(p)) = 1 - s · op("clip")(r^2, 0, 1), s ∼ cal(U)(0.00, 0.15). $

并得到：

$ bold(Y) = cal(V)(bold(X)_b) = bold(X)_b ⊙ M(bold(p)). $

*量化输出（8-bit quantization）*

最后将结果裁剪并映射回 8-bit 空间：

$ bold(Y)_8 = op("round")(255 · op("clip")(bold(Y), 0, 1)). $

需要指出的是，本模型在 RGB 域实现，未显式模拟 RAW 与去马赛克过程，因此属于 image-domain approximation；但其包含低光退化的关键统计因素（曝光降低、通道偏置、Poisson--Gaussian 噪声、轻度模糊与暗角），可用于构造训练数据。所有参数均在对应区间内均匀采样，以增强退化多样性并提升数据覆盖能力。

=== 数据集制作

针对真实夜间农业 UAV 影像难以获取严格配准昼夜样本的问题，本文采用“合成成对训练 + 真实夜间外部验证”的策略构建数据集，以在可控监督学习条件下提升模型对真实场景的适用性与鲁棒性。

#figure(
  image("dataset_aug.png", width: 100%),
  caption: [
    用于低光增强的训练对生成流程。将正常曝光的无人机图像裁剪成多尺度补丁，然后通过曝光衰减、噪声注入和可选的轻微模糊进行降质，以合成对应的低光输入，从而生成用于监督训练的成对样本 $(x, t)$。
  ],
) <fig:lowlight_pairgen>

如@fig:lowlight_pairgen 所示，在每张图像的四角与中心区域进行带抖动采样并随机选择缩放因子以模拟不同飞行高度带来的细节软化；随后对 patch 进行随机旋转与水平翻转等增强；最后对目标 patch 施加曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角等退化因素，合成对应低光输入，得到用于监督学习的低光—正常曝光成对样本 $(x, t)$。最终数据按 train/val/test = 0.8/0.1/0.1 的比例划分。

#figure(
  image("dataset_dis.png", width: 80%),
  caption: [
    合成低光退化图像与真实夜间 UAV 图像的统计一致性分析。(a) 多维图像统计指标（亮度、对比度、饱和度、RGB 通道比例等）的均值及标准差对比，(b) 合成数据与真实数据在各统计指标上的绝对差异，(c) 亮度（luma）直方图分布对比。结果表明，所构建的合成低光退化在多项关键统计特性上与真实夜间 UAV 图像具有良好一致性，为后续低光增强模型训练与全天候识别任务提供了合理的数据基础。
  ],
) <fig:lowlight_stats>

基于真实夜间数据对齐结果表明，合成低光图像在亮度、饱和度与 RGB 通道比例等关键统计特性上能够有效逼近真实夜间农业 UAV 成像分布，为后续增强与识别研究提供可靠的数据基础。

== 总结

本章围绕夜间农业 UAV 影像缺少严格配准样本的问题，构建了面向低光场景的合成成对数据流程。通过物理与 ISP 启发的退化建模，数据生成过程具备可解释性和可复现性；通过真实夜间样本的统计对齐验证，证明合成数据在亮度、饱和度与通道比例等关键分布上与真实场景具有较好一致性。该数据基础为下一章的低光增强与全天候识别模型提供了稳定输入。
