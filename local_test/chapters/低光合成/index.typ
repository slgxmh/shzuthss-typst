= 面向夜间 UAV 场景的低光成对数据构建与退化建模

== 引言

第 4 章完成了正常光照条件下的轻量级双指标识别，农业 UAV 影像在夜间和日落弱光条件下仍会受到曝光不足、噪声、颜色偏移与运动模糊等退化影响。本章承接第 2 章的数据基础与第 4 章的识别需求，构建物理与 ISP 启发的可控低光退化模型，生成低光—正常曝光成对样本，并利用真实夜间影像校准和评价其统计一致性。本章其余部分安排如下：5.2 分析真实夜间场景特征与数据需求；5.3 介绍低光退化建模方法；5.4 说明低光成对数据集构建流程；5.5 评价合成数据与真实夜间数据的一致性；5.6 总结本章工作。

== 真实夜间场景特征与数据需求

本研究涉及的研究区域与低光成对数据集构建流程统一归档在@chap:dataset，以下重点给出低光退化模型的数学定义与参数设置。

与直接学习整体映射相比，本研究更强调退化链路的分解建模。若合成流程缺乏可解释的算子结构，即使生成图像在视觉上较为逼真，也难以分析模型学习到的退化规律，并据此调整数据分布。将低光退化拆解为曝光、颜色、噪声、模糊和空间照度衰减等环节，有助于建立参数采样、统计对齐和下游训练之间的对应关系。

本研究采用“合成成对训练 + 真实夜间校准”的技术路线。成熟期棉花低光影像同时存在亮度压缩、颜色偏移、纹理弱化和局部照度不均。如@fig:lowlight_scenario 所示，真实样本的强度、局部对比度和通道比例均发生变化。分解这些退化因素便于控制训练数据分布并分析增强结果。

#figure(
  image("fig1_scenario.png", width: 100%),
  caption: [
    农业 UAV 低光成像问题。通过田间影像与图像块级统计展示低光条件下的强度压缩、局部对比度下降与通道比例偏移；真实低光图像块来自 26 张棉花低光校准影像的裁剪样本，图像块数量不等同于原始影像数。
  ],
) <fig:lowlight_scenario>

本章为第 6 章的低光增强与任务适用性评价提供训练数据，重点说明低光退化的构造与校准方法。

== 低光退化建模方法

=== 建模目标与总体流程

为在缺乏严格配准的夜昼成对样本条件下构建可控训练数据，本研究在 RGB 图像域设计一种物理与 ISP（Image Signal Processing）启发的低光退化模型。给定正常曝光图像块（8-bit RGB）$bold(I) in [0, 255]^(H times W times 3)$，先归一化得到强度表示：

$ bold(X) = frac(bold(I), 255) in [0, 1]^(H times W times 3). $

给定归一化后的正常曝光图像块 $bold(X) in [0, 1]^(H times W times 3)$，模型通过依次模拟欠曝、白平衡漂移、Poisson--Gaussian 噪声，以及轻度 ISP/光学伪影（模糊与暗角）生成低光观测 $bold(Y)$。整体前向过程表示为：

$ bold(Y) = cal(V)(cal(B)(Gamma_(gamma)(T_(tau)(cal(N)(cal(W)(alpha bold(X)))))))) $

其中 $cal(W)$ 表示通道增益（白平衡）、$cal(N)$ 表示噪声注入、$T_(tau)$ 与 $Gamma_(gamma)$ 分别表示 tone 映射与 gamma 映射、$cal(B)$ 表示轻度模糊、$cal(V)$ 表示暗角（径向照度衰减）。下文对各算子给出具体定义及参数采样范围。

=== 曝光衰减与颜色偏移

*曝光衰减（Exposure reduction）*

在低照度条件下，相机往往通过降低曝光或增益调整导致整体信号幅度下降。本研究使用线性缩放模拟欠曝：

$ bold(X)_e = alpha bold(X), alpha ∼ cal(U)(0.08, 0.30). $

该范围覆盖可辨识弱光至显著欠曝状态。较高的 $alpha$ 会使样本接近正常曝光，过低则会造成结构信息大量损失，因此参数范围以保留可恢复结构为界。

*通道增益（White balance / ISP color bias）*

为刻画弱光条件下 ISP 白平衡与通道增益的漂移，引入通道独立增益向量 $bold(g) = (g_r, g_g, g_b)$，并对三通道逐元素缩放：

$ bold(X)_("wb") = cal(W)(bold(X)_e) = bold(X)_e ⊙ bold(g), g_c ∼ cal(U)(g_(c, "min"), g_(c, "max")), c in {r, g, b}. $

本研究取 $g_r ∼ cal(U)(1.05, 1.18)$，$g_g ∼ cal(U)(0.95, 1.03)$，$g_b ∼ cal(U)(0.98, 1.06)$，并进行裁剪以保证数值稳定：

$ bold(X)_("wb") ← op("clip")(bold(X)_("wb"), 0, 1). $

通道增益用于近似自动白平衡和 ISP 增益控制造成的颜色偏移。脱叶率与吐絮率识别依赖绿色叶片、棕褐枝条和白色棉絮之间的相对颜色关系，因此增益范围需覆盖真实偏色，同时避免破坏原有颜色语义。

=== 噪声、非线性映射与局部退化

*Poisson--Gaussian 噪声（Shot + read noise）*

夜间噪声通常由信号相关的散粒噪声（shot noise）与信号无关的读出噪声（read noise）共同构成。本研究采用 Poisson--Gaussian 近似进行建模：

$ bold(X)_p = frac(1, k) · op("Poisson")(k · bold(X)_("wb")), k ∼ cal(U)(30, 100). $

较小的 $k$ 对应更强的相对散粒噪声强度。随后叠加零均值高斯噪声以模拟读出噪声：

$ bold(X)_n = bold(X)_p + epsilon, epsilon ∼ cal(N)(0, sigma_r^2), sigma_r ∼ cal(U)(0.004, 0.020). $

最终结果被裁剪至 $[0, 1]$ 以满足图像强度范围约束。

低光条件下信噪比下降，叶缘、棉絮轮廓和行间纹理容易受到破坏。Poisson 噪声描述低光子计数下的信号相关扰动，高斯噪声近似传感器读出噪声。二者叠加用于覆盖夜间成像中的主要噪声形态。

*Tone 映射与 gamma（Tone mapping + gamma）*

为在 RGB 域近似 ISP 的 tone curve 以及显示域非线性响应，本研究采用幂律形式的 tone 与 gamma 变换：

$ bold(X)_t = T_(tau)(bold(X)_n) = bold(X)_n^tau, tau ∼ cal(U)(0.90, 1.05). $

$ bold(X)_g = Gamma_(gamma)(bold(X)_t) = bold(X)_t^gamma, gamma ∼ cal(U)(1.2, 1.8). $

Tone 与 gamma 变换用于近似 ISP 曲线和显示域映射造成的非线性亮度压缩。仅采用线性缩放时，即使均值亮度接近真实样本，直方图形状和局部对比度仍可能存在明显差异。

*轻度模糊（Mild blur）*

考虑到弱光条件下 ISP 降噪、轻微失焦或运动引起的细节软化，使用高斯核近似点扩散函数（PSF）：

$ bold(X)_b = cal(B)(bold(X)_g) = bold(X)_g * cal(G)(sigma_b), sigma_b ∼ cal(U)(0.00, 0.60). $

当 $sigma_b = 0$ 时跳过该步骤。

轻度模糊用于近似平台扰动、轻微失焦和 ISP 降噪造成的细节软化。参数范围不覆盖大尺度拖影，以避免将训练任务转变为重度去模糊。

*暗角（Vignetting / illumination falloff）*

为模拟镜头暗角或空间非均匀照度，引入径向衰减掩膜 $M(bold(p))$。令归一化坐标 $tilde(x), tilde(y) in [-1, 1]$，$r^2 = tilde(x)^2 + tilde(y)^2$，则：

$ M(bold(p)) = 1 - s · op("clip")(r^2, 0, 1), s ∼ cal(U)(0.00, 0.15). $

并得到：

$ bold(Y) = cal(V)(bold(X)_b) = bold(X)_b ⊙ M(bold(p)). $

径向衰减用于模拟镜头暗角和局部照度不均造成的边缘变暗，使训练样本包含空间非均匀亮度变化。

=== 参数采样范围与实现原则

*量化输出（8-bit quantization）*

最后将结果裁剪并映射回 8-bit 空间：

$ bold(Y)_8 = op("round")(255 · op("clip")(bold(Y), 0, 1)). $

本模型在 RGB 域实现，未显式模拟 RAW 与去马赛克过程，属于图像域近似。模型包含曝光降低、通道偏置、Poisson--Gaussian 噪声、轻度模糊与暗角等关键退化因素，各项参数均在设定区间内随机采样，以生成稳定、可控且具有多样性的监督样本。

== 低光成对数据集构建流程

=== 正常曝光源数据与 patch 生成

正常曝光数据采集于 2025 年 5 月 6 日至 6 月 12 日，覆盖作物生长的不同时期与田间外观变化。本研究从多期航测数据中选取小麦、棉花和玉米影像，共获得 1306 张原始 UAV 图像。不同作物的冠层结构和背景组合扩大了源数据的场景范围，有利于模拟农业 UAV 影像中的共性低光退化。

针对真实夜间农业 UAV 影像难以获取严格配准昼夜样本的问题，本研究采用“合成成对训练 + 真实夜间外部验证”的策略构建数据集，以在可控监督学习条件下提升模型对真实场景的适用性与鲁棒性。

#figure(
  image("dataset_aug.png", width: 100%),
  caption: [
    用于低光增强的训练对生成流程。将正常曝光的无人机图像裁剪成多尺度补丁，然后通过曝光衰减、噪声注入和可选的轻微模糊进行降质，以合成对应的低光输入，从而生成用于监督训练的成对样本 $(x, t)$。
  ],
) <fig:lowlight_pairgen>

如@fig:lowlight_pairgen 所示，首先在每张图像的四角和中心区域进行带抖动采样，并随机选择缩放因子；随后进行随机旋转和水平翻转；最后施加曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角，得到低光—正常曝光成对样本 $(x, t)$。数据按 train/val/test = 0.8/0.1/0.1 的比例划分。

多位置采样、尺度抖动和几何增强用于覆盖作物结构、背景组合与观测尺度的变化；退化参数随机采样用于扩展亮度、颜色、噪声和模糊程度的分布。采用 patch 级监督还可增加训练样本，并使训练尺度与后续局部图像块处理尺度保持一致。

=== 数据增强与数据集划分

退化参数范围兼顾训练难度与图像结构保留。强度过低时，低光输入与正常曝光目标差异有限；强度过高时，极暗样本中的语义结构会明显损失。因此，参数采样以覆盖常见退化、保留可恢复结构并控制极端样本比例为原则。

=== 真实夜间样本的统计校准作用

另选取 26 张真实田间夜间低光图像，用于标定退化参数和比较统计分布。这些影像不作为像素级监督标签，而是用于校准合成样本的亮度、颜色和退化范围。后续增加真实夜间 UAV 数据后，可继续修正参数分布和局部照度模型。

== 合成数据与真实夜间数据的一致性评价

=== 评价指标与对比方式

#figure(
  image("fig2_degradation.png", width: 100%),
  caption: [
    校准图像域退化流程及统计对齐。(a) 正常曝光 RGB 图像块依次经过曝光衰减、通道增益、Poisson--Gaussian 噪声、色调/gamma 变换、模糊和暗角后生成低光输入；(b)--(e) 分别为亮度累积分布（CDF）、通道比例偏移、饱和度分布与 RGB 均值离散度，使用 $n = 128$ 个图像块样本比较正常曝光、合成低光与真实低光的统计区间，真实低光样本裁剪自 26 张棉花低光校准影像，为退化参数人工标定提供定量参照。
  ],
) <fig:lowlight_stats>

@fig:lowlight_stats 比较了正常曝光、合成低光和真实低光图像块的统计分布。(a) 为退化流程，(b)--(e) 分别对应亮度累积分布（CDF）、通道比例偏移、饱和度分布和 RGB 均值离散度，比较样本数均为 $n = 128$。

=== 统计一致性结果分析

@fig:lowlight_stats (b) 显示，合成低光样本覆盖了真实低光样本的暗强度区间，二者在低亮度段基本重合。图 (c) 和 (e) 中，合成样本的通道比例偏移与 RGB 均值离散度位于真实样本的统计区间内；图 (d) 中，两类样本的饱和度分布形态也较为接近。结果表明，曝光衰减、通道增益、Poisson--Gaussian 噪声、轻度模糊和暗角能够覆盖真实夜间 UAV 图像的主要统计特征。

=== 误差来源与局限性讨论

@fig:lowlight_stats (b) 中，合成样本的 CDF 在极低亮度区域上升较快，暗部像素比真实样本更集中；真实样本在中低亮度区间分布更分散，尾部也更长。这一差异可能来自局部照明、地物反射和相机自动曝光。RGB 域模型可以近似主要退化趋势，但未包含 RAW 采集、ISP 处理和压缩存储的完整过程。后续可通过空间非均匀照明建模或 RAW 域退化进一步改善亮度尾部分布。现有合成样本已覆盖亮度、颜色和噪声的主要统计区间，可用于下一章低光增强模型的监督训练。

== 小结

本章针对农业 UAV 低光影像缺少严格配准样本的问题，构建了合成成对数据流程。退化模型包含曝光衰减、通道偏置、Poisson--Gaussian 噪声、轻度模糊和暗角，并通过 26 张真实低光影像校准参数范围。统计结果显示，合成数据与真实样本在亮度主体区间、通道比例和饱和度分布上较为接近，可为下一章低光增强模型的训练和任务适用性评价提供数据基础。
