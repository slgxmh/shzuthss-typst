= 低光图像的合成实验

== 摘要

夜间农业无人机（UAV）影像在作物表型监测、田间巡检与精准管理中具有重要应用潜力，但受低照度条件下曝光不足、噪声显著及颜色不稳定等成像退化因素影响，图像质量往往难以稳定支撑后续视觉分析任务。与此同时，真实场景中严格配准的“夜间低光—白天正常曝光”成对样本获取成本高、周期长，成为农业低光视觉研究的关键瓶颈。

针对上述问题，本文聚焦“低光数据合成”这一基础环节，构建了一套物理与 ISP 启发的低光退化建模流程，在 patch 级别生成可控的低光—正常曝光成对样本。该流程在 RGB 域联合模拟曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角，以覆盖夜间农业 UAV 成像中的主要退化因素。

此外，本文引入真实夜间 UAV 图像进行外部统计对齐，从亮度、饱和度与 RGB 通道比例等维度评估合成数据与真实分布的一致性。结果表明，所构建的合成数据在关键统计特性上能够有效逼近真实夜间场景，可作为后续低光增强与全天候识别任务的可靠训练数据基础。

== 引言

无人机平台能够在大尺度农田上快速获取高分辨率可见光影像，为作物长势评估、病虫害巡查与表型分析提供高时空分辨率数据@tian2020uav_crop_monitor @aierken2024review。然而在夜间或弱光条件下，UAV 影像常出现整体曝光不足、噪声显著、对比度下降与颜色偏移等问题@widartono2025aerial @fu2022highlightnet，直接影响后续检测、分割与计数等视觉任务的稳定性与可用性@silwal2021robust。

在农业生产中，夜间影像获取并非边缘需求。受白天温度、风速、人员与机械调度影响，夜间与清晨时段反而更利于持续开展航测与巡检@flynn2026assessing。以棉花等大田作物为例，生长中后期群体结构变化快、田块跨度大，影像质量一旦不稳，易造成下游任务在田块间和批次间表现波动@bendig2015combining @tian2020uav_crop_monitor @kamilaris2018deep @xu2019multispectral。

从成像链路看，夜间 UAV 退化通常是多因素叠加：低照度引起曝光与增益变化，使噪声显著增强@kurimo2009effect；机体振动与飞行速度放大运动模糊风险@tsekhmystro2025uav；ISP 在弱光下更易产生纹理涂抹与伪影@li2021low；复杂反射光又进一步扰动白平衡与色彩一致性@lu2025unsupervised。上述因素共同导致夜间图像在亮度、纹理和颜色三个层面同步退化。

现有低光增强研究虽然在公开数据集上进展迅速，但农业夜间场景缺少高质量配准训练对，导致“方法有效但数据支撑不足”的矛盾长期存在。相比直接依赖难以获取的真实配准数据，构建可解释、可控且可校验的合成退化流程，是提升农业低光研究可复现性与工程可用性的关键路径。

需要进一步指出的是，农业夜间场景与常见城市街景或室内低光场景存在明显差异。前者通常缺少稳定人工照明，目标表面反射率变化大，冠层内部存在大量细粒度遮挡与弱纹理区域，且无人机飞行平台还会引入姿态扰动和成像视角快速变化。因此，若直接借用通用低光数据集或通用退化模型，往往难以覆盖农业 UAV 夜间成像中的关键失真来源。本章选择从“退化机理可解释”和“统计分布可校验”两方面入手，其目的不仅是生成训练样本，更是为后续全天候识别建立与真实生产环境相匹配的数据基础。

围绕这一目标，本文工作聚焦数据层面，主要贡献如下：

+ *提出面向农业 UAV 的低光成对数据合成流程：* 从正常曝光影像裁剪多尺度 patch，并按成像机理注入曝光衰减、通道偏移、噪声、轻度模糊与暗角等退化，构建可控监督训练对。
+ *建立物理与 ISP 启发的退化参数体系：* 在统一数学表达下给出各退化算子的采样区间与组合方式，使数据生成过程具备可解释性与可复现性。
+ *引入真实夜间样本进行统计一致性验证：* 从亮度、饱和度和通道比例等统计维度评估合成数据与真实分布的一致性，为后续增强与识别模型提供可靠的数据基础。

== 方法与数据

本章涉及的研究区域与低光成对数据集构建流程统一归档在@chap:dataset，本章以下重点给出低光退化模型的数学定义与参数设置。

与直接学习黑箱映射不同，本章更强调退化链路的分解建模。其原因在于，若合成流程缺乏可解释的算子结构，则即便生成图像在视觉上较为逼真，也难以分析模型究竟学习了哪些退化规律，更难以根据真实场景反馈调整数据分布。通过把低光退化拆解为曝光、颜色、噪声、模糊和空间照度衰减等若干可控环节，本章可以将参数采样、统计对齐和下游训练之间建立清晰的对应关系，使整个数据构建过程具备可追溯性与可迭代优化能力。

=== 低光退化模型

为在缺乏严格配准的夜昼成对样本条件下构建可控训练数据，我们在 RGB 图像域设计一种物理与 ISP（Image Signal Processing）启发的低光退化模型。给定正常曝光图像块（8-bit RGB）$bold(I) in [0, 255]^(H times W times 3)$，先归一化得到强度表示：

$ bold(X) = frac(bold(I), 255) in [0, 1]^(H times W times 3). $

给定归一化后的正常曝光图像块 $bold(X) in [0, 1]^(H times W times 3)$，模型通过依次模拟欠曝、白平衡漂移、Poisson--Gaussian 噪声，以及轻度 ISP/光学伪影（模糊与暗角）生成低光观测 $bold(Y)$。整体前向过程表示为：

$ bold(Y) = cal(V)(cal(B)(Gamma_(gamma)(T_(tau)(cal(N)(cal(W)(alpha bold(X)))))))) $

其中 $cal(W)$ 表示通道增益（白平衡）、$cal(N)$ 表示噪声注入、$T_(tau)$ 与 $Gamma_(gamma)$ 分别表示 tone 映射与 gamma 映射、$cal(B)$ 表示轻度模糊、$cal(V)$ 表示暗角（径向照度衰减）。下文对各算子给出具体定义及参数采样范围。

*曝光衰减（Exposure reduction）*

在低照度条件下，相机往往通过降低曝光或增益调整导致整体信号幅度下降。我们使用线性缩放模拟欠曝：

$ bold(X)_e = alpha bold(X), alpha ∼ cal(U)(0.08, 0.30). $

该范围的设置并非任意选取，而是为了覆盖从“弱光但仍可辨识”到“显著欠曝”的主要亮度区间。若 $alpha$ 取值过高，则生成样本过于接近日间图像，难以形成有效的低光训练压力；若取值过低，则大量样本将进入严重信息塌缩区间，虽然视觉上更暗，但对学习有用的结构性证据会被整体抹去。因而，本章更关注“保留少量可恢复结构”的欠曝状态，而不是追求极端暗化效果。

*通道增益（White balance / ISP color bias）*

为刻画弱光条件下 ISP 白平衡与通道增益的漂移，引入通道独立增益向量 $bold(g) = (g_r, g_g, g_b)$，并对三通道逐元素缩放：

$ bold(X)_("wb") = cal(W)(bold(X)_e) = bold(X)_e ⊙ bold(g), g_c ∼ cal(U)(g_(c, "min"), g_(c, "max")), c in {r, g, b}. $

本文取 $g_r ∼ cal(U)(1.05, 1.18)$，$g_g ∼ cal(U)(0.95, 1.03)$，$g_b ∼ cal(U)(0.98, 1.06)$，并进行裁剪以保证数值稳定：

$ bold(X)_("wb") ← op("clip")(bold(X)_("wb"), 0, 1). $

这一通道增益扰动用于近似弱光条件下自动白平衡与 ISP 增益控制带来的颜色偏移。对农业图像而言，白平衡变化具有特别重要的影响，因为脱叶率与吐絮率识别高度依赖绿色叶片、棕褐枝条与白色棉絮之间的相对颜色关系。若训练数据中缺少合理的通道比例漂移，增强模型可能只学到“变亮”而无法处理真实夜间图像中的偏色现象；反之，若增益扰动过强，又可能破坏作物表型原有的颜色语义。因此，本章仅在相对保守范围内对三通道增益进行扰动，以保证颜色变化既具有真实性，又不至于与目标语义脱耦。

*Poisson--Gaussian 噪声（Shot + read noise）*

夜间噪声通常由信号相关的散粒噪声（shot noise）与信号无关的读出噪声（read noise）共同构成。我们采用 Poisson--Gaussian 近似进行建模：

$ bold(X)_p = frac(1, k) · op("Poisson")(k · bold(X)_("wb")), k ∼ cal(U)(30, 100). $

较小的 $k$ 对应更强的相对散粒噪声强度。随后叠加零均值高斯噪声以模拟读出噪声：

$ bold(X)_n = bold(X)_p + epsilon, epsilon ∼ cal(N)(0, sigma_r^2), sigma_r ∼ cal(U)(0.004, 0.020). $

最终结果被裁剪至 $[0, 1]$ 以满足图像强度范围约束。

噪声建模在低光合成中具有基础性作用，因为弱光场景下的主要困难之一并不是“图像不够亮”，而是信号与噪声比显著下降。在农业 UAV 图像中，这种噪声会直接破坏叶缘、棉絮轮廓和行间纹理等细粒度特征，使下游识别模型更容易在边界类样本上失稳。Poisson 噪声刻画了低光子计数下的信号相关扰动，高斯噪声则近似传感器读出过程中的稳定噪声底，两者组合能够更贴近夜间成像退化的实际统计形态。因此，本章没有采用单一高斯噪声近似，而是显式引入两类噪声的叠加，以提高合成样本对真实场景的覆盖能力。

*Tone 映射与 gamma（Tone mapping + gamma）*

为在 RGB 域近似 ISP 的 tone curve 以及显示域非线性响应，我们采用幂律形式的 tone 与 gamma 变换：

$ bold(X)_t = T_(tau)(bold(X)_n) = bold(X)_n^tau, tau ∼ cal(U)(0.90, 1.05). $

$ bold(X)_g = Gamma_(gamma)(bold(X)_t) = bold(X)_t^gamma, gamma ∼ cal(U)(1.2, 1.8). $

引入 tone 与 gamma 变换的目的，在于弥补“仅靠线性缩放与噪声注入”无法表达弱光图像非线性亮度压缩的缺陷。真实相机在弱光条件下通常会通过 ISP 曲线调整和显示域映射改变灰度分布，使暗部与中低亮度区域的对比关系发生明显变化。如果忽略这一过程，合成图像虽然可能在均值亮度上接近真实样本，但其直方图形状与局部对比度分布仍会存在偏差，进而影响增强模型对亮度恢复规律的学习。因而，本章将 tone 与 gamma 变换视为连接“物理退化”与“显示观测”的关键桥梁。

*轻度模糊（Mild blur）*

考虑到弱光条件下 ISP 降噪、轻微失焦或运动引起的细节软化，使用高斯核近似点扩散函数（PSF）：

$ bold(X)_b = cal(B)(bold(X)_g) = bold(X)_g * cal(G)(sigma_b), sigma_b ∼ cal(U)(0.00, 0.60). $

当 $sigma_b = 0$ 时跳过该步骤。

轻度模糊的设置主要针对无人机夜间飞行中的平台扰动、轻微失焦与 ISP 降噪引起的细节软化现象。需要注意的是，本章并未试图用该算子重建复杂的运动模糊轨迹，而是将其限定为“有限幅度的细节退化”，原因在于：对于低光增强训练而言，更常见的情况是纹理被轻度平滑而非大尺度拖影；若模糊过强，则会把问题从“低光恢复”转移为“重度去模糊”，反而偏离本章的数据构建目标。

*暗角（Vignetting / illumination falloff）*

为模拟镜头暗角或空间非均匀照度，引入径向衰减掩膜 $M(bold(p))$。令归一化坐标 $tilde(x), tilde(y) in [-1, 1]$，$r^2 = tilde(x)^2 + tilde(y)^2$，则：

$ M(bold(p)) = 1 - s · op("clip")(r^2, 0, 1), s ∼ cal(U)(0.00, 0.15). $

并得到：

$ bold(Y) = cal(V)(bold(X)_b) = bold(X)_b ⊙ M(bold(p)). $

这一设计用于模拟夜间图像中常见的空间非均匀亮度衰减。对农业 UAV 影像而言，画面边缘区域往往因镜头暗角、姿态变化或局部照度不均而呈现系统性偏暗，如果训练样本始终保持全局均匀照明，则模型会更倾向于学习“全图统一增强”，而难以处理实际场景中的局部明暗差异。通过引入弱暗角，本章使生成样本在空间亮度分布上更接近真实夜间航拍条件，也为后续增强模型学习局部自适应恢复提供了先验。

*量化输出（8-bit quantization）*

最后将结果裁剪并映射回 8-bit 空间：

$ bold(Y)_8 = op("round")(255 · op("clip")(bold(Y), 0, 1)). $

需要指出的是，本模型在 RGB 域实现，未显式模拟 RAW 与去马赛克过程，因此属于 image-domain approximation；但其包含低光退化的关键统计因素（曝光降低、通道偏置、Poisson--Gaussian 噪声、轻度模糊与暗角），可用于构造训练数据。所有参数均在对应区间内均匀采样，以增强退化多样性并提升数据覆盖能力。

总体而言，该模型的目标不是精确重建相机底层物理链路，而是在训练可用性与成像真实性之间取得平衡。对于本文后续低光增强与全天候识别任务来说，更关键的是生成样本能否提供稳定、可控、具有代表性的监督信号，而不是是否在传感器层面完全复现真实相机内部处理过程。正是在这一原则下，本章采用了以统计逼近和任务导向为核心的数据构建思路。

=== 数据集制作

针对真实夜间农业 UAV 影像难以获取严格配准昼夜样本的问题，本文采用“合成成对训练 + 真实夜间外部验证”的策略构建数据集，以在可控监督学习条件下提升模型对真实场景的适用性与鲁棒性。

#figure(
  image("dataset_aug.png", width: 100%),
  caption: [
    用于低光增强的训练对生成流程。将正常曝光的无人机图像裁剪成多尺度补丁，然后通过曝光衰减、噪声注入和可选的轻微模糊进行降质，以合成对应的低光输入，从而生成用于监督训练的成对样本 $(x, t)$。
  ],
) <fig:lowlight_pairgen>

如@fig:lowlight_pairgen 所示，在每张图像的四角与中心区域进行带抖动采样并随机选择缩放因子以模拟不同飞行高度带来的细节软化；随后对 patch 进行随机旋转与水平翻转等增强；最后对目标 patch 施加曝光衰减、白平衡漂移、Poisson–Gaussian 噪声、轻度模糊与暗角等退化因素，合成对应低光输入，得到用于监督学习的低光—正常曝光成对样本 $(x, t)$。最终数据按 train/val/test = 0.8/0.1/0.1 的比例划分。

该数据构造策略的关键在于同时覆盖“空间多样性”与“退化多样性”。前者通过多位置采样、尺度抖动和几何增强实现，使模型能够看到不同作物结构、背景组合与观测尺度下的局部区域；后者则通过退化参数随机采样实现，使训练样本不对应某一种固定低光模式，而是在亮度、颜色、噪声与局部模糊程度上形成分布覆盖。二者结合后，得到的训练对更接近真实夜间场景中“同一作物区域可能在不同日期、不同高度、不同光照下呈现不同观测结果”的实际情况，从而提高模型对真实输入变化的适应能力。

此外，采用 patch 级监督而非整幅正射影像级监督，还有两点现实考虑：一是 patch 更适合在有限显存与算力条件下进行大批量训练，并有助于增加样本数；二是后续增强与识别链路本身即以局部图像块为主要处理单元，因而 patch 级数据构建在尺度上与下游任务保持一致。这种“训练单元与部署单元一致”的设计，有助于减少从数据构建到模型落地之间的尺度失配。

== 合成结果评价

#figure(
  image("dataset_dis.png", width: 80%),
  caption: [
    合成低光退化图像与真实夜间 UAV 图像的统计一致性分析。(a) 多维图像统计指标（亮度、对比度、饱和度、RGB 通道比例等）的均值及标准差对比，(b) 合成数据与真实数据在各统计指标上的绝对差异，(c) 亮度（luma）直方图分布对比。结果表明，所构建的合成低光退化在多项关键统计特性上与真实夜间 UAV 图像具有良好一致性，为后续低光增强模型训练与全天候识别任务提供了合理的数据基础。
  ],
) <fig:lowlight_stats>

@fig:lowlight_stats 不是对单幅图像视觉效果的主观比较，而是对“合成退化是否在统计意义上逼近真实夜间分布”的系统性检验。图 @fig:lowlight_stats (a) 从均值与标准差两个层面，对亮度水平、亮度波动、暗像素比例、噪声强度、清晰度、RGB 通道均值、通道比值、饱和度统计、动态范围以及欠曝/过曝比例等多维指标进行了并列比较。可以看到，合成数据与真实夜间图像在大多数一阶与二阶统计量上保持了较高一致性：亮度均值及其标准差处于相近量级，说明退化模型能够有效重建夜间图像“整体偏暗且波动受限”的典型特征；RGB 三通道均值及通道比值差异较小，表明白平衡漂移与颜色偏置的模拟方向总体合理，没有出现明显违背真实夜间成像规律的色彩失真；饱和度均值、暗区比例以及边缘密度等指标也与真实样本较为接近，说明所构建的合成退化不仅降低了亮度，而且在颜色压缩、细节衰减与暗区占比上较好再现了夜间农业场景的综合退化效应。

图 @fig:lowlight_stats (b) 进一步给出了各统计量的绝对差异，用于衡量“对齐误差”在不同指标上的分布。结果表明，多数标量统计量的偏差均维持在较低水平，说明本文所采用的曝光衰减、通道增益扰动、Poisson--Gaussian 噪声、轻度模糊与暗角等退化因子，在总体上可以共同逼近真实夜间 UAV 图像的统计结构。这一点对于后续监督训练尤为关键：如果合成样本仅在视觉上“看起来像夜间图像”，但在亮度、噪声或颜色统计上与真实场景偏离过大，则增强模型容易学习到不稳定的映射关系，进而削弱对真实数据的迁移能力。从当前结果看，合成数据已经在多数低阶统计量上建立了较稳定的分布对应关系。不过，图中仍可观察到个别指标的偏差相对更大，这说明 RGB 域退化模型虽然能够刻画夜间成像的主要趋势，但仍不能完全等价于真实相机从 RAW 采集、ISP 处理到压缩存储的完整链路。换言之，本文的合成策略更适合作为“可控、可复现、可用于训练的近似监督数据”，而不是对真实夜间成像机理的完全替代。

图 @fig:lowlight_stats (c) 则从分布形状而非单点统计量的角度，比较了合成数据与真实夜间图像的亮度直方图。两条曲线均在低亮度区间形成尖峰，并在亮度升高后迅速衰减，反映出夜间农业 UAV 图像中“低灰度像素占主导、高亮区域稀缺”的典型分布特征。合成曲线在极低亮度区域具有更高的峰值，表明模拟数据在暗部压缩上略强于真实数据，即模型更倾向于生成“更暗、更集中”的亮度分布；而真实夜间样本在中低亮度区间保持了更长的尾部，这可能与实际场景中的局部照明差异、地物反射不均匀性以及相机自动曝光补偿有关。该差异说明合成退化在整体趋势上是正确的，但在亮度分布尾部和局部曝光多样性方面仍有进一步优化空间。综合图 @fig:lowlight_stats 三个子图可知，本文构建的低光合成数据已经较好捕捉了真实夜间农业 UAV 图像在亮度、颜色与退化形态上的主要统计规律，足以支撑后续低光增强模型的监督训练；同时，这一结果也提示后续研究可进一步引入更精细的曝光控制、空间非均匀照明建模或 RAW 域退化机制，以缩小与真实夜间成像之间的剩余分布差距。

== 总结

本章围绕夜间农业 UAV 影像缺少严格配准样本的问题，构建了面向低光场景的合成成对数据流程。通过物理与 ISP 启发的退化建模，数据生成过程具备可解释性和可复现性；通过真实夜间样本的统计对齐验证，证明合成数据在亮度、饱和度与通道比例等关键分布上与真实场景具有较好一致性。该数据基础为下一章的低光增强与全天候识别模型提供了稳定输入。

从全文结构看，本章承担的是“数据先行”的基础性角色。若缺少本章所构建的低光成对样本与统计对齐分析，后续增强模型训练将难以获得稳定监督，全天候识别章节中的性能提升也缺乏可追溯的数据依据。通过在本章显式建立“退化机理—参数采样—统计分布—下游训练”之间的对应关系，全文关于弱光鲁棒性的研究链路才具备了完整的数据支撑。因而，本章不仅是一个辅助数据准备章节，也是全文从单场景识别走向全天候识别的重要转折点。
