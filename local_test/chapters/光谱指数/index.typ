#import "../../../template.typ": three-line-table

本章基于@chap:dataset 构建的数据集，开展“光谱指数 + 前景分割 + 传统机器学习”的可解释基线研究。与端到端深度模型相比，基于可见光指数与结构化特征的方法具有实现简单、参数可控、误差来源可解释等优势，可作为后续轻量深度模型与全天候识别研究的对照基准。

== 摘要

成熟期棉花催熟作业效果通常以脱叶率与吐絮率两项指标衡量，但在大田尺度下依赖人工抽样与经验判断难以满足“快速、客观、可复现”的在线管理需求。针对这一问题，本章从可解释性与工程可复现角度出发，构建了基于可见光植被指数与传统机器学习的双指标识别基线。方法上，首先利用 ExG/VARI 等可见光指数进行前景分割，抑制土壤、阴影等背景干扰；随后从光谱指数统计、形态结构与纹理统计三个层面构建结构化特征，并与“展平像素 + 传统模型”的工程基线进行对比；最后采用 Accuracy 与 Macro-F1 对多等级脱叶率与吐絮率任务进行评估。实验结果表明，在当前一次固定切分实验中，PCA+Logistic Regression 在两项任务的 Macro-F1 指标上整体更优（脱叶率测试集 Macro-F1 为 0.3049，吐絮率测试集 Macro-F1 为 0.3989），而 Random Forest 系列方法存在 Accuracy 较高但 Macro-F1 较低的现象，反映出类别不均衡对决策边界的显著影响。本章工作为后续“结构化特征融合”和“端侧深度模型”提供了可解释对照基线与可复现实验框架。

== 引言

成熟期棉花田间影像呈现显著的背景复杂性与成像非稳定性：地表土壤反射、滴灌带与覆膜区域带来强背景纹理，阴影与逆光导致亮度动态范围压缩，且不同地块与不同作业批次的冠层结构差异会引入分布偏移。这些因素使得“直接对 RGB 像素建模”的泛化能力受限，并增加模型在跨地块、跨时段条件下的失效风险。

从工程落地视角，传统机器学习在小样本与可解释性方面仍具优势。通过显式构造与作物生理含义对应的结构化特征（如绿度、白度、连通域、纹理），不仅便于定位误差来源，也有助于建立与农艺机制一致的先验约束。因此，本章以“可解释基线”为目标，提出一条由“前景分割 → 特征提取 → 传统模型识别 → 指标评估”构成的完整技术链路，为后续章节的轻量深度模型与全天候鲁棒性改进提供对照参照。

需要说明的是，本章的意义并不在于证明传统方法优于深度学习，而在于建立一个能够被农艺人员、算法研究者与工程实施者共同理解的“中间坐标系”。端到端深度模型虽然在复杂场景中通常具有更强的表征能力，但其误差来源往往隐含在高维特征空间中，难以直接对应到田间可解释因素。相较之下，基于光谱指数、形态与纹理的结构化特征能够把“叶片残留”“棉絮暴露”“冠层稀疏化”和“背景干扰”这些关键现象显式映射为可测变量，从而为后续方法比较、误差诊断与处方决策提供共同语言。这也是博士论文中保留并深入分析传统基线的重要原因。

== 前后背景及棉株对象分割

田间场景中的天空、土壤、阴影与滴灌带等背景干扰，会显著影响脱叶率和吐絮率判别。前景分割的核心目标是将棉株区域从复杂背景中剥离，降低非目标噪声对分类边界的干扰。

=== 基于颜色特征的植被背景分割

在仅有 RGB 通道条件下，常用做法是利用可见光植被指数构造“植株与背景”的可分离表征。指数的优势在于其形式简单、可快速计算，并能在一定程度上抵消光照强度整体变化带来的比例缩放影响。结合田间棉花背景与冠层颜色分布特征，本文选取 ExG、VARI 与 NGRDI 三类可见光指数作为分割与绿度刻画的基础表征，其定义分别为（其中 $epsilon$ 为防止分母为零的极小常数）：

超绿指数（ExG）：
$ "ExG" = 2G - R - B $

可见光大气阻抗植被指数（VARI）：
$ "VARI" = frac(G - R, G + R - B + epsilon) $

归一化绿红差（NGRDI）：
$ "NGRDI" = frac(G - R, G + R + epsilon) $

ExG 通过强调绿色通道相对红蓝通道的优势响应，常用于农业场景的植被提取与背景抑制@woebbecke1995exg；VARI 通过可见光通道的组合构造对大气与照度变化更稳定的表征，被用于植被覆盖度估计等任务@gitelson2002vari。NGRDI 采用“归一化差分”形式，可视为将经典归一化差分思想移植到可见光域的简化实现（与 NDVI 等归一化差分指数在形式上相近），在弱监督场景下可作为快速分割与绿度刻画的候选特征。@tucker1979ndvi

对于成熟期棉花场景，上述指数之所以具有实际意义，在于其与催熟过程中的显著视觉变化存在直接对应。随着脱叶推进，叶片覆盖度下降，绿色通道在 ROI 内的主导程度会逐步减弱，使 ExG、VARI 与 NGRDI 等指数整体呈下降趋势；与此同时，吐絮增加会带来高亮白色纤维暴露面积上升，使得绿色优势进一步被削弱，并改变局部分位数和高阶统计量的分布形态。也就是说，这些指数并非抽象的数学组合，而是对“绿叶减少、白絮增加”这一成熟进程的可见光表征。正因如此，即使在没有多光谱或高光谱载荷的条件下，基于 RGB 的可见光指数仍可作为成熟度识别的低成本先验。

=== 阈值分割

基于指数图执行阈值分割，得到初始前景掩膜。常用的 Otsu 方法通过最大化类间方差自动选择阈值，在无需先验参数的条件下具有较好的泛用性@otsu1979threshold。随后采用形态学开闭运算去噪与补洞，并结合连通域筛选保留主要棉株区域，从而形成稳定的 ROI 掩膜。该 ROI 可显著降低背景像素占比，为后续特征统计与模型训练提供更一致的输入分布。

从任务本身出发，前景分割的重要性还在于它改变了后续特征的统计对象。若不经过 ROI 提取，指数均值、纹理统计和形态结构特征都会同时受到土壤、地膜、滴灌带和阴影区域的影响，导致模型学习到的并非棉株成熟状态，而是“某类背景在某些地块中是否频繁出现”的伪相关关系。通过把统计范围限制在前景区域，可以显著提高特征与目标表型之间的因果关联程度，从而使传统模型的判别边界更接近农艺机理，而不是被背景共现模式主导。

=== 图像分割的其它方法

除颜色特征与阈值方法外，工程上还可结合区域生长、边缘约束或轻量学习式分割策略作为补充。当前流程优先采用“指数 + 阈值 + 形态学”的原因在于其实现成本低、可解释性强，且在小样本条件下具备较好可迁移性。实践表明，前景分割可显著降低背景颜色分布干扰，提升与棉株状态相关像素的有效占比，并为后续面积比例、白絮连通域和纹理统计等特征提取提供可靠输入。

== 棉花植株特征提取

在完成前景分割后，可从光谱、形态与纹理三个层面构建特征，以提升模型解释能力和结果稳定性。

=== 光谱指数特征

光谱指数与颜色统计特征能够直接反映“绿叶减少”与“白絮暴露增加”的观测变化。考虑到单一均值对噪声与局部遮挡敏感，本文在 ROI 内对指数图与颜色分量提取多种统计量（均值、方差、分位数、峰度/偏度等），以增强对分布形态变化的刻画能力。具体而言，以 ExG/VARI/NGRDI 三类指数作为基础统计量来源，同时构造两类与任务指标高度相关的派生统计：其一是白度相关特征，例如高亮像素占比或高亮分位数，用于刻画棉絮外露程度并辅助吐絮率分级；其二是叶绿相关特征，例如绿色像素占比或绿色通道强度分布，用于描述冠层叶片覆盖程度并辅助脱叶率分级。

其中，“白度相关特征”可通过阈值或分位数统计描述高亮像素占比，用于刻画棉絮外露程度；“叶绿相关特征”可通过绿色像素占比或绿色通道强度分布刻画冠层叶片覆盖程度。上述特征与生理表型存在清晰对应关系，有利于后续误差解释与跨地块对比分析。

相比单一均值，分位数、偏度和峰度等统计量能够更好地刻画成熟期棉花图像中的分布异质性。例如，在部分区域已出现棉絮集中暴露而整体仍保留较多叶片的情况下，均值可能只表现为温和变化，而高亮分位数与分布偏度会更敏感地捕捉到“少量高亮目标开始出现”这一过渡特征。同理，对于脱叶率尚未充分提高但局部冠层已出现明显稀疏化的样本，绿色通道分布的尾部变化往往比整体均值更早反映成熟趋势。因此，本章采用多统计量联合描述指数分布，实质上是为了让结构化特征能够覆盖成熟过程中的早期变化、过渡状态与空间不均匀性。

=== 形态结构特征

形态结构特征用于在“颜色统计之外”补充冠层结构与棉絮空间分布的几何信息。本文主要统计前景面积占比，用于刻画可见植株区域的覆盖程度；对白色连通域的数量与平均面积进行统计，用于表征棉絮团块的出现频率与尺度；以边界复杂度（周长/面积）刻画轮廓形状的破碎程度与行间背景干扰；并以外接矩形长宽比描述 ROI 的整体形态与方向性。

上述特征对“叶片减少”与“棉絮暴露增加”具有明确物理意义。

从机理角度看，形态特征弥补了颜色特征对“空间组织方式”描述不足的问题。成熟度变化并不仅仅体现为像素颜色比例的改变，还体现为冠层内部几何结构的重组：脱叶后冠层孔隙率增大，前景覆盖面积和边界复杂度会发生变化；吐絮后白色连通域数量及其尺度分布会改变，且这些变化通常具有一定的空间聚集性。因此，若仅依赖颜色统计，模型可能难以区分“亮度变化来自白絮增加”还是“亮度变化来自局部反光或背景穿透”；而引入连通域和轮廓特征后，这种判别将更具结构依据。

=== 纹理与统计特征

当背景被抑制后，冠层内部纹理、行间结构与棉絮团块分布会在纹理统计上体现差异。典型纹理特征包括：

灰度共生矩阵（GLCM）通过统计不同灰度在空间邻域的共现频率，能够描述冠层纹理的对比度、同质性与能量等特性@haralick1973glcm；局部二值模式（LBP）通过局部邻域二值编码刻画微纹理模式，具有较强的旋转与灰度鲁棒性@ojala2002lbp；此外，通道级统计量（均值、标准差、偏度、分位数等）可作为轻量补充，用于描述 ROI 内颜色与亮度分布的高阶形态。

纹理特征对“叶缘纹理减少”与“高亮纤维块出现”的结构变化敏感，可作为指数统计之外的补充信息源。

这类纹理特征对田间复杂背景尤其重要。成熟期棉株图像中，地膜、裸土和阴影区域常会引入与棉絮高亮区域相似的局部反差，单纯依赖颜色统计容易产生混淆。纹理描述子能够从灰度共生关系、局部模式重复性和方向性分布等角度刻画“棉絮块”“叶片纹理”“背景粗糙度”的差异，从而提高对相似颜色但不同结构区域的辨别能力。换言之，纹理特征并非简单附加维度，而是在颜色与形态之间提供了重要的中间层信息。

=== 当前基线与特征路线关系

当前工程基线以“展平像素 + 传统模型”为主，目标是快速验证任务可行性，但其对背景与光照变化敏感且缺乏显式语义约束。后续可将“光谱指数 + 形态 + 纹理”拼接为低维结构化特征，与当前基线进行公平对比，从而更清晰地体现“基于光谱指数”的稳定性与可解释性优势；同时，该结构化特征也可作为轻量深度模型的先验或辅助分支输入，以提升跨场景鲁棒性。

== 使用回归法进行脱叶率吐絮率识别

本节基于当前实验记录（`results/experiment_results.csv`）开展分析，选取三类代表性基线模型进行对比：其一为 PCA + Logistic Regression（PCA+LR），用于验证低维线性判别子空间的可行性；其二为标准 Random Forest（RF），用于评估非线性集成模型对结构化特征的拟合能力；其三为 Random Forest（Balanced），用于考察类别重采样/重加权等均衡策略对长尾等级识别的影响。

现有记录对应一次完整对比实验（`target=de/ba`），并输出 `train/val/test` 三分结果。

=== 算法流程

整体流程包括：特征准备、模型训练、验证集调参与测试集评估。考虑到脱叶率与吐絮率为多等级离散标签（等级划分见@chap:dataset），且类别占比通常呈长尾分布，本章采用 Accuracy 与 Macro-F1 联合评估，以同时反映整体正确率与少数类识别能力：

其中，Macro-F1 为本章优先关注指标。

这样设置的原因在于，本章关注的不仅是“多数样本是否被大致分对”，还关注少数类和边界类是否能够被识别。对于处方图决策而言，恰恰是这些数量较少但风险较高的等级最值得关注：若模型只对中间主类有较高正确率，而对成熟明显滞后或明显提前的区域识别较差，则其工程价值会被显著削弱。因此，Macro-F1 在本章中承担了“风险敏感评价”的角色，比单纯的 Accuracy 更能体现模型对各等级的均衡判别能力。

为提高可复现实验的可比性，本章报告一次固定切分实验结果；进一步的稳健性评估应采用多随机种子重复实验，并在跨地块/跨时段切分下报告均值与方差。

=== 回归法存在的问题

当前流程仍存在以下瓶颈：

一方面，展平像素特征缺乏显式语义约束，使得模型容易利用背景纹理等“捷径特征”而非成熟状态相关证据；另一方面，田间背景变化显著，前景分割与 ROI 统计尚未系统性融入主流程，导致输入分布不稳定并削弱跨地块可比性。此外，类别分布不均衡会诱导模型对多数类决策偏置，使 Accuracy 与 Macro-F1 出现显著背离；再叠加单次随机种子与单次切分评估，容易产生偶然结论。

上述问题说明，若希望仅依赖传统模型在复杂田间场景获得稳定表现，需要进一步将“前景分割 + 结构化特征 + 类别重加权/分层采样”等机制系统性纳入训练与评测流程，并在跨域测试上验证其收益。

更深层地看，这些问题并非传统模型独有，而是成熟度识别任务本身的结构性难点在浅层方法中的集中体现。展平像素特征的局限说明“以低层观测直接对应高层语义”在复杂田间场景中往往是不充分的；类别不均衡导致的多数类偏置则说明，若没有显式的任务约束和评测设计，模型很容易学到与管理风险不一致的决策边界。正因如此，本章对传统方法局限性的分析并不是为了否定传统路线，而是为了明确后续深度模型设计必须解决哪些核心问题：即如何抑制背景噪声、如何增强对边界等级的表征、以及如何在真实场景中保持跨地块稳定性。

=== 应用回归法进行脱叶率吐絮率识别

脱叶率任务（`de`）结果如@tab:de-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.3864], [*0.1976*], [*0.4091*], [*0.3049*],
    [RandomForest], [0.3409], [0.0658], [0.3258], [0.1469],
    [RandomForestBalanced], [0.3636], [0.1897], [0.3106], [0.1305],
  )),
  caption: "脱叶率（de）实验结果",
) <tab:de-res>

吐絮率任务（`ba`）结果如@tab:ba-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.4091], [*0.3312*], [*0.5000*], [*0.3989*],
    [RandomForest], [*0.4773*], [0.1077], [0.4242], [0.1105],
    [RandomForestBalanced], [*0.4773*], [0.1077], [0.4167], [0.0980],
  )),
  caption: "吐絮率（ba）实验结果",
) <tab:ba-res>

结果解读如下：

从 Macro-F1 指标看，PCA+LR 在 `de` 与 `ba` 两任务上均明显优于 RF 系列：脱叶率任务测试集 Macro-F1 达到 0.3049（@tab:de-res），吐絮率任务测试集 Macro-F1 达到 0.3989（@tab:ba-res），表明在当前基线输入下，“线性可分的低维判别子空间”更有利于少数类等级的稳定识别。与此相对，RF 在吐絮率任务验证集 Accuracy 达到 0.4773，但 Macro-F1 仅 0.1077（@tab:ba-res），显示模型明显偏向多数类；这类“Accuracy 高而 Macro-F1 低”的背离现象在长尾分布下具有代表性，说明仅依赖整体正确率会掩盖少数类失效。另一方面，三类模型训练集指标接近满分而测试集明显下降，也提示当前设置存在过拟合风险，特征表达与模型复杂度仍需进一步匹配。

这一结果具有两层启示。第一，在当前特征组织方式下，PCA+LR 的表现优于 RF 系列，说明有效信息可能更多地集中在若干低维方向上，而不是需要由高复杂度非线性树模型去刻画的高维交互关系。换言之，当前传统基线的主要瓶颈可能尚不在于分类器表达能力不足，而在于输入特征尚未被组织成更稳定、更具语义约束的结构化表示。第二，RF 系列出现的 Accuracy 与 Macro-F1 背离说明，树模型虽然可以较容易拟合高频类别，但在长尾等级识别上会表现出明显的类别偏置，这对处方决策场景是不利的，因为真正需要关注的往往正是数量较少但管理风险更高的少数类区域。

同时，吐絮率任务中 PCA+LR 的 Macro-F1 高于脱叶率任务，也提示“白絮暴露”这一视觉现象在当前可见光特征空间中可能更容易形成相对稳定的判别边界，而“叶片残留程度”由于更容易受到阴影、遮挡和背景穿透影响，其等级区分更具挑战性。这一观察为后续深度模型设计提供了直接启发：对于脱叶率任务，模型需要更强的空间结构建模能力和背景抑制能力，而不仅仅是更丰富的颜色统计输入。

基于上述观察，后续改进应围绕“输入更结构化、训练更稳健、评测更可信”三条主线展开：在输入层面，将 ROI 分割、指数统计与形态/纹理特征系统性并入训练输入，减少背景噪声主导特征空间；在训练层面，对 LR 联合调节 `C` 与 `n_components` 并加强正则化，对 RF 约束树深度与叶节点样本数等复杂度参数，并结合分层抽样或类别重加权提升少数类梯度贡献；在评测层面，采用多随机种子重复实验并报告均值与标准差，同时引入跨地块/跨时段切分以验证泛化能力。

== 讨论

本章实验表明，在当前一次固定切分设置下，PCA+LR 在 Macro-F1 指标上优于 RF 系列，提示“线性可分的低维判别子空间”可能更符合当前基线输入的有效信息结构；而 RF 系列出现 Accuracy 与 Macro-F1 明显不一致的现象，则进一步说明类别不均衡会导致模型对多数类过拟合并忽视少数类边界。针对这一现象，后续工作可从三条路径改进：

其一，在输入表征上，应将 ROI 分割与结构化特征显式并入训练输入，避免背景噪声主导特征空间；其二，在训练策略上，应采用类别重加权、分层采样或代价敏感学习等机制提升少数类的梯度贡献；其三，在评测协议上，应在跨地块、跨年份与跨光照切分下评估，并报告多随机种子统计量，以避免单次切分带来的偶然结论。

从“工程闭环”角度看，传统特征方法的价值不在于追求单项指标最优，而在于提供可解释的中间变量（如前景面积、白絮连通域、绿度统计），用于支撑处方决策的可解释性与风险控制；同时，它也为后续端侧深度模型的设计提供了结构化先验与误差分析坐标系。

进一步而言，本章可解释基线的意义在于为全文后续章节提供“问题分解视角”。如果深度模型在某些场景中出现误判，可以回到本章所构建的结构化特征坐标系中分析：问题究竟来源于叶绿统计失真、白絮连通域识别不足，还是背景纹理干扰过强。这样的分析路径能够把黑箱误差重新投影到可解释变量空间中，从而提高后续模型优化与处方规则校准的针对性。这也是传统方法在博士论文体系中不可替代的价值所在。

== 本章小结

本章围绕“基于光谱指数的脱叶率吐絮率识别”构建了可解释基线技术链路，覆盖前景分割、特征提取与传统模型识别，并给出了在一次固定切分实验上的对比结果。总体而言，PCA+LogisticRegression 在两项任务的 Macro-F1 指标上表现更优，但整体仍受类别不均衡、背景干扰与单次切分偶然性影响。下一阶段应进一步将光谱指数与分割特征深度耦合到训练与评测流程中，并与端侧轻量深度模型形成互补，以持续提升鲁棒性、可解释性与田间应用价值。

从全文技术链路看，本章完成了从“农艺可解释现象”到“可计算特征”的首次系统映射，为后续深度模型和全天候识别章节提供了重要参照。一方面，本章证明了即使在仅使用 RGB 影像的条件下，成熟期棉花的脱叶与吐絮状态仍可通过颜色、形态与纹理特征获得一定程度的稳定刻画；另一方面，本章也清楚揭示了传统方法在复杂背景、长尾等级与跨场景泛化上的瓶颈，从而为后续端到端模型、低光增强与处方图闭环研究提供了明确的问题导向。由此，本章不仅是对照基线，更是全文方法演进逻辑的起点。
