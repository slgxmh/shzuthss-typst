#import "../../../template.typ": three-line-table

== 数据集采集

本研究以田间棉花样本为对象，构建了脱叶率（`de_labels`）与吐絮率（`ba_labels`）双标签识别数据集。为提升数据管理效率并保证实验可复现性，当前阶段采用统一的 HDF5 文件组织样本、标签与切分索引，支撑后续批量训练与多模型公平对比。

=== 研究区域

本次实验于新疆石河子北泉双泉集三分厂进行，如@data_area 所示。
该地区以其独特的气候和土壤条件成为研究棉花生长和脱叶剂效果的理想场所。
研究区属温带大陆性气候，年均降水量较低（125.9至207.毫米），水资源相对匮乏，同时昼夜温差显著。
这些气候特点对棉花的生长周期及脱叶剂的效果均有显著影响。
实验中选用的棉花品种为新陆早83号，这一品种因其适应性强、产量高而在当地广泛种植。
选择该品种的目的是在实际农业生产中评估脱叶剂的效果，从而提高研究结果的实际应用价值。
本次实验中使用的脱叶剂由噻苯敌草隆和乙烯利两种成分组成，具体用量为噻苯敌草隆15克/亩，乙烯利40克/亩。
这一配比在确保棉花产量和品质的基础上，有效促进了棉花叶片脱落，为后续的机械采摘提供便利。
实验所用的棉花田为当地农民自耕的农田，而非专门试验田。这种选择不仅模拟了实际农业生产环境，还使实验结果具有更高的现实意义和应用价值。

数据采集工作从2024年9月24日开始至10月4日结束，共进行26期数据收集，每期包括L1和L2两块试验田的可见光数据。在数据采集过程中，采用了大疆Mavic3多光谱版无人机。这款无人机专为农业应用设计，具有高精度遥感能力。
实验航线由大疆Pilot2软件自动生成， 如@fly_param 所示。研究时间覆盖了棉花生长的关键阶段，为实验提供了连续且全面的数据支持。


#figure(
  image("data_area.png"),
  caption: "试验区概况。(a)试验区位于石河子；(b)L1与L2两块实验田；(c)L2实验田的航线图与航线参数；(d)L1实验田的航线图与航线参数；(e)对棉花进行标记以采集数据；(f)催熟作业过程中的棉花；(g)达到采收标准的棉花。",
) <data_area>

#figure(
  three-line-table(
    table(
      columns: 3,
      [参数名称], [参数值], [参数说明],
      [地面分辨率], [1cm/pixel], [手动指定分辨率],
      [航线高度], [27.3m], [由地面分辨率自动计算的航线高度],
      [航向重叠率], [60%], [平坦地面的验设置],
      [旁向重叠率], [70%], [平坦地面的经验设置],
      [飞行速度], [2.7m/s], [为了更好获得清晰的图像],
    ),
  ),
  caption: "任务航线参数表",
) <fly_param>


我们采取人工调查的方式进行脱叶率与吐絮率的统计，为确保数据的代表性和准确性，在试验田内均匀布置了样本点。
样本点的设置遵循均匀的空间分布原则，以便全面反映试验田的整体情况。
在每个样本点，选取三柱棉花作为样本，记录绿叶数量、总棉铃数量和开放棉铃数量。
这些数据用于计算棉花的吐絮率和脱叶率，两项衡量棉花成熟度和脱叶剂效果的重要指标。
吐絮率通过开放棉铃数量与总棉铃数量的比值计算得出；脱叶率则通过绿叶数量的变化对比计算得出。如 @eq_boll 与 @eq_de 所示。

$ B o l l space O p e n i n g space R a t e (%) == N_(o b)/N_(t b) times 100 % $ <eq_boll>
$ D e f o l i a t i o n space R a t e (%) == (1 - N_(g l)/N_(o l)) times 100 % $ <eq_de>

其中，$N_(o b)$是本次统计时已经开放的棉铃数量，$N_(t b)$第一次统计时所有棉铃的数量 ，
$N_(g l)$为本次统计时残留的叶片数量，$N_(o l)$第一次统计时的叶片数量。

=== 数据预处理

在完成数据收集后，我们依据人工调查结果筛选无人机获取的图像，并为其提供相应的分类标签标注。为提高数据标注的效率与准确性，我们自主开发了一款无人机数据解析软件。
该软件主要用于读取MRK文件中的位置信息，实现图像与地理坐标的匹配，并辅助标注分类标签。
为了增强模型对复杂田间环境的适应性，同时确保结果的可靠性和泛化能力，我们对数据进行了全面的预处理和处理。
首先，由于无人机飞行航线固定，图像中棉花种植方向具有一致性，为增强模型对不同种植方向的识别能力，我们对原始数据进行了随机旋转和镜像操作。
此外，为提高模型对尺度变化的鲁棒性，对图像随机裁剪为224×224的固定大小。
其次，为减少数据集中的冗余信息，我们剔除了田块边缘的图像数据。
田块边缘通常套种其他作物（如玉米或树木），并且无人机施药覆盖不均，导致脱叶效果显著偏差，这些区域不具有代表性。此外，田间图像数据内的颜色差异较小，为避免特征分布过于单一，仅对数据进行了均值化处理，而未采用标准化操作。
最终，我们的软件将处理后的图像数据及其人工标注信息打包，构建为标准化数据集。根据田间调查结果，我们对脱叶率和吐絮率分别进行了精细化分类，其中脱叶率被划分为9个等级，吐絮率被划分为10个等级。

#figure(
  image("dataset_num.png"),
  caption: "成品数据集（图a为数据集例子，D为脱叶率标签，B为吐絮率标签；图b为数据分布情况，其中de_label为脱叶率标签，ba_label为吐絮率标签。标0表示0%-10%，标签1表示10%-20%，以此类推9表示80%-90%",
)<fig:dataset_num>

经过上述预处理后，得到的数据集如@fig:dataset_num 所示，训练集、验证集和测试集比例为5:1:4。其中，训练集用于调整模型参数，验证集用于优化超参数，测试集则用于评估模型的最终性能。
这种数据划分方法确保了模型能够在复杂田间环境中具有较好的泛化能力，并为后续实验提供了坚实的基础。

=== 数据组织方式

数据文件包含三个核心数据集，如表所示。

#figure(
  table(
    columns: 4,
    [字段名], [含义], [维度], [类型],
    [`images`], [变换增强后的图像数据], [`(N, 3, 224, 224)`], [`float32`],
    [`de_labels`], [脱叶率标签], [`(N,)`], [`int8`],
    [`ba_labels`], [吐絮率标签], [`(N,)`], [`int8`],
  ),
  caption: "HDF5 数据文件核心字段说明",
) <tab:hdf5-core>

其中，$N$ 表示样本总数。图像采用通道优先（$C, H, W$）格式，能够被常见机器学习框架直接读入，减少数据转置和额外预处理带来的工程开销。

=== 标签体系

本研究采用双任务识别框架：

1. 脱叶率识别任务（`de`）。
2. 吐絮率识别任务（`ba`）。

标签类型统一为离散整型（`int8`），可直接用于多分类建模。若后续获得连续百分比标注，该数据结构亦可平滑迁移至回归任务。

=== 采集数据的工程约束

为保证实验过程可控且结论可复验，数据读取流程设置以下约束：

1. 必要字段完整性校验（`images`、`de_labels`、`ba_labels`）。
2. 样本数量一致性校验（3 个字段长度严格一致）。
3. 统一随机种子与切分比例控制。

上述约束为后续算法对比提供了稳定且可追溯的数据基础。

== 数据集处理

=== 数据校验与加载

数据加载器在初始化阶段完成以下步骤：

1. 文件存在性验证。
2. HDF5 键名验证。
3. 样本数一致性验证。
4. 训练集、验证集与测试集切分。

默认切分比例为 `0.6/0.1/0.3`，并支持固定随机种子（默认 `seed=42`），以保证不同实验批次间的可比性。

=== 实验规模控制

为支持快速迭代与资源受限场景，本研究在工程实现中加入“按百分比控制实验规模”机制：

- 参数：`sample_percent ∈ (0, 100]`。
- 规则：先按百分比采样，再与 `max_train/val/test_samples` 取较小值。

例如，当 `sample_percent=10` 时，可显著降低训练成本，同时基本保持类别分布结构，适用于模型初筛与超参数粗调阶段。

=== 特征准备策略

当前对比实验以“展平像素特征”作为基线输入：

$ bold(x)_i in RR^(3 times 224 times 224) -> RR^150528 $

该基线方案实现简单、复现门槛低，但其局限性同样明确：空间结构信息被压缩，且在小样本条件下更易过拟合。基于此，后续章节将引入前景分割与光谱指数特征，以增强模型的可解释性与泛化能力。

== 前后背景分割

田间场景中的天空、土壤、阴影与滴灌带等背景干扰，会显著影响脱叶率和吐絮率判别。前景分割的核心目标是将棉株区域从复杂背景中剥离，降低非目标噪声对分类边界的干扰。

=== 分割思路

在仅有 RGB 通道条件下，采用“光谱指数 + 阈值 + 形态学”流程：

1. 计算可见光植被指数（ExG、VARI、NGRDI）。
2. 对指数图执行 Otsu 或自适应阈值分割。
3. 通过形态学开闭运算去噪和补洞。
4. 基于连通域筛选保留主棉株区域。

=== 典型指数

1. 超绿指数（ExG）。
$ "ExG" = 2G - R - B $

2. 可见光大气阻抗植被指数（VARI）。
$ "VARI" = frac(G - R, G + R - B + epsilon) $

3. 归一化绿红差（NGRDI）。
$ "NGRDI" = frac(G - R, G + R + epsilon) $

其中，$epsilon$ 为防止分母为零的极小常数。

=== 分割收益

前景分割可带来三方面收益：

1. 降低背景颜色分布对模型决策的干扰。
2. 强化与棉株状态相关的有效像素占比。
3. 为后续特征提取（面积比例、白絮连通域、纹理统计）提供可靠 ROI。

== 棉花特征提取

在完成前景分割后，可从光谱、形态与纹理三个层面构建特征，以提升模型解释能力和结果稳定性。

=== 光谱指数特征

建议提取以下统计量（均值、方差、分位数）：

1. ExG、VARI、NGRDI。
2. 白度相关特征（如高亮区域比例，用于表征吐絮状态）。
3. 叶绿相关特征（如绿色像素占比，用于表征脱叶进程）。

=== 形态结构特征

1. 前景面积占比。
2. 白色连通域数量与平均面积。
3. 目标边界复杂度（周长/面积）。
4. 目标外接矩形长宽比。

上述特征对“叶片减少”与“棉絮暴露增加”具有明确物理意义。

=== 纹理与统计特征

1. 灰度共生矩阵（GLCM）特征：对比度、同质性、能量。
2. 局部二值模式（LBP）直方图。
3. 通道级统计量：均值、标准差、偏度。

=== 当前基线与特征路线关系

当前工程基线以“展平像素 + 传统模型”为主，目标是快速验证任务可行性。后续可将“光谱指数 + 形态 + 纹理”拼接为低维结构化特征，与当前基线进行公平对比，更清晰体现“基于光谱指数”方法在稳定性与可解释性方面的优势。

== 使用回归法进行脱叶率吐絮率识别

本节基于当前实验记录（`results/experiment_results.csv`）开展分析。实验比较了三种经典方法：

1. PCA + Logistic Regression（PCA+LR）。
2. Random Forest（RF）。
3. Random Forest（Balanced）。

现有记录对应一次完整对比实验（`target=de/ba`），并输出 `train/val/test` 三分结果。

=== 评价指标

本研究采用以下指标：

1. Accuracy（整体正确率）。
2. Macro-F1（类别不均衡场景下更稳健）。

考虑到脱叶率与吐絮率等级普遍存在类别占比不均，Macro-F1 为本章优先关注指标。

=== 实验结果

（1）脱叶率任务（`de`）结果如下。

#figure(
  table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.3864], [*0.1976*], [*0.4091*], [*0.3049*],
    [RandomForest], [0.3409], [0.0658], [0.3258], [0.1469],
    [RandomForestBalanced], [0.3636], [0.1897], [0.3106], [0.1305],
  ),
  caption: "脱叶率（de）实验结果",
) <tab:de-res>

（2）吐絮率任务（`ba`）结果如下。

#figure(
  table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.4091], [*0.3312*], [*0.5000*], [*0.3989*],
    [RandomForest], [*0.4773*], [0.1077], [0.4242], [0.1105],
    [RandomForestBalanced], [*0.4773*], [0.1077], [0.4167], [0.0980],
  ),
  caption: "吐絮率（ba）实验结果",
) <tab:ba-res>

=== 结果解读

1. *PCA+LR 在 Macro-F1 维度综合最优。* 在 `de` 和 `ba` 两任务上，PCA+LR 的验证集与测试集 Macro-F1 均明显高于 RF 系列，说明其在少数类识别上更稳定。
2. *RF 系列存在 Accuracy 与 Macro-F1 不一致现象。* 以 `ba` 任务为例，RF 的验证集 Accuracy 达到 `0.4773`，但 Macro-F1 仅 `0.1077`，表明模型显著偏向多数类。
3. *当前设置存在过拟合风险。* 三类模型训练集指标均接近满分（`train Acc/F1≈1.0`），而测试集下降明显，说明特征表达与模型复杂度之间仍需进一步匹配。

=== 误差来源分析

1. 展平像素特征缺乏显式语义约束。
2. 田间背景变化显著，前景分割尚未充分融入主流程。
3. 类别分布不平衡导致模型决策偏置。
4. 单次随机种子评估存在偶然性。

=== 改进方向

1. 引入“前景分割 + 光谱指数”结构化特征。
2. 在 LR 中联合调节 `C` 与 `n_components`，并加强正则化。
3. 在 RF 中约束树复杂度（`max_depth`、`min_samples_leaf`）。
4. 采用多随机种子重复实验，报告均值与标准差。
5. 引入分层抽样与类别重加权策略，针对性优化 Macro-F1。

== 本章小结

本章围绕“基于光谱指数的脱叶率吐絮率识别”构建了完整技术链路，覆盖数据组织、预处理、前景分割、特征提取与模型识别。当前对比实验显示，PCA+LogisticRegression 在两个任务上的泛化表现更优；但整体仍受过拟合和类别不平衡影响。下一阶段应进一步将光谱指数与分割特征深度耦合到建模流程中，以持续提升模型鲁棒性、可解释性与田间应用价值。
