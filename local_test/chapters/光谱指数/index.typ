#import "../../../template.typ": three-line-table

== 引言

本章使用第 2 章的棉花脱叶率与吐絮率数据，考察传统分类器在不同输入表征下的识别效果。3.2 节首先以 RGB 像素向量建立分类基线，并分析其暴露的问题；3.3 节据此设计颜色与光谱指数结构化特征，在统一数据划分下验证其改进效果；3.4 节进一步在低光场景下测试传统识别流程的稳定性；3.5 节给出本章小结。

== 基于 RGB 像素向量分类

首先从每幅影像中心裁剪 $224 times 224$ 像素区域，再按固定顺序将 RGB 三通道像素值展开为长度为 $224 times 224 times 3 = 150528$ 的一维向量。该输入不包含前景分割、光谱指数、形态或纹理等人工特征。对比模型包括 PCA + Logistic Regression（PCA+LR）和标准 Random Forest（RF）。PCA+LR 用于验证降维后线性判别子空间的可行性，RF 用于评估非线性集成分类器对 RGB 像素向量的拟合能力。

=== 算法流程与评价指标

整体流程包括图像像素展平、PCA 降维或随机森林输入准备、模型训练，以及验证集与测试集评估，具体如@fig:ch3-rgb-ml-flow 所示。脱叶率与吐絮率均为多等级离散标签（等级划分见@chap:dataset），类别占比呈长尾分布，因此采用准确率与 Macro-F1 联合评估。准确率反映总体正确率，Macro-F1 反映各等级的均衡识别能力，并作为本章的主要评价指标。

#figure(
  image("rgb_ml_flow.png", width: 100%),
  caption: "RGB 像素向量的 PCA 与随机森林分类流程。",
) <fig:ch3-rgb-ml-flow>

PCA+LR 分支由 StandardScaler、256 维 PCA 和 Logistic Regression 串联构成；RF 则直接接收 RGB 像素向量。两个模型分别针对脱叶率和吐絮率标签进行训练，并在统一数据划分下计算准确率与 Macro-F1。

本节报告一次固定划分的结果，用于比较当前条件下的分类器表现。其稳定性还需通过多随机种子重复实验及跨地块、跨时段测试评价。

=== 脱叶率与吐絮率分类结果

脱叶率任务结果如@tab:de-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [验证集准确率], [验证集 Macro-F1], [测试集准确率], [测试集 Macro-F1],
    [PCA+LogisticRegression], [0.386], [0.298], [*0.353*], [*0.257*],
    [RandomForest], [0.364], [0.190], [0.355], [0.207],
  )),
  caption: "传统机器学习模型的脱叶率等级分类结果。",
) <tab:de-res>

吐絮率任务结果如@tab:ba-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [验证集准确率], [验证集 Macro-F1], [测试集准确率], [测试集 Macro-F1],
    [PCA+LogisticRegression], [0.489], [0.331], [*0.461*], [*0.302*],
    [RandomForest], [0.424], [0.277], [0.359], [0.155],
  )),
  caption: "传统机器学习模型的吐絮率等级分类结果。",
) <tab:ba-res>

PCA+LR 在两项任务上的测试集 Macro-F1 均高于 RF：脱叶率任务分别为 0.257 和 0.207，吐絮率任务分别为 0.302 和 0.155。RF 在吐絮率任务上的验证集准确率为 0.424，而 Macro-F1 仅为 0.277，两者差距明显，表明预测偏向多数类。两个模型的训练集指标接近满分，测试集指标则明显下降，存在过拟合现象。

在当前 RGB 像素向量输入下，PCA+LR 的等级识别比 RF 更均衡。吐絮率任务的 Macro-F1 高于脱叶率任务，可能与白絮外露的颜色差异较明显有关；脱叶率还受到阴影、遮挡和背景显露的影响。这些现象提示后续特征设计需同时关注颜色变化与背景干扰。

=== 综合分析

RGB 像素向量基线的结果暴露出三类问题。其一，像素向量缺乏显式语义约束，模型可能利用背景纹理等共现信息，田间背景变化也会直接改变输入分布。其二，类别不均衡造成多数类偏置，使准确率与 Macro-F1 出现明显差异，RF 的裂差尤为突出。其三，各模型训练集指标接近满分而测试集明显下降，高维像素输入在小样本下过拟合严重。

针对上述问题，改进流程可沿两个方向展开：一是通过前景分割抑制背景干扰，二是以低维结构化特征替代高维像素向量，并配合类别重加权和分层采样缓解多数类偏置。这些问题也为后续深度模型的背景抑制和边界等级表征提供了设计依据。下一节按这一思路设计结构化特征并进行定量验证。

== 基于结构化特征分类

=== 结构化特征设计

田间影像中的天空、土壤、阴影与滴灌带可能干扰双指标判断。本章构建的结构化特征候选提取流程如@fig:ch3-structured-feature-flow 所示。该流程首先从固定航点 RGB 影像中裁剪中心区域，通过可见光指数与阈值分割获得候选棉株 ROI；随后并行提取光谱与颜色、形态结构及纹理统计特征，经标准化和拼接形成候选结构化特征向量。

#figure(
  image("structured_feature_flow.png", width: 100%),
  caption: "棉花双指标识别的结构化特征候选提取流程。",
) <fig:ch3-structured-feature-flow>

*棉株前景分割。*仅使用 RGB 影像时，可通过可见光植被指数增强植株与背景的差异。这类指数计算简单，部分比值或归一化形式还可减轻整体亮度缩放的影响。本研究选择 ExG、VARI 与 NGRDI 作为分割和绿度描述的候选特征，定义如下，其中 $epsilon$ 是防止分母为零的极小常数：

超绿指数（ExG）：
$ "ExG" = 2G - R - B $

可见光大气阻抗植被指数（VARI）：
$ "VARI" = frac(G - R, G + R - B + epsilon) $

归一化绿红差（NGRDI）：
$ "NGRDI" = frac(G - R, G + R + epsilon) $

ExG 通过强调绿色通道相对红蓝通道的优势响应，常用于农业场景的植被提取与背景抑制@woebbecke1995exg；VARI 通过可见光通道的组合构造对大气与照度变化更稳定的表征，被用于植被覆盖度估计等任务@gitelson2002vari。NGRDI 采用“归一化差分”形式，可视为将经典归一化差分思想移植到可见光域的简化实现（与 NDVI 等归一化差分指数在形式上相近），在弱监督场景下可作为快速分割与绿度刻画的候选特征@tucker1979ndvi。

对于成熟期棉花场景，这些指数与催熟过程中的视觉变化具有较明确的对应关系。随着脱叶推进，叶片覆盖度下降，绿色通道在 ROI 内的主导程度逐步减弱，ExG、VARI 和 NGRDI 等指数通常呈下降趋势；随着吐絮增加，高亮白色纤维暴露面积上升，局部分位数和高阶统计量的分布也会发生变化。因此，在缺少多光谱或高光谱载荷的条件下，基于 RGB 的可见光指数仍可作为脱叶率与吐絮率识别的有效先验。

在拟议方案中，可基于指数图执行阈值分割以获得初始前景掩膜。Otsu 方法通过最大化类间方差自动选择阈值@otsu1979threshold，随后采用形态学开闭运算去噪与补洞，并结合连通域筛选保留主要棉株区域。ROI 将指数、纹理和形态统计限定在棉株前景内，可减轻土壤、地膜、滴灌带和阴影造成的背景干扰。

为直观展示不同成熟阶段下可见光指数及候选分割流程的响应差异，选取早期、中期和后期代表性图像，分别计算 ExG、VARI 和 NGRDI，并基于 ExG 响应生成候选绿叶区域掩膜，结果如@fig:ch3-index-stages 所示。三个阶段的同类指数采用统一显示范围，掩膜由 Otsu 阈值分割、形态学开闭运算及小连通域清理获得。

#figure(
  image("fig3_indices_stages.png", width: 100%),
  caption: "不同成熟阶段棉花图像的可见光指数响应与候选绿叶区域分割结果。",
) <fig:ch3-index-stages>

随着脱叶和吐絮进程推进，绿色冠层响应整体减弱，白絮与裸露背景比例增加。三类指数主要突出绿色植被，而亮度、白度和连通域统计可进一步补充对白絮暴露特征的描述。

*颜色与光谱指数统计特征。*颜色与光谱指数统计可描述叶片减少、白絮外露增加时的图像变化，也是本章定量验证的特征类型。候选方案包括 ExG、VARI 和 NGRDI 的均值、方差、分位数、偏度与峰度，也包括高亮像素占比、绿色像素占比及通道强度分布。获得可靠 ROI 后，可将这些统计限定在棉株前景内。高亮像素占比和高亮分位数可反映棉絮外露，绿度统计可反映叶片覆盖；分位数、偏度和峰度则补充均值难以描述的分布差异，有助于分析局部棉絮暴露和冠层稀疏化。

*形态与纹理候选特征。*形态结构特征可在颜色统计之外补充冠层结构与棉絮空间分布的几何信息，候选特征包括前景面积占比、白色连通域数量及平均面积、边界复杂度和外接矩形长宽比。纹理特征则可通过灰度共生矩阵（GLCM）@haralick1973glcm 和局部二值模式（LBP）@ojala2002lbp 刻画棉絮、叶片与背景粗糙度的差异。受验证条件限制，形态与纹理特征未纳入本章定量实验，其收益需在统一训练与评测协议下进一步验证；区域生长、图割和学习式分割也可用于棉株提取，分别存在对初始条件敏感和需要像素级标注的问题，同样作为后续候选方案。

=== 实验流程与设置

针对像素基线暴露的问题，进一步使用颜色与指数统计检验输入表征的改进效果。为在同一条件下比较输入表征，各模型使用相同的 8737 个图像块和相同的随机划分，在像素基线基础上增加 Structured+LR、Structured+RF 和 Structured+SVM。光照变化对传统识别流程的影响在 3.4 节单独测试。

除准确率和 Macro-F1 外，本节计算等级平均绝对误差（MAE），衡量预测与真实等级的平均偏差：
$ "MAE" = frac(1, N) sum_(i=1)^N abs(hat(y)_i-y_i) $
其中 $y_i$、$hat(y)_i$ 为真实和预测等级，$N$ 为样本数。MAE 单位为“级”。

=== 脱叶率与吐絮率分类结果

脱叶率实验结果见@tab:structured-de。三种结构化分类器的 Macro-F1 均在 0.856 以上，明显优于像素基线。其中 Structured+LR 的 Macro-F1 与 MAE 最优，分别为 0.867 和 0.069 级，准确率与 Structured+RF 并列最高（0.851）。

#figure(three-line-table(table(columns: (1.5fr, 1fr, 1fr, 1fr),
[模型], [准确率], [Macro-F1], [MAE],
[Structured+LR], [*0.851*], [*0.867*], [*0.069*],
[Structured+RF], [0.851], [0.860], [0.081],
[Structured+SVM], [0.829], [0.856], [0.092],
)), caption: [结构化特征分类实验中脱叶率等级分类的测试集结果。]) <tab:structured-de>

相同 RF 配置下，结构化输入的 Macro-F1 明显高于像素输入，等级 MAE 也大幅降低，说明颜色与指数统计在本次划分下提供了更稳定的判别信息。对于脱叶率任务，低维统计量已能表达相当部分的等级差异，线性分类器也获得了较好的结果。

吐絮率实验结果见@tab:structured-ba。Structured+SVM 的准确率与 Macro-F1 最优，分别为 0.854 和 0.830；Structured+RF 的 MAE 最低，为 0.115 级，其余指标与 SVM 接近。Structured+LR 也明显优于像素基线，但低于两种非线性分类器，提示吐絮率识别可进一步利用统计特征之间的非线性关系。

#figure(three-line-table(table(columns: (1.5fr, 1fr, 1fr, 1fr),
[模型], [准确率], [Macro-F1], [MAE],
[Structured+LR], [0.834], [0.794], [0.161],
[Structured+RF], [0.853], [0.824], [*0.115*],
[Structured+SVM], [*0.854*], [*0.830*], [0.127],
)), caption: [结构化特征分类中吐絮率等级分类的测试集结果。]) <tab:structured-ba>

相同 RF 配置下，结构化输入同样明显降低了吐絮率任务的等级误差。两项任务的共同改善表明，颜色与指数统计适合用作本任务传统分类器的输入。脱叶率任务上线性分类器已接近最优，提示其等级边界在颜色统计空间中近似线性可分；吐絮率任务则明显受益于非线性分类器，说明统计特征之间存在可利用的非线性组合关系。此外，结构化模型训练耗时不足 1 秒，较像素 RF 快两个数量级以上。

=== 特征与成熟等级的相关性

为分析统计特征与成熟等级的关系，对所有图像块的 21 维特征分别计算与脱叶率、吐絮率等级的 Spearman 秩相关系数。代表性结果见@tab:structured-correlation。相关性分析用于解释特征，分类实验仍采用完整的 21 维输入。

#figure(three-line-table(table(columns: 3,
[特征], [脱叶率相关系数], [吐絮率相关系数],
[ExG中位数], [−0.8557], [−0.8201],
[ExG均值], [−0.8287], [−0.7979],
[$D_("GR",3)$ 中位数], [−0.8339], [−0.8099],
[NGRDI中位数], [−0.8307], [−0.8065],
[蓝通道标准差], [0.7526], [0.6558],
)), caption: [代表性结构化特征与成熟等级的Spearman相关系数。]) <tab:structured-correlation>

ExG 中位数与脱叶率等级的相关系数为 −0.8557，与叶片减少时绿色响应减弱的变化一致。$D_("GR",3)$ 和 NGRDI 中位数也呈较强负相关，说明绿色相对红色的比例与成熟等级有关。蓝通道标准差呈正相关，反映观测期内图像颜色分布离散程度的变化。

绿度统计与吐絮率等级同样具有较强负相关，反映本数据中脱叶和吐絮随物候进程共同变化的特征。这些结果为统计量的农艺解释提供依据，但相关系数不等同于分类器中的特征重要性。脱叶与吐絮随物候共同推进，特征与等级的强相关可能部分来自共同的时间变化，需要在同日期、同地块内进一步控制比较。

@fig:corr-de 和@fig:corr-ba 进一步展示了相关度最高的六个特征随等级变化的箱线分布。随着脱叶等级升高，ExG、VARI 和 NGRDI 的中位数、均值及低分位数整体下移，且高等级组内离散度明显收缩，与吐絮率等级中的分布变化趋势一致。箱线分布还显示相邻等级间存在一定重叠，说明单一特征难以完全区分边界等级，多特征组合与分类器仍是必要的。

#figure(
  image("脱叶结构化特征相关度.png", width: 100%),
  caption: [相关度最高的六个结构化特征随脱叶率等级变化的箱线分布。子图标题括号内为该特征与脱叶率等级的 Spearman 秩相关系数。],
) <fig:corr-de>

#figure(
  image("吐絮结构化特征相关度.png", width: 100%),
  caption: [相关度最高的六个结构化特征随吐絮率等级变化的箱线分布。子图标题括号内为该特征与吐絮率等级的 Spearman 秩相关系数。],
) <fig:corr-ba>

== 低光场景识别测试

为考察光照变化对传统识别流程的影响，本节构建两组输入条件：原始正常光照输入（RAW）和经@chap:lowlight 低光退化模型生成的低光输入（LOW）。对 LOW 输入重复与 RAW 完全相同的训练与测试流程，比较各模型在两种条件下的准确率，并以 LOW 相对 RAW 的精度下降幅度衡量模型对光照的敏感性，结果见@tab:structured-low。

#figure(three-line-table(table(columns: (1.5fr, 1fr, 1fr, 1fr, 1fr),
  [模型], [脱叶率准确率], [吐絮率准确率], [脱叶率下降], [吐絮率下降],
[Pixel+PCA+LR], [0.180], [0.375], [0.173], [0.086],
[Pixel+RF], [0.143], [0.274], [0.212], [0.085],
[Structured+LR], [0.544], [0.555], [0.307], [0.279],
[Structured+RF], [0.433], [0.527], [0.418], [0.326],
[Structured+SVM], [0.441], [0.456], [0.388], [0.398],
)), caption: [低光输入（LOW）下脱叶率与吐絮率的识别结果。下降列为 LOW 相对 RAW 测试精度的绝对下降幅度。]) <tab:structured-low>

低光输入下所有模型的识别精度均出现下降，但下降模式存在明显差异。像素基线在低光下接近失效：Pixel+RF 的脱叶率准确率降至 0.143，Pixel+PCA+LR 降至 0.180，已接近随机猜测水平。像素模型在吐絮率任务上的下降幅度看似较小（0.085 至 0.086），但这只是因为其正常光照下的精度本就偏低，可下降空间有限，并不代表其对光照变化稳健。

结构化特征模型的绝对下降幅度更大（脱叶率 0.307 至 0.418，吐絮率 0.279 至 0.398），这是其正常光照精度基数较高所致；低光下其精度仍维持在 0.43 至 0.56，明显优于像素输入，说明颜色与指数统计在低光下仍保留了部分判别能力。下降的主要来源是低光对颜色信息的系统性破坏：曝光不足压缩了各通道的动态范围，噪声抬升和通道比例偏移改变了 ExG、VARI 等指数及亮度统计的分布，绿叶与白絮之间的颜色对比减弱，使基于颜色统计的等级边界发生漂移。

上述结果表明，光照变化是传统识别流程的主要失效因素之一，仅靠特征设计无法根除输入分布漂移的影响。这一结论从输入端说明了低光增强的必要性：@chap:lowlight 将在成像退化建模的基础上构建低光增强模型，以恢复颜色与结构信息，为后续识别提供稳定的输入。

== 小结

本章以 RGB 像素向量构建传统机器学习分类基线，比较了 PCA+Logistic Regression 和 Random Forest，实验反映出类别不均衡、背景干扰和过拟合问题。在此基础上，本章设计了前景分割、颜色与光谱指数统计、形态和纹理等结构化特征方案，并在 8737 个图像块的统一数据划分下，对其中 21 维颜色与指数统计特征进行了定量验证。结果表明：结构化特征配合 LR、RF 和 SVM 在两项任务上均明显优于像素基线，脱叶率测试 Macro-F1 最高 0.867（Structured+LR），吐絮率最高 0.830（Structured+SVM）；Spearman 秩相关分析显示 ExG、$D_("GR",3)$ 和 NGRDI 的分位数与成熟等级呈强负相关，为统计量提供了农艺解释。本章还构建了低光输入（LOW）与正常光照输入（RAW）的对比测试，结果表明低光下所有模型的识别精度均明显下降，像素基线接近失效，结构化特征模型虽保持相对优势但也出现 0.28 至 0.42 的精度下降，说明仅靠特征设计无法解决光照引起的输入分布漂移，从输入端进行低光增强是必要的。
