#import "../../../template.typ": three-line-table

== 引言

本章使用第 2 章的棉花双指标数据，考察传统分类器在不同输入表征下的识别效果。先以 RGB 像素向量建立基线，再验证颜色与光谱指数统计特征，并讨论前景分割、形态和纹理等候选方案。3.2 节介绍特征设计，3.3 和 3.4 节分别报告像素基线与结构化特征实验，3.5 和 3.6 节给出讨论与小结。

== 结构化特征方案

田间影像中的天空、土壤、阴影与滴灌带可能干扰双指标判断。前景分割将后续统计限定在棉株区域，是减少背景影响的一种处理方法。

本章构建的结构化特征候选提取流程如@fig:ch3-structured-feature-flow 所示。该流程首先从固定航点 RGB 影像中裁剪中心区域，通过可见光指数与阈值分割获得候选棉株 ROI；随后并行提取光谱与颜色、形态结构及纹理统计特征，经标准化和拼接形成候选结构化特征向量。

#figure(
  image("structured_feature_flow.png", width: 100%),
  caption: "棉花双指标识别的结构化特征候选提取流程。",
) <fig:ch3-structured-feature-flow>

=== 棉株对象分割

仅使用 RGB 影像时，可通过可见光植被指数增强植株与背景的差异。这类指数计算简单，部分比值或归一化形式还可减轻整体亮度缩放的影响。本研究选择 ExG、VARI 与 NGRDI 作为分割和绿度描述的候选特征，定义如下，其中 $epsilon$ 是防止分母为零的极小常数：

超绿指数（ExG）：
$ "ExG" = 2G - R - B $

可见光大气阻抗植被指数（VARI）：
$ "VARI" = frac(G - R, G + R - B + epsilon) $

归一化绿红差（NGRDI）：
$ "NGRDI" = frac(G - R, G + R + epsilon) $

ExG 通过强调绿色通道相对红蓝通道的优势响应，常用于农业场景的植被提取与背景抑制@woebbecke1995exg；VARI 通过可见光通道的组合构造对大气与照度变化更稳定的表征，被用于植被覆盖度估计等任务@gitelson2002vari。NGRDI 采用“归一化差分”形式，可视为将经典归一化差分思想移植到可见光域的简化实现（与 NDVI 等归一化差分指数在形式上相近），在弱监督场景下可作为快速分割与绿度刻画的候选特征@tucker1979ndvi。

对于成熟期棉花场景，这些指数与催熟过程中的视觉变化具有较明确的对应关系。随着脱叶推进，叶片覆盖度下降，绿色通道在 ROI 内的主导程度逐步减弱，ExG、VARI 和 NGRDI 等指数通常呈下降趋势；随着吐絮增加，高亮白色纤维暴露面积上升，局部分位数和高阶统计量的分布也会发生变化。因此，在缺少多光谱或高光谱载荷的条件下，基于 RGB 的可见光指数仍可作为脱叶率与吐絮率识别的有效先验。

在拟议方案中，可基于指数图执行阈值分割以获得初始前景掩膜。Otsu 方法通过最大化类间方差自动选择阈值@otsu1979threshold，随后采用形态学开闭运算去噪与补洞，并结合连通域筛选保留主要棉株区域。ROI 将指数、纹理和形态统计限定在棉株前景内，可减轻土壤、地膜、滴灌带和阴影造成的背景干扰。该流程参数少且便于追溯，适合用于后续结构化特征实验，其实际贡献仍需通过分割质量和分类消融实验验证。

为直观展示不同成熟阶段下可见光指数及候选分割流程的响应差异，选取早期、中期和后期代表性图像，分别计算 ExG、VARI 和 NGRDI，并基于 ExG 响应生成候选绿叶区域掩膜，结果如@fig:ch3-index-stages 所示。三个阶段的同类指数采用统一显示范围，掩膜由 Otsu 阈值分割、形态学开闭运算及小连通域清理获得。

#figure(
  image("fig3_indices_stages.png", width: 100%),
  caption: "不同成熟阶段棉花图像的可见光指数响应与候选绿叶区域分割结果。",
) <fig:ch3-index-stages>

随着脱叶和吐絮进程推进，绿色冠层响应整体减弱，白絮与裸露背景比例增加。三类指数主要突出绿色植被，而亮度、白度和连通域统计可进一步补充对白絮暴露特征的描述。

区域生长、图割和学习式分割也可用于棉株提取，但前两者对初始条件和背景较敏感，后者需要像素级标注及额外训练。本章先验证无需分割的颜色与指数统计特征；可见光指数、阈值分割和形态学修正构成后续 ROI 提取方案，其效果还需在相同数据划分下比较。

=== 光谱指数特征

颜色与光谱指数统计可描述叶片减少、白絮外露增加时的图像变化。候选方案包括 ExG、VARI 和 NGRDI 的均值、方差、分位数、偏度与峰度，也包括高亮像素占比、绿色像素占比及通道强度分布。获得可靠 ROI 后，可将这些统计限定在棉株前景内。

高亮像素占比和高亮分位数可反映棉絮外露，绿度统计可反映叶片覆盖。分位数、偏度和峰度则补充均值难以描述的分布差异，有助于分析局部棉絮暴露和冠层稀疏化。

=== 形态结构特征

形态结构特征可在颜色统计之外补充冠层结构与棉絮空间分布的几何信息。候选特征包括前景面积占比、白色连通域数量及平均面积、边界复杂度（周长/面积）和外接矩形长宽比，分别用于描述植株覆盖程度、棉絮团块尺度、轮廓破碎程度与 ROI 整体形态。

脱叶后冠层孔隙率增大，前景覆盖面积和边界复杂度随之变化；吐絮后，白色连通域的数量、尺度和空间聚集程度也会改变。这些形态变量可补充颜色统计，并帮助区分棉絮增加、局部反光和背景穿透造成的亮度变化。

=== 纹理与统计特征

在棉株区域内，冠层纹理、行间结构和棉絮团块的差异可通过纹理统计描述。常用方法如下。

灰度共生矩阵（GLCM）通过统计不同灰度在空间邻域的共现频率，能够描述冠层纹理的对比度、同质性与能量等特性@haralick1973glcm；局部二值模式（LBP）通过局部邻域二值编码刻画微纹理模式，具有较强的旋转与灰度鲁棒性@ojala2002lbp；此外，通道级统计量（均值、标准差、偏度、分位数等）可作为轻量补充，用于描述 ROI 内颜色与亮度分布的高阶形态。

成熟期棉株图像中的地膜、裸土和阴影可能产生与棉絮相似的局部反差。纹理描述子可通过灰度共生关系、局部模式重复性和方向分布刻画棉絮、叶片与背景粗糙度的差异，作为颜色和指数统计的补充。

=== 当前基线与特征路线关系

当前工程基线采用“RGB 像素向量 + 传统模型”，用于快速验证任务可行性。3.4 节将颜色与光谱指数统计拼接为低维结构化特征，在统一数据划分下与像素基线进行定量对比，检验其稳定性和可解释性；形态与纹理等空间特征仍作为待验证方案。经实验验证有效的特征还可作为轻量深度模型的先验或辅助分支输入。

== 基于 RGB 像素向量的传统分类基线

对于固定航点采集的 140 个样本，首先从每幅影像中心裁剪 $224 times 224$ 像素区域，再按固定顺序将 RGB 三通道像素值展开为长度为 $224 times 224 times 3 = 150528$ 的一维向量。该输入不包含前述 ROI、光谱指数、形态或纹理特征。对比模型包括 PCA + Logistic Regression（PCA+LR）、标准 Random Forest（RF）和类别均衡 Random Forest（Balanced RF）。PCA+LR 用于验证降维后线性判别子空间的可行性，RF 用于评估非线性集成分类器对 RGB 像素向量的拟合能力，Balanced RF 用于考察类别均衡策略对长尾等级识别的影响。

=== 算法流程

整体流程包括图像像素展平、PCA 降维或随机森林输入准备、模型训练，以及验证集与测试集评估，具体如@fig:ch3-rgb-ml-flow 所示。脱叶率与吐絮率均为多等级离散标签（等级划分见@chap:dataset），类别占比呈长尾分布，因此采用 Accuracy 与 Macro-F1 联合评估。Accuracy 反映总体正确率，Macro-F1 反映各等级的均衡识别能力，并作为本章的主要评价指标。

#figure(
  image("rgb_ml_flow.png", width: 100%),
  caption: "RGB 像素向量的 PCA 与随机森林分类流程。",
) <fig:ch3-rgb-ml-flow>

PCA+LR 分支由 StandardScaler、256 维 PCA 和 Logistic Regression 串联构成；标准 RF 与类别均衡 RF 则直接接收 RGB 像素向量。三个模型分别针对脱叶率和吐絮率标签进行训练，并在统一数据划分下计算 Accuracy 与 Macro-F1。

本节报告一次固定划分的结果，用于比较当前条件下的分类器表现。其稳定性还需通过多随机种子重复实验及跨地块、跨时段测试评价。

=== RGB 像素向量基线存在的问题

RGB 像素向量缺乏显式语义约束，模型可能利用背景纹理等共现信息。田间背景变化也会影响输入分布，类别不均衡则容易造成多数类偏置，使 Accuracy 与 Macro-F1 出现明显差异。改进流程可进一步纳入前景分割、结构化特征、类别重加权和分层采样，并采用多次重复实验评价结果稳定性。这些问题也为后续深度模型的背景抑制和边界等级表征提供了设计依据。

=== 双指标分类结果

脱叶率任务结果如@tab:de-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.3864], [*0.1976*], [*0.4091*], [*0.3049*],
    [RandomForest], [0.3409], [0.0658], [0.3258], [0.1469],
    [RandomForestBalanced], [0.3636], [0.1897], [0.3106], [0.1305],
  )),
  caption: "传统机器学习模型的脱叶率等级分类结果。",
) <tab:de-res>

吐絮率任务结果如@tab:ba-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.4091], [*0.3312*], [*0.5000*], [*0.3989*],
    [RandomForest], [*0.4773*], [0.1077], [0.4242], [0.1105],
    [RandomForestBalanced], [*0.4773*], [0.1077], [0.4167], [0.0980],
  )),
  caption: "传统机器学习模型的吐絮率等级分类结果。",
) <tab:ba-res>

PCA+LR 在两项任务上的测试集 Macro-F1 均高于 RF 系列，脱叶率和吐絮率分别达到 0.3049 和 0.3989。RF 在吐絮率验证集上的 Accuracy 为 0.4773，而 Macro-F1 仅为 0.1077，表明预测明显偏向多数类。三类模型的训练集指标接近满分，测试集指标则明显下降，存在过拟合现象。

在当前 RGB 像素向量输入下，PCA+LR 的等级识别比 RF 系列更均衡。吐絮率任务的 Macro-F1 高于脱叶率任务，可能与白絮外露的颜色差异较明显有关；脱叶率还受到阴影、遮挡和背景显露的影响。这些现象提示后续特征设计需同时关注颜色变化与背景干扰。

== 结构化特征对比实验

=== 实验设置

针对上述像素基线暴露的问题，进一步使用颜色与指数统计检验输入表征的改进效果。为在同一条件下比较输入表征，各模型使用相同的 8737 个图像块和相同的随机划分。本次重新训练 Pixel+PCA+LR 和 Pixel+RF 作为像素基线参照，并增加 Structured+LR、Structured+RF 和 Structured+SVM。

除 Accuracy 和 Macro-F1 外，本节计算等级平均绝对误差（MAE），衡量预测与真实等级的平均偏差：
$ "MAE" = frac(1, N) sum_(i=1)^N abs(hat(y)_i-y_i) $
其中 $y_i$、$hat(y)_i$ 为真实和预测等级，$N$ 为样本数。MAE 单位为“级”。

=== 结构化特征与像素输入的对比

脱叶率实验结果见@tab:structured-de。三种结构化分类器的 Macro-F1 均在 0.90 左右，明显优于像素基线。其中 Structured+LR 的 Accuracy 与 Macro-F1 最高，分别为 0.892 和 0.909。

#figure(three-line-table(table(columns: (1.5fr, 1fr, 1fr, 1fr),
[模型], [Accuracy], [Macro-F1], [MAE],
[Structured+LR], [*0.892*], [*0.909*], [*0.158*],
[Structured+RF], [0.891], [0.899], [0.165],
[Structured+SVM], [0.874], [0.907], [0.164],
)), caption: [统一补充实验中脱叶率等级分类的测试集结果。]) <tab:structured-de>

相同 RF 配置下，结构化输入的 Macro-F1 明显高于像素输入，等级 MAE 也大幅降低，说明颜色与指数统计在本次划分下提供了更稳定的判别信息。对于脱叶率任务，低维统计量已能表达相当部分的等级差异，线性分类器也获得了较好的结果。

吐絮率实验结果见@tab:structured-ba。Structured+SVM 的 Accuracy、Macro-F1 和 MAE 三项指标均最优，分别为 0.898、0.851 和 0.215 级。Structured+RF 紧随其后；Structured+LR 也明显优于像素基线，但低于两种非线性分类器，提示吐絮率识别可进一步利用统计特征之间的非线性关系。

#figure(three-line-table(table(columns: (1.5fr, 1fr, 1fr, 1fr),
[模型], [Accuracy], [Macro-F1], [MAE],
[Structured+LR], [0.836], [0.747], [0.293],
[Structured+RF], [0.881], [0.819], [0.235],
[Structured+SVM], [*0.898*], [*0.851*], [*0.215*],
)), caption: [统一补充实验中吐絮率等级分类的测试集结果。]) <tab:structured-ba>

相同 RF 配置下，结构化输入同样明显降低了吐絮率任务的等级误差。两项任务的共同改善表明，颜色与指数统计适合用作本任务传统分类器的输入。

=== 特征与成熟等级的相关性

为分析统计特征与成熟等级的关系，对 8737 个图像块的 21 维特征分别计算与脱叶率、吐絮率等级的 Spearman 秩相关系数。代表性结果见@tab:structured-correlation。相关性分析用于解释特征，分类实验仍采用完整的 21 维输入。

#figure(three-line-table(table(columns: 3,
[特征], [脱叶率相关系数], [吐絮率相关系数],
[ExG中位数], [−0.8557], [−0.8201],
[ExG均值], [−0.8287], [−0.7979],
[$D_("GR",3)$ 中位数], [−0.8339], [−0.8099],
[NGRDI中位数], [−0.8307], [−0.8065],
[蓝通道标准差], [0.7526], [0.6558],
)), caption: [代表性结构化特征与成熟等级的Spearman相关系数。]) <tab:structured-correlation>


ExG 中位数与脱叶率等级的相关系数为 −0.8557，与叶片减少时绿色响应减弱的变化一致。$D_("GR",3)$ 和 NGRDI 中位数也呈较强负相关，说明绿色相对红色的比例与成熟等级有关。蓝通道标准差呈正相关，反映观测期内图像颜色分布离散程度的变化。

绿度统计与吐絮率等级同样具有较强负相关，反映本数据中脱叶和吐絮随物候进程共同变化的特征。这些结果为统计量的农艺解释提供依据，但相关系数不等同于分类器中的特征重要性。

== 讨论

在当前固定切分设置下，像素基线中 PCA+LR 的 Macro-F1 高于 RF 系列，RF 系列的 Accuracy 与 Macro-F1 差异较大，反映出类别不均衡造成的多数类偏置。统一划分下的对比实验进一步表明，21 维颜色与指数统计特征在两项任务上均明显优于 150528 维像素向量：脱叶率测试 Macro-F1 最高达 0.909，吐絮率达 0.851，等级 MAE 降至 0.3 级以内，且结构化模型训练耗时不足 1 秒，较像素 RF 快两个数量级以上。脱叶率任务上线性分类器已接近最优，提示其等级边界在颜色统计空间中近似线性可分；吐絮率任务则明显受益于非线性分类器，说明统计特征之间存在可利用的非线性组合关系。

这些结果仍受样本和划分条件限制。脱叶与吐絮随物候共同推进，特征与等级的强相关可能部分来自共同的时间变化，需要在同日期、同地块内进一步控制比较。本章仅报告一次固定划分，跨地块、跨时段和多随机种子下的稳定性尚待评价；形态与纹理特征也未纳入定量实验。

像素向量基线反映了背景干扰、过拟合和类别偏置问题；颜色与指数统计则将输入压缩到较低维度，在当前划分下取得了更好的等级识别结果。这些统计量便于解释，可用于检查深度模型的误判，也可作为后续先验或辅助分支的候选输入。

== 小结

本章以 RGB 像素向量构建传统机器学习工程基线，比较了 PCA+Logistic Regression、Random Forest 和 Balanced RF，实验反映出类别不均衡、背景干扰和过拟合问题。在此基础上，本章从光谱指数、前景分割、形态和纹理等方面设计了结构化特征方案，并在 8737 个图像块的统一数据划分下，对其中 21 维颜色与指数统计特征进行了定量验证。结果表明：结构化特征配合 LR、RF 和 SVM 在两项任务上均明显优于像素基线，脱叶率测试 Macro-F1 最高 0.909，吐絮率最高 0.851；Spearman 秩相关分析显示 ExG、$D_("GR",3)$ 和 NGRDI 的分位数与成熟等级呈强负相关，为统计量提供了农艺解释。形态与纹理等空间特征尚未纳入本章定量实验，其收益需在统一训练与评测协议下进一步验证。
