#import "../../../template.typ": three-line-table

== 引言

本研究基于@chap:dataset 构建的数据集，以 RGB 像素向量建立传统机器学习工程基线，并在此基础上分析“光谱指数 + 前景分割 + 结构化特征”的可解释改进方案。需要明确的是，本章定量结果来自 RGB 像素向量结合 PCA 或传统分类器的固定切分实验；光谱指数、形态与纹理部分用于说明后续结构化特征的设计依据和实现路径，不作为已完成实验结果报告。

成熟期棉花催熟作业效果通常以脱叶率与吐絮率两项指标衡量，但在大田尺度下依赖人工抽样与经验判断难以满足“快速、客观、可复现”的在线管理需求。针对这一问题，本章首先以 RGB 像素向量为输入，比较 PCA+Logistic Regression、Random Forest 和类别均衡 Random Forest，并采用 Accuracy 与 Macro-F1 评价多等级脱叶率与吐絮率任务。在当前一次固定切分实验中，PCA+Logistic Regression 在两项任务的测试集 Macro-F1 上均高于 Random Forest 系列（脱叶率为 0.3049，吐絮率为 0.3989），而 Accuracy 与 Macro-F1 的背离反映出类别不均衡对模型决策的影响。针对 RGB 像素向量缺乏语义约束的问题，本章进一步分析利用 ExG、VARI、NGRDI、前景 ROI、形态和纹理统计构造低维结构化特征的方案，为后续实验改进提供依据。

成熟期棉花田间影像呈现显著的背景复杂性与成像非稳定性：地表土壤反射、滴灌带与覆膜区域带来强背景纹理，阴影与逆光导致亮度动态范围压缩，且不同地块与不同作业批次的冠层结构差异会引入分布偏移。这些因素使得“直接对 RGB 像素建模”的泛化能力受限，并增加模型在跨地块、跨时段条件下的失效风险。

从工程应用角度看，传统机器学习在小样本条件和快速验证方面仍具有研究价值。当前实验采用“RGB 像素向量—降维/分类—指标评估”的工程流程；与作物生理含义对应的绿度、白度、连通域和纹理统计量则作为结构化特征方案进行分析，用于说明如何在后续实验中提高输入可解释性。

本章的重点不在于证明结构化特征已经优于 RGB 像素向量，也不直接比较传统方法与深度学习方法的优劣，而是先报告可复现的工程基线，再给出可解释特征的设计方案。光谱指数、形态与纹理特征有望将叶片残留、棉絮暴露、冠层稀疏化和背景干扰等现象转化为可测变量，但其实际收益仍需在统一切分和训练协议下进一步验证。

== 结构化特征方案：背景与棉株对象分割

田间场景中的天空、土壤、阴影与滴灌带等背景干扰，会显著影响脱叶率和吐絮率判别。前景分割的核心目标是将棉株区域从复杂背景中剥离，降低非目标噪声对分类边界的干扰。

=== 基于颜色特征的植被背景分割

在仅有 RGB 通道条件下，常用做法是利用可见光植被指数构造“植株与背景”的可分离表征。指数的优势在于其形式简单、可快速计算，并能在一定程度上抵消光照强度整体变化带来的比例缩放影响。结合田间棉花背景与冠层颜色分布特征，本研究将 ExG、VARI 与 NGRDI 三类可见光指数选作后续分割与绿度刻画的候选表征，其定义分别为（其中 $epsilon$ 为防止分母为零的极小常数）：

超绿指数（ExG）：
$ "ExG" = 2G - R - B $

可见光大气阻抗植被指数（VARI）：
$ "VARI" = frac(G - R, G + R - B + epsilon) $

归一化绿红差（NGRDI）：
$ "NGRDI" = frac(G - R, G + R + epsilon) $

ExG 通过强调绿色通道相对红蓝通道的优势响应，常用于农业场景的植被提取与背景抑制@woebbecke1995exg；VARI 通过可见光通道的组合构造对大气与照度变化更稳定的表征，被用于植被覆盖度估计等任务@gitelson2002vari。NGRDI 采用“归一化差分”形式，可视为将经典归一化差分思想移植到可见光域的简化实现（与 NDVI 等归一化差分指数在形式上相近），在弱监督场景下可作为快速分割与绿度刻画的候选特征。@tucker1979ndvi

对于成熟期棉花场景，这些指数与催熟过程中的视觉变化具有较明确的对应关系。随着脱叶推进，叶片覆盖度下降，绿色通道在 ROI 内的主导程度逐步减弱，ExG、VARI 和 NGRDI 等指数通常呈下降趋势；随着吐絮增加，高亮白色纤维暴露面积上升，局部分位数和高阶统计量的分布也会发生变化。因此，在缺少多光谱或高光谱载荷的条件下，基于 RGB 的可见光指数仍可作为脱叶率与吐絮率识别的有效先验。

=== 阈值分割

在拟议方案中，可基于指数图执行阈值分割以获得初始前景掩膜。Otsu 方法通过最大化类间方差自动选择阈值，在无需先验参数的条件下具有较好的泛用性@otsu1979threshold；随后可采用形态学开闭运算去噪与补洞，并结合连通域筛选保留主要棉株区域。该流程旨在降低背景像素占比，为后续特征统计提供更一致的输入对象，其有效性仍需通过分割质量和分类消融实验验证。

前景分割的重要性还在于它限定了后续特征的统计对象。若不进行 ROI 提取，指数均值、纹理统计和形态结构特征会同时受到土壤、地膜、滴灌带和阴影区域的影响，导致模型更容易学习到背景共现关系而非棉株成熟状态。将统计范围限制在前景区域，有助于提高特征与目标表型之间的一致性，并减轻背景噪声对判别边界的干扰。

此外，阈值分割方案的价值还体现在其工程可控性上。与复杂学习式分割模型相比，阈值方法虽然表达能力有限，但参数少、流程透明、对小样本依赖低，适合在后续可解释特征实验中承担“快速建立 ROI”的任务。对于成熟期棉花这类场景，评价重点应是 ROI 是否足以支持后续统计，而不是追求像素级边界的极致精度；其对分类性能和可解释性的实际贡献仍需通过对照实验确认。

=== 图像分割的其它方法

除颜色特征与阈值方法外，工程上还可结合区域生长、边缘约束或轻量学习式分割策略作为补充。方案优先考虑“指数 + 阈值 + 形态学”，主要因为其实现成本低、参数透明且便于开展消融分析。预期该流程能够降低背景颜色分布干扰，并为面积比例、白絮连通域和纹理统计等特征提取限定统计对象，但本文未将该预期表述为已经获得的分类性能收益。

若从方法谱系进一步展开，田间对象分割大致还可包括三类路线。第一类是基于聚类与区域一致性的无监督方法，如 k-means、模糊 C 均值和区域生长等，其优点是无需标注，但在复杂背景和光照波动下对初值及超参数较为敏感；第二类是基于图割、边缘约束与显著性先验的半自动方法，这类方法在目标与背景对比清晰时可获得较好边界，但在棉田场景中容易受到叶片阴影、滴灌带和地膜反光干扰；第三类是基于 U-Net、DeepLab 等深度网络的监督分割方法，其表达能力最强，能够在复杂背景中学习更稳健的语义边界，但需要额外构建像素级标注数据，训练成本和部署复杂度也明显提高。

从本研究任务定位出发，上述方法各有适用条件。若目标是构建高精度像素级分割系统，学习式分割更具潜力；若目标是为传统机器学习提供可解释 ROI，则更应强调流程简洁、参数透明和误差可追溯。基于这一考虑，后续结构化特征实验可优先验证“可见光指数 + 阈值分割 + 形态学修正”的组合，并将其与无分割的 RGB 像素向量基线进行公平比较。

== 结构化特征方案：棉花植株特征提取

在获得前景 ROI 后，可从光谱、形态与纹理三个层面构建候选特征。以下内容给出特征定义与农艺解释，不代表这些特征已经纳入本章定量实验。

=== 光谱指数特征

光谱指数与颜色统计特征可用于描述“绿叶减少”与“白絮暴露增加”的观测变化。考虑到单一均值对噪声与局部遮挡敏感，候选方案可在 ROI 内对指数图与颜色分量提取均值、方差、分位数、偏度和峰度等统计量。以 ExG、VARI 和 NGRDI 作为基础统计量来源，同时可构造白度相关特征（如高亮像素占比或高亮分位数）和叶绿相关特征（如绿色像素占比或绿色通道强度分布），分别服务于吐絮率和脱叶率分级。

其中，“白度相关特征”可通过阈值或分位数统计描述高亮像素占比，用于刻画棉絮外露程度；“叶绿相关特征”可通过绿色像素占比或绿色通道强度分布刻画冠层叶片覆盖程度。上述特征与生理表型存在清晰对应关系，有利于后续误差解释与跨地块对比分析。

相比单一均值，分位数、偏度和峰度等统计量有望更充分地刻画成熟期棉花图像中的分布异质性。例如，在部分区域已出现棉絮集中暴露而整体仍保留较多叶片的情况下，均值可能只表现为温和变化，而高亮分位数与分布偏度可能更敏感地表征“少量高亮目标开始出现”这一过渡状态。同理，对于脱叶率尚未充分提高但局部冠层已出现稀疏化的样本，绿色通道分布的尾部变化可作为候选描述量。因此，结构化特征方案可采用多统计量联合描述指数分布，并在后续实验中检验其对早期变化、过渡状态与空间不均匀性的表征能力。

=== 形态结构特征

形态结构特征可在颜色统计之外补充冠层结构与棉絮空间分布的几何信息。候选特征包括前景面积占比、白色连通域数量及平均面积、边界复杂度（周长/面积）和外接矩形长宽比，分别用于描述植株覆盖程度、棉絮团块尺度、轮廓破碎程度与 ROI 整体形态。

上述特征对“叶片减少”与“棉絮暴露增加”具有明确物理意义。

从机理角度看，形态特征弥补了颜色特征对“空间组织方式”描述不足的问题。成熟度变化并不仅仅体现为像素颜色比例的改变，还体现为冠层内部几何结构的重组：脱叶后冠层孔隙率增大，前景覆盖面积和边界复杂度会发生变化；吐絮后白色连通域数量及其尺度分布会改变，且这些变化通常具有一定的空间聚集性。因此，若仅依赖颜色统计，模型可能难以区分“亮度变化来自白絮增加”还是“亮度变化来自局部反光或背景穿透”；而引入连通域和轮廓特征后，这种判别将更具结构依据。

=== 纹理与统计特征

当背景被抑制后，冠层内部纹理、行间结构与棉絮团块分布会在纹理统计上体现差异。典型纹理特征包括：

灰度共生矩阵（GLCM）通过统计不同灰度在空间邻域的共现频率，能够描述冠层纹理的对比度、同质性与能量等特性@haralick1973glcm；局部二值模式（LBP）通过局部邻域二值编码刻画微纹理模式，具有较强的旋转与灰度鲁棒性@ojala2002lbp；此外，通道级统计量（均值、标准差、偏度、分位数等）可作为轻量补充，用于描述 ROI 内颜色与亮度分布的高阶形态。

纹理特征对“叶缘纹理减少”与“高亮纤维块出现”的结构变化敏感，可作为指数统计之外的补充信息源。

这类纹理特征对田间复杂背景尤其重要。成熟期棉株图像中，地膜、裸土和阴影区域常会引入与棉絮高亮区域相似的局部反差，单纯依赖颜色统计容易产生混淆。纹理描述子能够从灰度共生关系、局部模式重复性和方向性分布等角度刻画“棉絮块”“叶片纹理”“背景粗糙度”的差异，从而提高对相似颜色但不同结构区域的辨别能力。换言之，纹理特征并非简单附加维度，而是在颜色与形态之间提供了重要的中间层信息。

=== 当前基线与特征路线关系

当前工程基线以“RGB 像素向量 + 传统模型”为主，目标是快速验证任务可行性，但其对背景与光照变化敏感且缺乏显式语义约束。后续可将“光谱指数 + 形态 + 纹理”拼接为低维结构化特征，与当前基线进行公平对比，以检验结构化表征在稳定性、可解释性与跨场景鲁棒性方面是否具有实际收益；若经实验验证有效，还可进一步研究其作为轻量深度模型先验或辅助分支输入的可行性。

== 基于 RGB 像素向量的传统分类基线

本节基于当前实验记录（`results/experiment_results.csv`）开展分析。对于固定航点采集的 140 个样本，首先从每幅影像中心裁剪 $224 times 224$ 像素区域，再按固定顺序将 RGB 三通道像素值展开为长度为 $224 times 224 times 3 = 150528$ 的一维像素向量。该输入不包含前述 ROI、光谱指数、形态或纹理特征。对比模型包括 PCA + Logistic Regression（PCA+LR）、标准 Random Forest（RF）和类别均衡 Random Forest（Balanced RF）：PCA+LR用于验证降维后线性判别子空间的可行性，RF用于评估非线性集成分类器对 RGB 像素向量的拟合能力，Balanced RF用于考察类别均衡策略对长尾等级识别的影响。

现有记录对应一次完整对比实验（`target=de/ba`），并输出 `train/val/test` 三分结果。

=== 算法流程

整体流程包括：图像像素展平、PCA降维或随机森林输入准备、模型训练、验证集调参与测试集评估。考虑到脱叶率与吐絮率为多等级离散标签（等级划分见@chap:dataset），且类别占比通常呈长尾分布，本研究采用 Accuracy 与 Macro-F1 联合评估，以同时反映整体正确率与少数类识别能力：

其中，Macro-F1 为本研究优先关注指标。

这样设置的原因在于，本研究不仅关注多数样本的总体识别情况，也关注少数类和边界类的识别能力。对于处方图决策而言，成熟明显滞后或明显提前的区域通常更值得关注，因此 Macro-F1 比单纯的 Accuracy 更能反映模型对各等级的均衡判别能力。

为提高可复现实验的可比性，本研究报告一次固定切分实验结果；进一步的稳健性评估应采用多随机种子重复实验，并在跨地块/跨时段切分下报告均值与方差。

=== RGB 像素向量基线存在的问题

当前流程仍存在以下瓶颈：

一方面，RGB 像素向量缺乏显式语义约束，使得模型容易利用背景纹理等“捷径特征”而非成熟状态相关证据；另一方面，田间背景变化显著，前景分割与 ROI 统计尚未系统性融入主流程，导致输入分布不稳定并削弱跨地块可比性。此外，类别分布不均衡会诱导模型对多数类决策偏置，使 Accuracy 与 Macro-F1 出现显著背离；再叠加单次随机种子与单次切分评估，容易产生偶然结论。

上述问题说明，若希望仅依赖传统模型在复杂田间场景获得稳定表现，需要进一步将“前景分割 + 结构化特征 + 类别重加权/分层采样”等机制系统性纳入训练与评测流程，并在跨域测试上验证其收益。

这些问题并非传统模型所独有，而是脱叶率与吐絮率识别任务在复杂田间场景下的共性难点。RGB 像素向量的局限说明，低层观测难以充分支撑高层语义判别；类别不均衡导致的多数类偏置则说明，若缺少明确的任务约束和评测设计，模型容易形成与管理需求不一致的决策边界。因此，本研究对传统方法局限性的讨论，主要用于明确后续深度模型仍需解决的关键问题，包括背景抑制、边界等级表征和跨地块稳定性。

=== 双指标分类结果

脱叶率任务（`de`）结果如@tab:de-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.3864], [*0.1976*], [*0.4091*], [*0.3049*],
    [RandomForest], [0.3409], [0.0658], [0.3258], [0.1469],
    [RandomForestBalanced], [0.3636], [0.1897], [0.3106], [0.1305],
  )),
  caption: "传统机器学习模型的脱叶率等级分类结果。",
) <tab:de-res>

吐絮率任务（`ba`）结果如@tab:ba-res 所示。

#figure(
  three-line-table(table(
    columns: 5,
    [模型], [Val Acc], [Val Macro-F1], [Test Acc], [Test Macro-F1],
    [PCA+LogisticRegression], [0.4091], [*0.3312*], [*0.5000*], [*0.3989*],
    [RandomForest], [*0.4773*], [0.1077], [0.4242], [0.1105],
    [RandomForestBalanced], [*0.4773*], [0.1077], [0.4167], [0.0980],
  )),
  caption: "传统机器学习模型的吐絮率等级分类结果。",
) <tab:ba-res>

结果解读如下：

从 Macro-F1 指标看，PCA+LR 在 `de` 与 `ba` 两任务上均明显优于 RF 系列：脱叶率任务测试集 Macro-F1 达到 0.3049（@tab:de-res），吐絮率任务测试集 Macro-F1 达到 0.3989（@tab:ba-res），表明在当前基线输入下，“线性可分的低维判别子空间”更有利于少数类等级的稳定识别。与此相对，RF 在吐絮率任务验证集 Accuracy 达到 0.4773，但 Macro-F1 仅 0.1077（@tab:ba-res），显示模型明显偏向多数类；这类“Accuracy 高而 Macro-F1 低”的背离现象在长尾分布下具有代表性，说明仅依赖整体正确率会掩盖少数类失效。另一方面，三类模型训练集指标接近满分而测试集明显下降，也提示当前设置存在过拟合风险，特征表达与模型复杂度仍需进一步匹配。

这一结果说明，在当前特征组织方式下，PCA+LR 比 RF 系列更适合当前任务，表明有效信息可能主要集中在若干低维方向上，而不必依赖高复杂度树模型刻画高维交互关系。同时，RF 系列中 Accuracy 与 Macro-F1 的明显差异表明，树模型在长尾等级识别中存在较强的类别偏置，这对处方决策场景并不有利。

同时，吐絮率任务中 PCA+LR 的 Macro-F1 高于脱叶率任务，提示“白絮暴露”在当前 RGB 像素向量空间中可能更容易形成判别边界，而“叶片残留程度”更容易受到阴影、遮挡和背景穿透影响。由于本章未完成结构化特征对照实验，这一差异只能用于提出后续假设，不能归因于特定颜色、形态或纹理特征。

基于上述观察，后续改进应围绕“输入更结构化、训练更稳健、评测更可信”三条主线展开：在输入层面，将 ROI 分割、指数统计与形态/纹理特征系统性并入训练输入，减少背景噪声主导特征空间；在训练层面，对 LR 联合调节 `C` 与 `n_components` 并加强正则化，对 RF 约束树深度与叶节点样本数等复杂度参数，并结合分层抽样或类别重加权提升少数类梯度贡献；在评测层面，采用多随机种子重复实验并报告均值与标准差，同时引入跨地块/跨时段切分以验证泛化能力。

== 讨论

实验结果表明，在当前一次固定切分设置下，PCA+LR 在 Macro-F1 指标上优于 RF 系列，提示“线性可分的低维判别子空间”可能更符合当前基线输入的有效信息结构；而 RF 系列出现 Accuracy 与 Macro-F1 明显不一致的现象，则进一步说明类别不均衡会导致模型对多数类过拟合并忽视少数类边界。针对这一现象，后续工作可从三条路径改进：

其一，在输入表征上，应将 ROI 分割与结构化特征显式并入训练输入，避免背景噪声主导特征空间；其二，在训练策略上，应采用类别重加权、分层采样或代价敏感学习等机制提升少数类的梯度贡献；其三，在评测协议上，应在跨地块、跨年份与跨光照切分下评估，并报告多随机种子统计量，以避免单次切分带来的偶然结论。

从工程应用角度看，RGB 像素向量基线的主要价值在于快速暴露背景干扰、过拟合和类别偏置问题。前景面积、白絮连通域和绿度统计等结构化变量具有潜在解释价值，但需要在后续实验中与当前基线使用相同数据划分进行验证。

本章的意义还在于为后续章节提供问题分析视角。当深度模型在特定场景中出现误判时，可依据本章提出的结构化特征方案进一步设计叶绿统计、白絮连通域和背景纹理等诊断变量，但这些变量的诊断有效性仍需专门实验确认。

== 小结

本章以 RGB 像素向量构建了传统机器学习工程基线，并给出一次固定切分下 PCA+Logistic Regression、Random Forest 和 Balanced RF 的比较结果。PCA+Logistic Regression 在两项任务的测试集 Macro-F1 上高于 RF 系列，但结果仍受到类别不均衡、背景干扰和单次切分偶然性的影响。针对这些问题，本章分析了光谱指数、前景分割、形态和纹理等结构化特征方案；这些方案尚未纳入本章定量实验，后续需在统一训练与评测协议下验证其实际收益。

从全文技术链路看，当前实验为后续深度模型提供了 RGB 像素向量传统分类参照，并揭示了复杂背景、长尾等级和单次切分条件下的局限。结构化特征部分则形成待验证的可解释方案，为后续补充实验和误差诊断提供候选变量，而不承担本章定量性能结论。
