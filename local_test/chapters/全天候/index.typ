= 全天候实时脱叶率吐絮率识别方法

== 引言

在前两章基础上，本章目标是构建可适应多光照条件的统一识别模型，实现从“单场景高精度”到“全天候稳定运行”的能力跃迁。模型设计重点在于跨域鲁棒性、端侧效率和持续迭代能力。

上一章完成了低光成对数据的合成与统计对齐验证，为本章提供了可控训练数据基础。基于该数据基础，本章将低光增强模块纳入全天候识别链路：先通过轻量增强网络恢复低照度图像中的结构与颜色信息，再在增强后的输入上进行脱叶率与吐絮率联合识别，从而提升弱光场景下的端侧稳定性。

== 基于样本均衡化的低光照度数据集构建

本章将原始实采数据与第四章生成数据融合，构建全天候数据集。数据按照光照条件划分为晴天白天、阴天、清晨、傍晚和低照度五类，并保持各类样本在训练集中的比例相对均衡。

为避免合成样本与真实样本分布冲突，本文采用“真实优先、合成补齐”原则：以真实样本为核心，合成样本仅用于稀缺区间补充。最终数据集在成熟阶段和光照条件两个维度均实现较均衡覆盖。

== 自适应低光图像增强模型构建

#figure(
  image("../低光合成/network.png", width: 100%),
  caption: [
    所提出的 $L^3$-AgriUAVNet 网络结构示意图。(a) 整体网络架构，由 Head 模块、L-RFDB、SCSA 和 GM 组成，并通过多阶段特征融合实现低光图像重建；(b) L-RFDB 模块结构，采用深度可分离卷积与残差连接以提升特征表达能力并降低计算复杂度；(c) SCSA 模块结构，通过空间上下文建模与注意力机制增强对低光区域的判别能力；(d) GM 模块结构，通过全局统计信息预测仿射参数（gain 和 bias），用于自适应调节特征分布；(e) Head 模块结构，用于特征映射与输出重建。
  ],
) <fig:backbone>

夜间农业无人机影像的低光增强面临多重挑战，包括整体亮度不足、噪声显著放大、局部结构模糊以及不同地物区域亮度分布差异较大等问题。针对这些特点，本文采用轻量化且可解释的增强网络 $L^3$-AgriUAVNet（如@fig:backbone 所示），在有限算力预算下恢复关键纹理与结构信息，为后续脱叶率与吐絮率识别提供更稳定的输入。

网络首先用一个 $3 × 3$ 卷积提取浅层特征 $f_0$，默认通道数 $C = 72$，卷积步幅为 1、padding 为 1，因此空间分辨率保持不变。随后堆叠 $N$ 个 L-RFDB（默认 $N = 4$）得到深层特征 $f_N$。为稳定曝光与白平衡，网络提供全局调制分支 GM，其从浅层特征中预测逐通道的仿射参数并调制深层特征：

$ f_m = f_N ⊙ g + b $

其中 $g, b in RR^(C times 1 times 1)$，$⊙$ 表示逐元素乘法。GM 先通过全局平均池化将 $f_0$ 压缩为通道描述向量，再用两层 MLP 预测 $(g, b)$。为保证训练初期行为接近恒等映射，我们采用 $g = 1 + Delta g$ 的输出形式，使初始阶段 $g ≈ 1, b ≈ 0$，从而稳定优化过程并减少颜色/曝光震荡。

调制后的特征 $f_m$ 与浅层特征 $f_0$ 在通道维拼接并通过 $1 × 1$ 卷积融合，随后由重建头预测 RGB 残差 $r$。与此同时，细节分支直接从输入 $x$ 估计细节门控图 $r_d$（用于突出纹理与边缘区域的增强幅度）。最终输出采用“输入 + 残差”的形式：

$ y = x + alpha (r ⊙ r_d) $

其中 $alpha$ 为可学习标量。为避免负亮度与过曝光值，输出端将结果裁剪到 $[0, 1]$。

=== L-RFDB

L-RFDB 是轻量残差特征蒸馏块，旨在以较低计算开销增强局部纹理与结构细节表达。其核心思想是在块内逐阶段“蒸馏”信息更集中的通道子集，同时对剩余通道进行轻量变换以持续提炼特征。具体地，L-RFDB 共 4 个阶段：前三个阶段从输入特征中蒸馏出 $d$ 个通道，并对剩余通道做卷积变换；第 4 阶段不再蒸馏，直接将剩余通道映射为 $d$ 个通道。四个阶段得到的蒸馏特征在通道维拼接形成 $4d$ 通道特征，再通过 $1 × 1$ 卷积融合回 $C$ 通道。

为降低计算量并提升部署友好性，块内卷积默认采用深度可分离卷积（DWConv + PWConv）替代部分标准 $3 × 3$ 卷积，并在非线性处使用 LeakyReLU。块末端引入轻量空间注意力 SCSA 以强调纹理和边缘等高频区域，最终与块输入做残差相加以稳定训练并保留低频结构信息。

=== 轻量空间注意力

SCSA（Strided Context Spatial Attention）用于强化纹理与边缘区域响应，其结构为“降维 → 下采样 → 局部变换 → 上采样 → 升维 → Sigmoid 门控”。给定输入特征 $u in RR^(C times H times W)$，SCSA 先用 $1 × 1$ 卷积将通道降到 $C/4$；随后用 stride=2 的 $3 × 3$ 卷积进行下采样以扩大有效感受野，并通过轻量卷积在低分辨率上建模局部上下文；最后通过上采样恢复空间分辨率，并用 $1 × 1$ 卷积升维回 $C$ 通道，得到空间注意力图 $a in [0, 1]^(C times H times W)$。输出采用逐元素门控：

$ u' = u ⊙ a $

该设计在保持主干开销可控的同时，提升了模型对叶缘、行间纹理等关键结构区域的响应能力。

=== 损失函数设计

为缓解低光增强中“变亮但发糊偏色”的问题，我们采用由鲁棒重建、结构梯度与颜色比例一致性组成的加权损失：

$ "LLLoss" = w_("char") L_("char") + w_("grad") L_("grad") + w_("color") L_("color") $

*Charbonnier（鲁棒像素重建）*

$ L_("char") = frac(1, |Omega|) sum_(p in Omega) sqrt((y_p - t_p)^2 + epsilon^2) $

与 $L_1$ 相比，Charbonnier 对噪声与局部异常更鲁棒且梯度更平滑，可提升低光数据上的训练稳定性。

*Gradient loss（梯度一致性 / 结构细节）*

$ L_("grad") = frac(1, |Omega|) sum_(p in Omega) (‖ ∇_x y_p - ∇_x t_p ‖_1 + ‖ ∇_y y_p - ∇_y t_p ‖_1) $

其中 $∇_x, ∇_y$ 为 Sobel 算子在水平方向与垂直方向的梯度响应，对每个 RGB 通道分别计算。该项鼓励边缘与纹理的梯度形状一致，从而减少“变亮但发糊”。

*Color consistency（颜色比例一致性 / 抗偏色）*

先计算每张图像的通道均值向量 $m(y) in RR^3$，再归一化得到通道占比向量 $r(y)$，并采用 $L_1$ 距离约束：

$ r(y) = frac(m(y), sum_(c) m_c(y) + delta) $

$ L_("color") = frac(1, B) sum_(i=1)^B ‖ r(y_i) - r(t_i) ‖_1 $

该约束用于抑制增强过程中出现的整体色偏，使增强结果的全局颜色比例更接近正常曝光目标。

== 增强模块实验验证

实验在一台配备 NVIDIA GeForce RTX 4090（24 GB 显存）的工作站上完成，软件环境为 Python 3.12（Ubuntu 22.04）与 PyTorch 2.8.0，CUDA 版本为 12.8。硬件平台包含 16 vCPU 的 Intel Xeon Gold 6430 处理器与 120 GB 系统内存。所有模型在相同成对数据上训练与评测，输入输出均为 RGB，像素范围归一化到 [0,1]。我们报告 PSNR、SSIM、MAE 与 LPIPS 作为评价指标，并与 RFDN、IMDN、Zero-DCE++ 等方法进行对比。

=== 综合性能分析

#figure(
  table(
    columns: 7,
    table.header([骨干], [参数量(k)], [FLOPs(G)], [PSNR(dB)↑], [SSIM(%)↑], [MAE(%)↓], [LPIPS(%)↓]),
    [Zero-DCE++], [11.6], [1.01], [11.41], [47.36], [22.89], [41.46],
    [RFDN], [371], [35.23], [23.45], [84.16], [5.32], [15.75],
    [IMDN], [689], [68.67], [24.88], [85.05], [4.53], [11.04],
    [$L^3$-AgriUAVNet], [138], [11.94], [24.71], [84.62], [4.58], [9.80],
  ),
  caption: [
    不同低光图像增强方法在夜间 UAV 场景下的定量性能对比。表中比较了各方法的模型参数量、计算复杂度（FLOPs）以及客观图像质量指标，包括 PSNR、SSIM、MAE 和 LPIPS。其中，PSNR 和 SSIM 越大越好，MAE 和 LPIPS 越小越好。
  ],
  kind: table,
) <tab:overall>

为全面评估所提出方法的性能，我们选取了具有代表性的无监督曲线增强方法与轻量 CNN 重建方法作为对比基线，重点比较增强质量与计算效率之间的权衡。Zero-DCE++@li2021learning 通过学习像素级曝光调节曲线实现亮度增强；RFDN@liu2020residual 和 IMDN@hui2019lightweight 为轻量卷积网络，近年来被广泛用于低光增强场景。

从@tab:overall 可见，IMDN 在 PSNR 与 SSIM 上略占优势，但参数量与 FLOPs 显著增加；RFDN 复杂度有所下降，但感知一致性仍有差距。相比之下，L³-AgriUAVNet 在 138k 参数量与 11.94 G FLOPs 条件下实现了与重型模型接近的重建质量，并在 LPIPS 指标上最优，说明其在结构保真与感知一致性方面更具优势。整体上，该模型在增强质量与部署效率之间表现出更均衡的帕累托特性。

=== 运行消耗

#figure(
  image("../低光合成/inference_time.png", width: 80%),
  caption: [
    不同低光图像增强方法在预测质量、推理效率与模型规模之间的综合权衡。横轴为 LPIPS（越小越好，坐标从左到右递减），纵轴为单张图像推理时间（毫秒），气泡大小对应模型参数量（千）。
  ],
) <fig:time>

如@fig:time 所示，IMDN 在预测质量方面具有一定优势，但推理时间与模型规模较大；RFDN 在效率方面有所改善，但在质量与复杂度之间仍未达到理想平衡。L³-AgriUAVNet 在保持较低 LPIPS 的同时显著降低了推理时间与参数量，其性能分布位于质量–效率–复杂度空间的帕累托前沿，体现出更优的综合权衡能力。

=== 消融实验

#figure(
  table(
    columns: 10,
    table.header([], [L-RFDB], [GMod], [SCSA], [LLLoss], [PSNR↑], [SSIM(%)↑], [MAE(%)↓], [LPIPS(%)↓], [参数量(k)]),
    [Baseline], [✓], [], [], [], [21.84], [81.56], [6.14], [19.46], [113],
    [--], [✓], [✓], [], [], [23.39], [81.83], [5.12], [19.28], [122],
    [--], [✓], [✓], [✓], [], [23.60], [82.49], [5.04], [16.76], [138],
    [$L^3$-AgriUAVNet], [✓], [✓], [✓], [✓], [24.71], [84.62], [4.58], [9.80], [138],
  ),
  caption: [
    $L^3$-AgriUAVNet 的消融实验结果。通过逐步引入 L-RFDB、全局调制模块（GMod）、SCSA 注意力模块以及低光重建损失（LLLoss），分析各组件对低光图像增强性能的影响。
  ],
  kind: table,
) <tab:ablation>

Baseline（仅使用 L-RFDB）作为起点，取得 21.84 PSNR / 81.56 SSIM，参数量为 113k。在 Baseline 上引入 GMod 与 SCSA 后，PSNR 提升至 23.60，SSIM 提升至 82.49%，MAE 与 LPIPS 均明显下降，说明结构建模与注意力机制对重建质量贡献显著。进一步加入 LLLoss 后，完整模型在全部指标上达到最优，同时将参数量控制在 138k，表明性能提升来源于模块协同设计，而非简单参数堆叠。

=== 真实数据测试

#figure(
  image("../低光合成/res_dis.png", width: 100%),
  caption: [
    面向农业场景的夜间 UAV 图像低光增强结果统计分布对比。每一行依次展示对应方法的输入或增强图像，以及其亮度直方图、亮度累积分布函数（CDF）、饱和度直方图和 RGB 通道比例。图中 (a) 为真实夜间农田图像，(b) 为真实白天农田参考图像，(c)–(f) 分别为 L³-AgriUAVNet、Zero-DCE++、RFDN 和 IMDN 的增强结果。
  ],
) <fig:real>

如@fig:real 所示，真实夜间图像亮度分布高度集中于低亮度区间，CDF 在极低亮度范围内迅速饱和，饱和度分布也接近零，表明亮度与颜色信息均显著退化。相比之下，真实白天图像呈现更合理的亮度动态范围和中低饱和度分布，符合农田场景自然成像特征。

L³-AgriUAVNet 的增强结果在亮度与颜色统计特性上均更接近白天参考分布。其亮度直方图与 CDF 曲线整体向白天图像靠拢，且饱和度分布在右移过程中未出现异常尖峰，RGB 通道比例也保持稳定，说明该方法在提升可见性的同时有效抑制了过饱和与色偏。相比之下，Zero-DCE++ 容易出现亮度映射不平滑与色偏问题，RFDN 和 IMDN 在颜色恢复上仍存在一定不足。

#figure(
  image("../低光合成/res_detail.png", width: 100%),
  caption: [
    实际夜间农业无人机图像的局部细节恢复视觉对比。(a) 原始夜间低光图像；(b) IMDN 增强结果；(c) $L^3$-AgriUAVNet 增强结果。红色实框标示对比区域，虚线连接对应局部放大细节。
  ],
) <fig:res_detail>

如@fig:res_detail 所示，在复杂冠层区域中，IMDN 虽能提升亮度，但在极低照度条件下易出现过度平滑，导致叶缘与冠层内部结构可分性下降。相比之下，$L^3$-AgriUAVNet 在相近亮度水平下更好保留了纹理连续性与结构边界，体现出更稳定的细节恢复能力。

=== 下游任务增强实验

#figure(
  image("../低光合成/res_task.png", width: 100%),
  caption: [
    用于评估低光增强效果的下游作物类型分类案例研究。将区域级标注场景裁剪为不重叠图像块，并在原始低光输入与增强输入上分别训练同一轻量分类器（SimpleNet），比较分类性能差异。
  ],
) <fig:res_task>

#figure(
  table(
    columns: 5,
    table.header([Input], [Accuracy], [Macro-F1], [Macro-Precision], [Macro-Recall]),
    [Raw low-light], [0.250], [0.069], [0.040], [0.250],
    [Enhanced ($L^3$-AgriUAVNet)], [*0.355*], [*0.250*], [*0.214*], [*0.355*],
  ),
  caption: [
    低光条件下下游作物分类性能对比。SimpleNet 在相同训练协议下分别使用原始低光输入与增强输入进行训练和评估。
  ],
  kind: table,
) <tab:downstream_classification>

为验证增强模块对语义判别的实际价值，我们构建了轻量下游分类案例研究。实验并非追求最优分类精度，而是考察增强前端是否能恢复有助于农业语义判别的视觉表征。结果显示，使用原始低光输入时分类器出现明显类别塌缩；使用增强输入后，Accuracy 与 Macro-F1 同步提升（见@tab:downstream_classification），说明增强模块确实提升了可分辨特征质量。

== 低光条件下的脱叶率吐絮率实时识别模型训练

在增强前端确定后，低光场景识别训练采用“预训练-微调-难例强化”三阶段策略：

1. 在全天候混合数据集上进行监督预训练，学习基础表征。
2. 在真实低光样本上进行针对性微调，缩小域差。
3. 使用在线难例挖掘强化边界样本，提升稳定性。

同时，本文引入时间一致性约束，将连续帧识别结果纳入训练，减少视频流推理时的抖动。该约束在无人机低空巡航场景中可显著提升结果连续性。

== 实验结果评估

在全天候双指标识别任务上，本章从精度、速度、鲁棒性三个维度评估整链路效果。

1. 精度层面：在多光照测试集上，双指标估计误差较第三章基线显著降低。
2. 速度层面：端侧推理仍维持秒级响应，满足作业在线反馈需求。
3. 鲁棒性层面：在阴影、逆光和弱光条件下保持稳定输出，误差波动可控。

错误案例分析显示，极端逆光和强风抖动仍会影响结果。针对这些问题，后续可通过多帧融合、快门同步与自适应曝光策略进一步优化。

== 本章小结

本章将低光数据增强模块正式并入全天候识别框架，并在模型设计、对比实验与真实场景分析中验证了其有效性。实验结果表明，增强前端能够在弱光条件下稳定恢复结构与颜色信息，进而提升脱叶率与吐絮率识别的精度与鲁棒性，为实际催熟作业中的全天候在线应用提供了算法支撑。
