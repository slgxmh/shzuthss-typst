#import "tables.typ": *

== 引言

棉花是一种重要的经济作物和战略物资，除了作为纺织业的基本原料之外，还广泛应用于各种产品，
例如硝化纤维、火药棉纸等，它与全球众多国家的经济和生计有着显著的关联@jans2021global。
棉花收获需要大量的劳动力投入，在农业劳动力日益短缺的背景下，棉花种植的自动化、智能化已成为一种不可避免的趋势@lu2022cotton。
与小麦或玉米等确定的大田作物不同，棉花的成熟过程在生长条件允许的情况下无限期地持续下去，棉铃并不会统一成熟，所以化学脱叶和催熟是机械化棉花收获的基础@gwathmey2016cotton。
在农业实践中，通常采用喷洒收获助剂的方法，以尽快改善棉花植株的落叶，提高机械收获的作业效率，降低籽棉的杂质率，并促进棉铃的裂开@wang2023correlation。
在实践中选择合适的时间进行催熟作业是非常关键的，只有及时的落叶和成熟才能保证落叶的效果，并促进同化物从“源”转移到“储”，才能提高收获棉花的质量@xueqin202314effects @bange2010minimizing，并且，脱叶率与吐絮率是收获的关键标准@NYT3084-2017 @GBT45102，这就需要在田间快速准确监测棉铃吐絮率和脱叶率。

测量棉铃吐絮率与脱叶率最常用的方法是人工取样法，这意味着在不同点手动测量或选择生长均匀的代表性植物@tan2024estimating。
然而，这种方法获得的结果缺乏代表性、动态性，并且对田间棉花植物具有破坏性，这不利于对棉田整体棉铃率的准确估计。随着农业现代化的发展，传统的棉铃开口率判断方法无法满足棉花机械化的要求@ma2023monitoring。
近年来，随着图像识别技术的发展，学者基于此技术发表关于棉花表型识别研究。
例如，Yu等人使用了Sentinel-2卫星数据对棉花吐絮率进行了估测，提出了BARI和BORI指数，获得了预测（R2=0.675，RMSE=7.96%）和验证（R2=0.616，RMSE=2.79%）集中的准确估计结果@ren2020novel。
Wang等人提出了一种基于无人机多光谱数据监测棉花吐絮率的方法。通过分析植被指数（如NDVI和GNDVI）的变化率与地面吐絮率之间的关系，实现了对棉花吐絮率的快速、无损检测。研究结果显示，NDVI和GNDVI对吐絮率具有良好的预测能力，其中NDVI在训练集和验证集中的R²分别为0.912和0.929，rRMSE分别为15.387%和13.414%。
此外，随机森林模型在预测中表现出最高的准确性。准确预测棉花吐絮率可为收获和收获辅助喷洒时机的决策提供支持，并为作物生长监测和精准农业提供技术支撑。Ma等人的研究发现@wang2023monitoring，归一化植被指数（NDVI）和绿色归一化植被指数（GNDVI）与棉花吐絮率之间存在显著相关性。通过构建基于NDVI和GNDVI的随机森林模型，研究人员实现了对棉花吐絮率的准确预测。

目前现有的吐絮率或脱叶率的监测方法主要对无人机图像进行基于光谱特征进行监测，这些方法依赖于特征工程，例如典型分析和主成分分析尽管特征提取或选择为提高RSIC的准确性提供了有用的信息，但这些方法中的大多数对于全面表示给定应用的原始数据来说都是次优的@jia2013feature。
近年来，随着深度学习的发展，为我们提供了全新的解决方法。深度学习采用神经网络进行自主特征选择，消除了传统的人工特征工程。
通过组合低级信息来构建抽象的高级特征，从而提高图像识别和目标识别等任务的准确性和泛化性@elharrouss2024backbones。
当前，在智能农业领域中，深度学习已经取得了显著的成果，  ·例如，在病虫害检测任务中深度学习技术通过提取图像中的复杂的特征极大的提高了识别性能@jafar2024revolutionizing @upadhyay2025deep；
通过数据驱动的计算机视觉方案持续促进着温室农业的进步，用于检测植物生长、病虫害和确定农作物何时收获@akbar2024comprehensive；
同样的，基于计算机视觉的技术方案也应用于棉花表型监测任务中，例如使用Android移动程序收集农田作图像，在标记之后使用MobileNetV3对11种不同的棉花植物部分进行了分类，准确性、特异性和精确度分别为93.9%、96.12%和97.48%@stephen2024efficient。
通过结合卷积神经网络和双向长短期记忆（CNN-BiLSTM）来整合时空信息，次研究将疾病分为五个严重程度，在一年数据集上的评估准确率为89.7%，在两年数据上的准确率为72.7%@abdalla2024assessing。
以上这些方法说明了基于深度学习的机器视觉算法在棉花表型领域具有巨大的潜力。

然而，农业生产是一项成本敏感的活动，不适用于昂贵的设备和先进的现代GPU@arun2023effective。
并且用户的设备往往资源有限，并不能良好的运行现代全尺寸模型@albattah2022artificial。
所以许多研究者提出了轻量级模型来进行植物表型检测。例如，CDDLite-YOLO是基于YOLOv8构建的轻量级模型，用于棉花病虫害检测，仅有1.8M参数，具有3.6G FLOPS和222.22FPS的检测速度@pan2024lightweight。
为了准确检测棉田中的杂草，研究者开发了基于YOLO v5的超轻量级的棉花和杂草检测网络，提出了YOLO-WDNet网络，其参数量减少了82.3%，推理时间缩短了57.14%@fan2024yolo。
更进一步，实时，低成本的监测大面积的吐絮率与脱叶率是一个挑战。首先，虽然卫星遥感数据因其低成本且广泛的覆盖面而具有重要价值，但有限的空间分辨率和由数据的更新时间往往以天为单位@kganyago2024optical。

因此，基于之前的研究，本研究的目标是基于无人机RGB图像与机器视觉技术完成棉花脱叶率与吐絮率实时监测任务。
本研究的主要贡献如下：（1）在真实田间环境中，我们针对棉花种植全周期进行了数据采集，该数据集涵盖了棉花从生长到成熟再到采收的完整过程，确保了数据的时序连续性与代表性。
通过精准的人工标注，我们为数据集提供了高置信度的分类标签，以支持棉花脱叶率与吐絮率的智能检测研究。
（2）提出了一种轻量级识别网络RTCMNet（Real-Time Cutton Moniter Net），专为移动端部署优化，旨在实现棉花脱叶率与吐絮率的高效、实时检测。
RTCMNet采用高效的网络结构设计，在保证识别精度的同时显著降低计算复杂度，使其能够在资源受限的设备（如农业无人机、手持终端等）上高效运行。
（3）我们在真实田间环境采集的数据集上对RTCMNet及多个主流深度学习网络（如DenseNet121、MobileNetV2等）进行了系统性对比实验，评估了模型在棉花脱叶率与吐絮率检测任务中的性能。
实验结果表明，RTCMNet在保持较高识别精度的同时，显著降低了计算复杂度，并具备更优的特征提取能力。此外，我们采用t-SNE算法对不同网络的特征分布进行了可视化分析，进一步验证了RTCMNet在高维特征空间中的语义表达能力。整体实验结果证明了RTCMNet的可靠性和应用价值，为棉花田间管理的智能化提供了坚实的技术支撑。

#figure(
  image("overview.png"),
  caption: "棉花脱叶率与吐絮率识别任务工作流。数据收集：通过DJI Mavic 3M无人机对农田进行高分辨率图像采集。数据预构建：对原始数据进行专家标注，之后进行数据预处理，处理后的图像与专家标签一起构建数据集。模型构建：将数据集分为训练集和测试集，通过训练测试构建最佳模型结构。对比：通过不同模型对遥感图像进行测试与预测，并通过DJI遥控器进行结果度量与展示；实际验证：使用真实大田数据与田野调查结果比对，验证模型识别效果",
)

本文的其余部分组织如下。
@pdata 部分说明了描述了实验中使用的数据，数据收集与处理过程。
@pmethod 部分说明了相关概念和方法，和网络模型结构。
@pexp 部分包括实验设置和结果，并评估实验结果并将其与当前其他高级方法进行比较。
最后，@pdis 总结了这项研究并提出了未来工作的方向，总体的工作流程如图1所示。

== 方法 <pmethod>
=== RTCMNet结构

#figure(
  image("network.png"),
  caption: "RTCM网络结构总览。网络主要包含两个部分，特征提取和分类器。(a)特征提取部分由5个阶段组成，stam对输入图片进行最基本的特征提取，layer_1至layer_4对特征进行更深层次的提取。(b)Conv Block使用2d卷积和BatchNorm对进行特征提取。(c)多尺度卷积注意力机制兼顾了大尺度和细节特征，使用不同大小的卷积操作",
)<network>

使用复杂的算法模型可以显著的增加识别的性能。然而，这将导致高昂的训练和部署成本。
使用经典的纯卷积轻量模型可以降低模型的参数量并加快推理速度，但这将导致网络缺乏全局上下文信息，导致准确率降低。
集成Vision Transformer可以增强模型的全局理解能力，但是会减慢推理过程。
为了解决这个问题，本研究建立了一个轻量快速的网络结构RTCMNet，通过集成多尺度卷积注意力模块（MSCA Block）与双分类器用，实现实时识别大田棉花的脱叶率与吐絮率。
RTCMNet的网络结构由两个部分组成：特征提取模块和两个分类器。
特征提取模块，基于卷积注意力构建，分类器复用特征提取模块的输出，输出吐絮率和脱叶率的分类结果。
输出图片首先通过卷积模块对输入特征进行初步提取，然后结合多尺度卷积注意力机制实现多尺度语义信息的深度挖掘。
MSCABlock模块受到了SCTNet@xu2024sctnet 的启发，利用卷积计算降低了复杂度，同时确保语义信息的捕获能力。
经过特征提取和融合后，网络通过双分类器分别输出脱叶率和吐絮率的检测结果。
模型的设计充分考虑了在资源受限设备（如无人机平台）中的实际应用需求。
相比传统注意力机制@vaswani2017attention 的网络，RTCMNet在保持深度特征提取的基础上，大幅减少了参数数量和计算时间，适合实时任务。同时，MSCABlock的引入进一步提升了模型的特征提取效率，使其能够适应复杂的田间环境并快速响应。

=== 特征提取

RTCMNet的特征提取模块由五个部分组成(@network (a))，layer_1与layer_2由Conv Block组成（@network (b)）layer_3和layer_4结合了MSCA Block（@network d）构成，可以产生不同尺度的注意力特征。MSCA Block集成了多尺度卷积注意力（@network (c)）,并结合了Drop Path和MLP。
首先输入图像经过Stemt、layer_1和layer_2提取初步特征，之后经过卷积与MSCA的组合，从而增加模型在不同尺度上的语义信息与特征的获取能力。
最后，主干网络提取到的特征用于后续分类器完成脱叶率与吐絮率分类任务。

=== 多尺度卷积注意力

由于网络类型不同，CNN和Transformer提取的特征表示存在显著差异。直接对齐CNN和转换器之间的特征使学习过程变得困难，从而导致有限的性能改进。为了使CNN分支能够轻松学会如何从transformer分支中提取高质量的语义信息，我们设计了MSCA Block。
MSCA Block模拟了传统注意力机制，不仅可以学习来自卷积模块的特征，还降低了计算复杂程度。
MSCA模块的运行方式非常接近与经典的transformer编码器@vaswani2017attention，如图@network (c)所示，其基本运行方式描述如下：
$ f == N o r m ( x+ M S C A (x) ) $ <eq1>
$ y == N o r m (x + C o n v (x) ) $ <eq2>
其中，$N o r r m (·)$指batch normalization操作，$x,f,y$分别表示输入、隐藏特征和输出。
$C o n v (·)$至代表一次卷积操作，用于替代经典Transformer的位置编码操作。

MSCA接收输入特征图，经过一系列模块完成特征的增强和提取，如图@network (c)所示。
// Unlike standard self-attention mechanisms that explicitly decompose the input features into three separate projections—Query (Q), Key (K), and Value (V)—and compute the pairwise similarity via dot-product operations, our proposed Multi-Scale Convolutional Attention (MSCA) replaces the explicit Q/K/V branches with lightweight multi-scale separable convolutional kernels. In MSCA, the local convolutional kernels implicitly encode spatial dependencies and extract local context, while the subsequent softmax normalization across spatial dimensions simulates the effect of computing similarity weights in conventional self-attention. This design significantly reduces the computational overhead and parameter cost, while still capturing rich local structural information at multiple scales, which is particularly beneficial for complex field scenes such as cotton canopy recognition. Compared to standard window-based self-attention (e.g., Swin Transformer blocks), MSCA avoids costly matrix multiplications and relative position embeddings, making it more suitable for lightweight UAV-based phenotyping tasks where both local detail preservation and real-time inference are required.
与传统自注意力机制不同，标准自注意力通常需要将输入特征显式映射为 Query（Q）、Key（K）和 Value（V）三条分支，并通过点积计算各位置对的相似性权重。而本文提出的多尺度卷积注意力（MSCA）采用多尺度可分离卷积核代替了显式的 Q/K/V 映射，卷积核在局部空间上隐式建模邻域依赖，并通过空间维度的Softmax归一化模拟相似性分布。
这种设计显著降低了矩阵点积带来的计算开销和参数量，同时保留了多尺度局部上下文信息，对于复杂田间场景下如棉花冠层等结构化目标识别尤为有效。
与窗口式自注意力（如 Swin Transformer 块@liu2021swin）相比，MSCA 避免了大规模矩阵运算和相对位置编码，适合对实时性和轻量化有较高要求的无人机植物表型任务。

同时，由于实时的注意力机制应具有低延迟和强大的语义提取能力。我们认为卷积注意力（CA）@xu2024sctnet 由于传统注意力机制。我们的多尺度卷积注意力来自CA。
CA采用固定方向的可分离卷积（如 7×1 和 1×7），主要用于提取垂直和水平方向的局部依赖关系。这种设计对棉花冠层中行列结构较为规则的场景有效，
但在遇到形态复杂、不规则的冠层纹理时，感受野固定、尺度单一，局限性较大。
本文提出的多尺度卷积注意力（MSCA）在此基础上进行改进，引入多尺度可分离卷积分支（3×3和5×5），使模型能自适应捕获不同尺度的局部结构信息。
同时，MSCA 增加了可学习的输出映射与残差连接，支持模块的深层堆叠和稳定训练，而这在原始设计中是缺失的。
此外，MSCA 对卷积核采用了更合理的初始化与输入归一化，有助于加速收敛并提升泛化能力。
与原始卷积注意力相比，MSCA 在面对多尺度、非规则的棉花冠层场景时能提取更丰富的局部结构特征，同时保持较低的计算复杂度，适用于无人机场景下的实时表型监测。

此外，卷积提供了一种更灵活的方法来扩展外部参数，它简单地计算几个可学习向量和每个像素之间的相似性，然后根据相似性映射和可学习向量增强像素。
一方面，这将像素和可学习向量之间的相似性计算转换为具有可学习内核的像素块之间的相似性计算。
另一方面，具有可学习内核的卷积作在一定程度上保留了更多的局部空间信息。卷积注意力的作可以总结如下：
$ A t t e n t i o n (X) == sum_(s=1)^S s o f t m a x (c o n v_s ( b n (X))) $ <sqattn>
$ Y == W_o [ A t t e n t i o n (X) ] + W_r(X) $ <eqtmsca>

其中，$X ∈ R^(C_in times H times W )$表示输入特征图,$B N(dot)$表示批归一化（Batch Normalization），$C O N V_s (dot)$表示尺度为$s$的可分离卷积。
每个分支输出在空间维度上通过$S o f t m a x (dot)$ 实现实现局部归一化，从而模拟局部自注意力机制。
多个尺度分支的归一化输出在通道维度上相加后，通过$W_o$（即1×1卷积）映射到指定的输出通道维度。
为提升特征表达的一致性，MSCA采用残差连接，其中$W_r(dot)$表示输入到输出维度匹配的 1×1 卷积映射（若输入输出通道一致，则为恒等映射）。
该模块不仅在计算复杂度上显著低于标准自注意力机制，而且通过多尺度卷积核有效捕获冠层结构的空间多样性，适用于无人机低空冠层表型的实时识别任务。

=== 分类器设计

多分类网络（Multi-class Classification Network）是一种经典的深度学习网络，用于对输入数据进行类别预测。
它输出的是每个类别的概率分布，并选择概率最大的类别作为预测结果。
在此研究中，我们以自己提出的RTCMNet作为骨干结构，利用其提取到的特征，输入两个分类器，同时输出脱叶率与吐絮率的分类结果。
对于这两个任务我们采用了相同结构。首先，输入的图像经过骨干网络的stem和多个卷积模块（Conv Block），提取低级到高级的特征；
随后，特征在 layer3 模块中进一步提炼，经过一个包含卷积层与CFBlock的结构以融合多尺度信息。
最后，在CFBlock的输出基础上，特征被分别输入两个分类器（脱叶率分类器与吐絮率分类器）。
每个分类器包含一个平均池化层（Avg Pool），全连接层（Fc）、ReLU激活函数，以及输出层，最终生成对应的脱叶率和吐絮率分类结果。

== 数据 <pdata>

=== 研究区域

本次实验于新疆石河子北泉双泉集三分厂进行，如@data_area 所示。
该地区以其独特的气候和土壤条件成为研究棉花生长和脱叶剂效果的理想场所。
研究区属温带大陆性气候，年均降水量较低（125.9至207.毫米），水资源相对匮乏，同时昼夜温差显著。
这些气候特点对棉花的生长周期及脱叶剂的效果均有显著影响。
实验中选用的棉花品种为新陆早83号，这一品种因其适应性强、产量高而在当地广泛种植。
选择该品种的目的是在实际农业生产中评估脱叶剂的效果，从而提高研究结果的实际应用价值。
本次实验中使用的脱叶剂由噻苯敌草隆和乙烯利两种成分组成，具体用量为噻苯敌草隆15克/亩，乙烯利40克/亩。
这一配比在确保棉花产量和品质的基础上，有效促进了棉花叶片脱落，为后续的机械采摘提供便利。
实验所用的棉花田为当地农民自耕的农田，而非专门试验田。这种选择不仅模拟了实际农业生产环境，还使实验结果具有更高的现实意义和应用价值。

数据采集工作从2024年9月24日开始至10月4日结束，共进行26期数据收集，每期包括L1和L2两块试验田的可见光数据。在数据采集过程中，采用了大疆Mavic3多光谱版无人机。这款无人机专为农业应用设计，具有高精度遥感能力。
实验航线由大疆Pilot2软件自动生成， 如@fly_param 所示。研究时间覆盖了棉花生长的关键阶段，为实验提供了连续且全面的数据支持。


#figure(
  image("data_area.png"),
  caption: "试验区概况。(a)试验区位于石河子；(b)L1与L2两块实验田；(c)L2实验田的航线图与航线参数；(d)L1实验田的航线图与航线参数；(e)对棉花进行标记以采集数据；(f)催熟作业过程中的棉花；(g)达到采收标准的棉花。",
) <data_area>

#figure(
  (
    table(
      columns: 3,
      [参数名称], [参数值], [参数说明],
      [地面分辨率], [1cm/pixel], [手动指定分辨率],
      [航线高度], [27.3m], [由地面分辨率自动计算的航线高度],
      [航向重叠率], [60%], [平坦地面的验设置],
      [旁向重叠率], [70%], [平坦地面的经验设置],
      [飞行速度], [2.7m/s], [为了更好获得清晰的图像],
    )
  ),
  caption: "任务航线参数表",
) <fly_param>


我们采取人工调查的方式进行脱叶率与吐絮率的统计，为确保数据的代表性和准确性，在试验田内均匀布置了样本点。
样本点的设置遵循均匀的空间分布原则，以便全面反映试验田的整体情况。
在每个样本点，选取三柱棉花作为样本，记录绿叶数量、总棉铃数量和开放棉铃数量。
这些数据用于计算棉花的吐絮率和脱叶率，两项衡量棉花成熟度和脱叶剂效果的重要指标。
吐絮率通过开放棉铃数量与总棉铃数量的比值计算得出；脱叶率则通过绿叶数量的变化对比计算得出。如@eq_boll 与@eq_de 所示。

$ B o l l space O p e n i n g space R a t e (%) == N_(o b)/N_(t b) times 100 % $ <eq_boll>
$ D e f o l i a t i o n space R a t e (%) == (1 - N_(g l)/N_(o l)) times 100 % $ <eq_de>

其中，$N_(o b)$是本次统计时已经开放的棉铃数量，$N_(t b)$第一次统计时所有棉铃的数量 ，
$N_(g l)$为本次统计时残留的叶片数量，$N_(o l)$第一次统计时的叶片数量。

=== 数据预处理

在完成数据收集后，我们依据人工调查结果筛选无人机获取的图像，并为其提供相应的分类标签标注。为提高数据标注的效率与准确性，我们自主开发了一款无人机数据解析软件。
该软件主要用于读取MRK文件中的位置信息，实现图像与地理坐标的匹配，并辅助标注分类标签。
为了增强模型对复杂田间环境的适应性，同时确保结果的可靠性和泛化能力，我们对数据进行了全面的预处理和处理。
首先，由于无人机飞行航线固定，图像中棉花种植方向具有一致性，为增强模型对不同种植方向的识别能力，我们对原始数据进行了随机旋转和镜像操作。
此外，为提高模型对尺度变化的鲁棒性，对图像随机裁剪为224×224的固定大小。
其次，为减少数据集中的冗余信息，我们剔除了田块边缘的图像数据。
田块边缘通常套种其他作物（如玉米或树木），并且无人机施药覆盖不均，导致脱叶效果显著偏差，这些区域不具有代表性。此外，田间图像数据内的颜色差异较小，为避免特征分布过于单一，仅对数据进行了均值化处理，而未采用标准化操作。
最终，我们的软件将处理后的图像数据及其人工标注信息打包，构建为标准化数据集。根据田间调查结果，我们对脱叶率和吐絮率分别进行了精细化分类，其中脱叶率被划分为9个等级，吐絮率被划分为10个等级。

#figure(
  image("dataset_num.png"),
  caption: "成品数据集（图a为数据集例子，D为脱叶率标签，B为吐絮率标签；图b为数据分布情况，其中de_label为脱叶率标签，ba_label为吐絮率标签。标0表示0%-10%，标签1表示10%-20%，以此类推9表示80%-90%",
)<fig:dataset_num>

经过上述预处理后，得到的数据集如@fig:dataset_num 所示，训练集、验证集和测试集比例为5:1:4。其中，训练集用于调整模型参数，验证集用于优化超参数，测试集则用于评估模型的最终性能。
这种数据划分方法确保了模型能够在复杂田间环境中具有较好的泛化能力，并为后续实验提供了坚实的基础。

== 实验 <pexp>

在本研究中，我们将所提出的RTCMNet模型与多个广泛使用的深度学习架构进行了比较，包括DenseNet-121、MobileNetV2、ResNet-18、ShuffleNetV2和EfficientNet。本次比较的目标不仅是评估各模型的准确率，还关注它们在资源受限环境（如用于精准农业的移动设备和无人机）中的实时部署适用性。
在本次实验中，模型训练训练在Windows平台上完成，PyTorch版本为2.5.1，TorchVision版本为0.20.1，ONXX版本为1.17.0。移动端测试在大疆遥控器上完成，型号为DJI RC Pro Enterprise，其搭载了Android10系统。

=== 评价方案

为了全面评估每个模型的性能，我们使用了以下标准质量指标：准确率，精确率，召回率，以及F1分数。准确率指的是所有样本中被正确预测的实例所占的比例。精确率衡量的是模型做出的所有正向预测中，真正的正向预测占的比例。召回率，或称为敏感性，评估的是实际正样本中被正确识别的比例。F1分数是精确率和召回率的调和均值，作为一种平衡的度量标准来评估模型性能。每个指标从不同的角度反映了模型正确分类和区分类别的能力，从而确保了对模型性能的全面评估。

$ text("Accuracy") == frac(T N + T P, T N + T P + F N + F P) $ <eq_acc>
$ text("Precision") == frac(T P, T P + F P) $ <eq_prec>
$ text("Recall") == frac(T P, T P + F N) $ <eq_recall>
$ text("F1 Score") == frac(2 T P, 2 T P + F N + F P) $ <eq_f1>

其中，$T P$是真阳性，$T N$ 是真阴性，$F T$是假阳性，和$F N$是假阴性。

为了增强分析模型运行的资源消耗，我们引入了额外的指标纳入评估方案。这些指标包括参数数量、$text("GMacs")$、PC端推理时间和移动端推理时间，其中GMacs（Multiply Accumulate）是每秒执行的定点乘累加操作次数的缩写，用于评价模型的运算复杂度。
一次$text("MAC")$运算表示执行一次乘法与一次加法。在卷积神经网络（CNN）中，标准卷积层的 MACs 可通过以下公式计算：

$ text("MACs")== K × K × C_{i n} × H_{o u t} × W_{o u t} × C_{o u t} $ <dq_macs>

其中，$K$ 表示卷积核的尺寸，$C_{i n}$ 与 $C_{o u t}$ 分别为输入与输出通道数，$H_{o u t}$ 与 $W_{o u t}$ 为输出特征图的高与宽。
整模型的总乘加运算量可通过对所有层的 MACs 求和得到：

$ text("MACs")_text("total") == sum_{l=1}^{L} text("MACs")_l $ <sq_macs_all>

为方便对比与展示，通常将总 MACs 转换为 十亿次乘加运算（GMACs），其换算公式如下：

$ text("GMACs") == frac(text("MACs")_text("total"), 10^9) $

通过整合这些指标，我们进行了更全面的比较和理解每个模型的整体效率、实用性和在真实移动应用中的适用性。这种整体方法确保了对模型的全面评估，不仅考虑了它们的分类准确性，还考虑了它们在实际使用场景中的适用性和性能。

=== 对比实验

为了更好地验证网络的性能及其在实际棉花田脱叶率与吐絮率智能监测场景下的适用性，
本研究从多个维度评估了不同模型的综合性能，包括分类准确率、参数数量、计算复杂度（以GMACs作为指标）以及在大疆遥控器上的移动推理性能，用以全面衡量模型在资源受限设备上的部署潜力。

#figure(
  all_metrics,
  caption: "综合性能表",
)<allmetric>

我们首先测试不同模型在数据集上的性能
如@allmetric 所示，我们系统比较了多种典型骨干网络在棉花脱叶率与吐絮率分类任务上的综合性能。在经典模型中，DenseNet121取得了最高的分类准确率（0.94），F1值（0.94），Precision（0.95）与Recall（0.94），这得益于其密集连接结构有效增强了特征的复用与梯度流动，适用于需要高精度的场景。ResNet18同样表现良好，准确率达到0.87，Precision高达0.91，F1值与Recall均为0.88和0.87，表明其残差连接设计在一定程度上缓解了深层网络的退化问题。InceptionV3与ViT_S的表现略逊一筹，二者准确率分别为0.79和0.77，但仍可作为大规模场景下的参考基线。

在轻量级模型方面，LeViT128、EfficientNet与MobileNetV2均展示出较优的精度与高效性权衡。例如，EfficientNet在参数高效性设计下，准确率达到0.88，F1值与Precision分别为0.87与0.90，Recall为0.89，说明其复合缩放策略在压缩计算量的同时仍保留了较强的表征能力。MobileNetV2与ShuffleNetV2的准确率分别为0.87和0.92，其中ShuffleNetV2以其极简的网络结构实现了0.92的高准确率与0.93的Precision，在轻量化设计中表现突出。相较之下，MobileNetV3S的准确率仅为0.15，F1值为0.08，Precision为0.06，明显低于其他模型，表明其极度的结构简化削弱了对复杂特征的捕获能力。SqueezeNet的表现也相对较弱，准确率仅为0.74。

相比上述基线网络，本文提出的RTCMNet在该任务中展现出优异的综合性能。RTCMNet的准确率高达0.94，F1值同样达到0.94，Precision和Recall均为0.95与0.94，完全达到DenseNet121的水平，且与SCTNet（Accuracy 0.90，F1 0.90，Precision 0.91，Recall 0.90）相比仍具明显优势。与大规模网络不同，RTCMNet在设计中充分考虑了参数高效性与推理速度，结合局部卷积特征提取与注意力机制，有效捕获棉花冠层图像中的细粒度纹理信息，同时显著降低模型的计算开销，具备在无人机等资源受限设备上实时部署的可行性。因此，RTCMNet在保证高精度的同时，更适合在移动端与边缘计算平台进行高效部署，满足实际棉花农田作业对实时性与准确率的双重需求。

#figure(
  block(image("model_params.png")),
  caption: "模型参数量、性能与推理时间对比。（a）展示了模型的准确率，参数量与推理时间之间的关系。(b)为在大疆遥控器平台上的推理实验结果",
)<model_params>

如@model_params，本文提出的RTCMNet模型在高分类性能与高运算效率之间实现了良好的平衡。在完整规模模型中，DenseNet-121虽在准确率上表现最优，但其参数量超过20M，推理时间高达1084ms（如图6(b)所示），不适用于对时效性要求极高的无人机实时监测场景。相比之下，RTCMNet仅需0.35M的参数规模（约为DenseNet121的5%），即实现了与其相当的分类准确率，且在DJI遥控器实测环境中，RTCMNet的推理时延仅为32ms，远低于Densenet121。

与MobileNetV2相比，RTCMNet在参数量上进一步减小，且 GMACs 规模相当，二者的推理时间也接近（MobileNetV2: 62 ms，RTCMNet:32ms），但 RTCMNet 通过优化的轻量化卷积与特征提取结构，实现了更高的分类准确率，进一步验证了其高效的特征学习能力。同样，尽管MobileNetV3-Small 的参数量与RTCMNet接近，推理时延也较短，但其分类准确率仅为0.15，远低于RTCMNet，说明过度轻量化会牺牲特征表达能力，而RTCMNet则有效避免了这一弊端。
ShuffleNetV2与EfficientNet也表现出较好的准确率和较快的推理速度），但它们的参数量明显高于RTCMNet，ShuffleNetV2的参数量约为RTCMNet的两倍，EfficientNet则接近6M，这在计算资源受限的无人机等边缘设备中，会增加存储与部署成本，不利于灵活集成。
进一步地，如图 6(b) 的实测结果所示，RTCMNet在DJI遥控器平台的推理时间与SCTNet等其他轻量网络相比也具明显优势，证明了其在真实农业作业环境下的低延迟、高效率可行性。这一优势使得RTCMNet尤其适用于需要快速响应与持续监测的农业场景，如棉花关键生育期的脱叶率与吐絮率现场识别，为农机作业与田间管理提供实时决策依据。

综上所述，尽管 DenseNet121等大型模型在分类准确率上表现优异，但高昂的计算开销限制了其实时应用能力；而传统轻量网络（如 MobileNetV2、ShuffleNetV2）虽在推理速度方面表现优异，但往往牺牲了部分识别精度。本文提出的 RTCMNet 则在准确率、参数规模、计算复杂度和端侧推理时延等方面实现了全面优化，展现出在无人机棉花表型实时监测中的显著应用潜力。

=== 吐絮率分类实验结果

#figure(
  image("bo_res.png"),
  caption: "吐絮率实验分类结果",
)<boll_opeening_res>

在我们的研究中，采用了多种深度学习模型来评估其在棉花脱叶率与吐絮率分类任务中的性能。
这些模型的评估基于一系列常用的精度指标，包括准确率（Accuracy）、精确率（Precision）、召回率（Recall）和F1分数（F1-score）。
这些指标全面反映了各个模型在任务中的表现，突出了它们在不同任务中的优缺点。对于吐絮率分类任务，由于该任务的主要特征来源于颜色和纹理信息，而这些特征往往受到光照条件和背景复杂性的影响，因此吐絮率分类相较于脱叶率分类更加具有挑战性。
@boll_opeening_res 展示了在吐絮率检测测试集上不同的模型的性能指标，从图中可以看出，DenseNet121在所有评估指标中均表现最佳，其准确率、精确率、召回率和F1分数均为最高。
而我们提出的RTCMNet模型，在所有指标上均取得了优异的成绩，尤其是在精确率方面，表现出色，超过了大部分模型，仅次于DenseNet121。

在棉花脱叶率检测任务中，避免错误分类至关重要，因为不正确的使用脱叶剂可能会导致棉花质量的下降。
在这一任务中，我们的RTCMNet模型展示出了显著的优势，尤其是在精确率方面，明显高于所有其他模型，且仅次于DenseNet121这一全尺寸模型。在准确率、精确率和F1分数上，RTCMNet_b1均表现优于ResNet-18，这表明我们的模型在整体分类准确性以及综合性能上明显优于ResNet-18。
综上所述，尽管在性能上稍逊一筹，RTCMnet仍展示了极为出色的综合性能，特别是在精确率和F1分数方面的表现，使其在实际应用中成为一个非常有竞争力的选择。

=== 脱叶率分类实验结果

#figure(
  image("de_res.png"),
  caption: "脱叶率分类结果",
)<defaliation_res>

如@defaliation_res 所示，脱叶率分类实验的目的是评估模型在棉花脱叶率分类任务中的表现。本实验与脱叶率分类实验共享相同的特征图和数据预处理方法，并采用一致的评价指标，包括准确率（Accuracy）、精确率（Precision）、召回率（Recall）和F1分数。
实验结果表明，DenseNet121在所有模型中表现最佳，其准确率、精确率、召回率和F1分数均达到0.95，体现了该模型在复杂任务中的强大能力。需注意，ShuffleNetV2的性能相比出现了下降，ResNet-18紧随其后，其性能略低于ShuffleNetV2，但仍保持较高的分类精度。
轻量化模型中，ShuffleNetV2展现了较好的性能，准确率和F1分数均接近ResNet18，同时参数量远小于全尺寸模型，说明其在权衡模型复杂度与性能方面具有优势。
EfficientNet和SqueezeNet的表现略低于ShuffleNetV2，但仍在一定程度上满足了任务需求。相比之下，MobileNetV3Small的性能较差，其准确率仅为0.13。这可能是由于该模型设计过于精简，参数量和计算复杂度不足以支持吐絮率特征的充分学习。
同样，MobileNetV2在该任务中的表现也低于预期，其准确率仅为0.85，表明模型需要更多优化以适应复杂的田间场景。

值得注意的是，本文提出的RTCM模型在吐絮率分类任务中表现优异。尽管参数量较小，但RTCMNe在所有指标上都超越了DenseNet121，同时显著优于其他轻量化模型。这表明我们提出的MSCA Block在捕捉田间细节特征非常有效，RTCMNet模型在保持较低计算成本的同时，能够有效捕捉吐絮率特征，具备较强的实际应用价值。

=== 消融实验

#figure(
  (ablation_study),
  caption: "消融实验。列出了五种不同的网络配置及其对应的性能指标。每种配置由layer_nums、Attn类型和head_num三个参数决定。其中，layer_nums表示网络层数与模块数量的配置，Attn表示注意力机制的类型（CF或MSCA），head_num表示多头注意力机制的头数。",
)<ablation_study>

为了深入探究多尺度通道注意力机制（MSCA）在模型性能中的作用，我们进行了详细的消融实验。通过消融实验，我们验证了MSCA注意力机制的有效性，并探讨了不同网络配置对模型性能的影响。实验中，我们使用了Conv-Former Block@xu2024sctnet 替换了MSCA Block，并对网络的规模进行了调整，以考察不同网络配置对识别准确率、推理时间和计算复杂度的影响。@ablation_study 展示了不同配置下的实验结果。

具体来看，当layer_nums为1,1,1且使用CF注意力机制时，模型的准确率为0.89，推理时间为22毫秒，计算复杂度为0.37GMacs。随着head_num增加到16，准确率提升至0.90，但推理时间显著增加至205毫秒，计算复杂度也大幅上升至6.75GMacs。相比之下，当使用MSCA注意力机制时，模型的性能表现更为优异。在layer_nums为1,1,1的情况下，准确率达到了0.91，推理时间为29毫秒，计算复杂度为0.7GMacs。进一步增加layer_nums至1,2,2并保持head_num为8时，准确率进一步提升至0.94，推理时间为31毫秒，计算复杂度为0.9GMacs。当head_num增加至16时，准确率略微下降至0.92，但推理时间显著增加至113毫秒，计算复杂度为4.31GMacs。从实验结果可以看出，MSCA注意力机制在提高模型准确率方面表现出色，尤其是在较小的网络规模下。然而，随着网络规模和head_num的增加，虽然准确率有所提升，但推理时间和计算复杂度也显著增加。因此，在实际应用中，需要根据具体需求权衡模型的准确率、推理时间和计算资源的消耗，选择合适的网络配置。

== 讨论 <pdis>

=== 骨干网络特征分析

#figure(
  image("model_feature.png"),
  caption: "网络特征输出",
)<model_feature>

为了深入分析不同深度神经网络在棉花脱叶率与吐絮率检测任务中的特征提取能力，我们选取了部分骨干网络的特征分布进行了可视化分析。
具体而言，我们使用相同的数据输入已训练好的模型，并提取其骨干网络的输出特征。
随后，我们利用t-SNE（t-Distributed Stochastic Neighbor Embedding）算法对高维特征进行降维至三维，以便直观展示不同网络对棉花田间数据的语义表示能力。

从可视化结果来看，在经典卷积网络中，DenseNet121由于其密集连接结构，展现出较为紧密且均匀的特征分布，表明其对棉花田间图像的特征提取能力较强。相对应的MobileNetV3S学习到的特征在高维空间中分布稠密聚集，下游分类器难以区分稠密的特征，这可能是其分类性能低的原因。在基于Vision Trasfromer的网络中，LEViT同样提取到了均匀分布的特征，而VIT_S没有学习到有效的特征提取参数。这可以说明LEViT的性能表现比ViT更优秀的深层原因。

我们提出的RTCMNet表现出卓越的特征提取能力，其特征在高维空间中的分布更均匀，表明该网络能够更有效地学习和区分棉花田间不同的语义信息，进而可是使用更低的参数进行有效学习。
相对于SCTNet，其提取的特征在z轴上有更加均匀的分布，说明我们对ConvAttn的改进取得了显著的效果。
这一结果进一步验证了RTCMNet在棉花脱叶率与吐絮率检测任务中的潜力，为后续模型优化和部署提供了有力支持。

=== 实验验证

#figure(
  image("field.png"),
  caption: "大田实验结果。图片命名分为三个部分，为地块、日期与类别。其中地块为L1与L2两块实验田；日期指催熟作业后的天数；类别中，DE表示脱叶率识别结果、BA表示吐絮率识别结果。",
)<field>


为了验证模型在实际棉花生产中的应用效果，我们使用了从大田中收集的原始数据进行识别实验。
数据来源于无人机获取的原始图像，并在经过分块后输入到训练好的RTCMNet进行预测。
每个分块的预测结果依据经纬度进行可视化，从而得到识别图像。
为了全面评估模型的表现，我们选取了不同地块与不同时期的原始数据进行测试。如@field 所示，随着催熟日期的推进，脱叶率与吐絮率都有明显提升。

对于L1地块，结果显示其边缘区域的脱叶效果较差，尤其在D18日，边缘的脱叶率仍未达到采收标准。
这一现象与田野调查一致，可能是由于地块边缘的通风和光照条件较好，棉花生长较为旺盛，导致药剂分散在更多的叶片上，造成脱叶效果较差。
此外，L1地块的左上方由于滴灌系统的压力问题，播种密度较低，导致植株之间的生长空间较大。对于L2地块，下边缘的催熟效果较差，这与田野调查结果相符。
不同于L1地块，L2地块下边缘紧挨着一排树木，使用大型农业无人机进行催熟作业时，自动避障系统会保持与树木的安全距离，从而导致边缘区域药剂覆盖不足，影响脱叶效果。
与此同时，L2地块左上角空缺区域种植了经济树木，也造成了该区域脱叶效果不佳。
总体而言，RTCM模型的预测结果与田野调查的观察结果高度一致，进一步验证了该模型在实际棉花生产中的适用性和有效性。

=== 总结

本研究开发了一种名为 的RTCMNet（Real-Time Defoliation and Ball Opening Rate Detection Net）的轻量化深度学习模型，用于棉花脱叶率和吐絮率的实时检测。研究结果表明，RTCMNet在保持较低计算成本的同时，实现了与全尺寸模型（如 DenseNet-121）相当的性能，展现出其在资源受限环境中的卓越适用性。
与其他轻量化模型相比，RTCMNet在分类精度、推理速度和资源消耗之间取得了良好的平衡，为田间实时监测任务提供了一种高效可靠的解决方案。实验的主要贡献包括以下几个方面：

*数据集构建*：创建了一个涵盖真实田间环境的棉花脱叶率和吐絮率数据集。该数据集结合多人机图像和人工标注数据，为研究棉花脱叶和吐絮情况的监测技术提供了重要基础，同时具有较高的泛化能力和应用价值。

*模型设计*：提出了一种结合卷积神经网络与轻量化模块（CFBlock）的网络结构，在显著降低模型复杂度的同时，实现了深层语义特征的有效捕捉。RTCMNet模型的设计充分考虑了实际农业生产环境中的设备资源限制，能够以较少的参数量和计算时间完成高效推理。

*性能验证*：在实验中，RTCMNet模型在脱叶率和吐絮率分类任务中的表现显著优于其他轻量化模型，并获得了与DenseNet12全尺寸模型的相近性能。其在移动端的低延迟推理能力进一步证明了其在田间实时监测中的实际应用能力。

*实际应用能力*：通过使用真实大田生产数据测试与田野调查评估，RTCMNet可高效的识别地块的脱叶率与吐絮率，且与田野调查结果高度一致。这为精准农业中的棉花表型检测提供了重要技术支持，同时为其他农作物的田间监测任务提供了通用的技术路径。

尽管本研究在棉花实时监测领域取得了重要进展，但仍存在一些局限性。例如，数据集的区域性限制可能会影响模型在不同地理环境中的泛化能力；此外，模型在极端光照条件下的稳定性也需进一步研究。未来的工作可以从以下几个方面进行改进：一是扩展数据集覆盖范围，包括不同地区、不同品种的棉花数据；二是结合更多多模态数据（如热红外或高光谱图像）以提高模型的鲁棒性；三是优化轻量化模型结构，使其进一步提升性能的同时减少资源消耗。总体而言，本研究为精准农业中的棉花表型智能监测提供了一种高效、实用的解决方案。这项研究的成果不仅在棉花种植管理中具有重要意义，也为未来推动农业智能化发展提供了新的技术途径。
