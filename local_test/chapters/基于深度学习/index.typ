#import "tables.typ": *
#import "../../../template.typ": *

== 引言

第 3 章的传统机器学习基线表明，RGB 像素向量在复杂田间背景下的表征能力有限。本章在统一数据集与评测协议下，设计面向端侧部署的轻量级双指标联合识别模型 RTCMNet，并通过系统对比、任务分项评价、消融实验和实机测试分析其精度—效率权衡。本章其余部分安排如下：4.2 介绍模型方法；4.3 说明实验数据；4.4 给出训练协议与实验结果；4.5 讨论模型特征与适用范围；4.6 总结本章工作。

#figure(
  image("overview.png"),
  caption: "棉花脱叶率与吐絮率识别任务工作流。依次包括 UAV 影像采集、专家标注与数据预处理、模型训练与比较、端侧推理测试以及真实大田验证。",
)<fig:workflow>

== 方法 <pmethod>
=== RTCMNet结构

#figure(
  image("network.png"),
  caption: "RTCMNet 网络结构总览。（a）特征提取主干由 stem 和 layer_1 至 layer_4 组成；（b）Conv Block 使用二维卷积与批归一化提取特征；（c）多尺度卷积注意力通过不同尺度的卷积分支聚合局部与上下文信息；（d）MSCA Block 结合卷积注意力、Drop Path 和 MLP；最右侧两个结构相同的分类器分别输出脱叶率与吐絮率等级。",
)<network>

更复杂的模型通常能够提升识别性能，但也会增加训练与部署成本；纯卷积轻量模型虽然能够减少参数量并加快推理速度，但往往难以充分建模复杂田间场景中的多尺度上下文。基于这一考虑，本研究构建了轻量级网络 RTCMNet，通过引入多尺度卷积注意力模块（MSCA Block）和双分类头，实现对大田棉花脱叶率与吐絮率的实时识别。
RTCMNet 由特征提取模块和双任务分类头组成。输入图像首先经过卷积模块提取浅层特征，再结合多尺度卷积注意力机制增强不同尺度上的语义表达；随后，网络通过两个分类头分别输出脱叶率和吐絮率的分类结果。MSCA Block 受 SCTNet@xu2024sctnet 启发，采用卷积计算降低复杂度，同时保持对语义信息的建模能力。整体设计面向资源受限设备，兼顾识别性能与实时部署需求。

=== 特征提取

RTCMNet 的特征提取模块由 stem 和 layer_1 至 layer_4 五个阶段组成（@network (a)）。layer_1 与 layer_2 采用 Conv Block（@network (b)），layer_3 与 layer_4 引入 MSCA Block；MSCA Block 包含多尺度卷积注意力、Drop Path 和 MLP（@network (c)）。输入图像先经 stem、layer_1 和 layer_2 提取浅层与局部特征，再由后续卷积和 MSCA 模块整合多尺度信息。主干网络的输出供两个分类头分别预测脱叶率和吐絮率等级。

=== 多尺度卷积注意力

标准自注意力能够建立长程依赖，但其矩阵运算会增加高分辨率特征图上的计算开销。为适应端侧推理需求，本研究采用 MSCA Block，以多尺度卷积近似空间关系建模，在保留局部上下文的同时降低计算复杂度。
MSCA 模块的运行方式接近经典 Transformer 编码器@vaswani2017attention 的“残差 + 归一化”结构（如图@network (c) 所示），其基本形式可概括为：
$ f = op("BN")(x + op("MSCA")(x)) $ <eq1>
$ y = op("BN")(x + op("Conv")(x)) $ <eq2>
其中 $op("BN")(·)$ 表示 Batch Normalization，$x,f,y$ 分别表示输入、隐藏特征与输出；$op("Conv")(·)$ 表示卷积操作，用于以局部卷积替代经典 Transformer 的位置编码，从而在保持语义建模能力的同时降低计算开销。

MSCA 的结构如@network (c)所示。标准自注意力将输入显式映射为 Query（Q）、Key（K）和 Value（V），并通过点积计算位置间的相似性。MSCA 使用多尺度可分离卷积替代显式 Q/K/V 映射，在局部邻域内提取不同尺度的响应，再沿空间维度进行 Softmax 归一化。该设计避免了大规模矩阵点积，同时保留多尺度局部上下文。
与窗口式自注意力（如 Swin Transformer 块@liu2021swin）相比，MSCA 避免了大规模矩阵运算和相对位置编码，适合对实时性和轻量化有较高要求的无人机植物表型任务。

MSCA 的设计受卷积注意力（CA）@xu2024sctnet 启发。CA 通常采用固定方向的可分离卷积（如 7×1 与 1×7）建模水平与垂直方向的局部依赖关系，在行列结构较规则的场景中表现稳定；但在棉花冠层等纹理尺度多变、形态不规则的场景中，单尺度、固定方向的卷积核容易出现感受野与目标尺度不匹配的问题。本研究提出的 MSCA 在此基础上引入多尺度可分离卷积分支（如 3×3 与 5×5），并通过可学习的输出映射与残差连接支持深层堆叠，从而在保持低计算复杂度的前提下增强对多尺度结构的适应性，更契合无人机场景的实时表型监测需求。

从计算形式看，卷积核直接作用于局部像素块，可在估计空间响应的同时保留邻域结构。本研究将多尺度卷积注意力表示为：
$ A(X) = sum_(s=1)^S op("softmax")(op("Conv")_s(op("BN")(X))) $ <sqattn>
$ Y = W_o A(X) + W_r(X) $ <eqtmsca>

其中，$X in RR^(C_"in" times H times W)$ 表示输入特征图；$op("BN")(·)$ 为批归一化；$op("Conv")_s(·)$ 为第 $s$ 个尺度的可分离卷积分支；$op("softmax")(·)$ 在空间维度上进行局部归一化以近似相似性权重；$W_o$ 为输出映射（可实现为 $1 times 1$ 卷积），$W_r(·)$ 为残差分支的通道匹配映射（输入输出通道一致时可为恒等映射）。多个尺度分支的归一化输出在通道维度上聚合，从而形成兼顾细节与上下文的注意力增强表示。
该模块以较低计算开销聚合不同感受野的局部响应，适合用于无人机冠层影像的端侧识别。

=== 分类器设计

RTCMNet 通过共享骨干和两个任务分类头同时预测脱叶率与吐絮率。主干网络提取的特征经自适应平均池化后，分别输入两个结构相同的分类头。每个分类头由全连接层、ReLU 激活函数和输出层组成，输出相应任务的类别概率。

== 数据 <pdata>

本研究深度学习实验所用数据来自真实大田场景的无人机 RGB 影像与人工标注结果，覆盖不同地块与不同时相，并按脱叶率与吐絮率分级标签组织。数据采集、清洗、划分与标注规范已在数据集章节@chap:dataset 中统一给出；本研究重点在同一数据与评测协议下比较不同端侧模型的识别性能与部署代价。

与通用自然图像分类任务相比，本研究数据具有三个更具挑战性的特征。其一，田间背景复杂且与目标高度耦合，土壤、滴灌带、残膜、阴影以及杂草都会与棉株冠层共同出现在同一视野中，使模型难以仅凭局部纹理做出稳定判断；其二，脱叶率与吐絮率均属于成熟进程的阶段性表型，其视觉差异往往表现为连续变化而非截然分离的类别边界，容易在相邻等级间形成混淆；其三，真实生产数据天然存在长尾分布，临界等级和少数场景样本不足，导致模型更容易偏向多数类。正因如此，本研究不仅比较模型的整体准确性，也强调其在复杂背景、相邻等级和端侧约束下的稳定性。

此外，本研究采用双指标联合建模而非分别训练两个完全独立的网络，主要基于以下考虑：脱叶率与吐絮率虽然对应不同的表型现象，但二者在成熟期棉株图像中共享大量底层与中层视觉证据，包括冠层结构、颜色分布、棉絮暴露程度和枝叶遮挡关系等。通过共享骨干表征，可以在有限样本条件下提高特征利用效率，并减少端侧部署时的重复计算；再通过任务特异分类头分别建模两项指标的判别边界，从而兼顾共享表征与任务差异。这种“共享骨干 + 双任务输出”的设计，是本研究轻量化与高精度同时成立的重要前提。

== 实验 <pexp>

本研究将 RTCMNet 与 DenseNet-121、MobileNetV2、ResNet-18、ShuffleNetV2 和 EfficientNet 进行比较。评价内容包括识别准确率、模型规模、计算量和端侧推理速度。
在本次实验中，模型训练在 Windows 平台上完成，PyTorch 版本为 2.5.1，TorchVision 版本为 0.20.1，ONNX 版本为 1.17.0。移动端测试在大疆遥控器上完成，型号为 DJI RC Pro Enterprise，其搭载 Android 10 系统。

模型性能采用固定随机种子的五折交叉验证进行评价。全部样本在固定随机种子下随机打乱并划分为五个互斥子集；每轮使用其中一折作为测试集，其余四折作为训练集，不另设独立验证集，直至每一折均作为测试集一次。每一折分别计算脱叶率与吐絮率任务的 Accuracy、Macro-Precision、Macro-Recall 和 Macro-F1，再对两个任务的对应指标取算术平均。最终结果以五折双任务平均指标的均值 $plus.minus$ 标准差表示。训练过程中不使用测试折进行最优检查点选择或超参数调整。

与第 2 章的数据划分口径一致，本章采用目标作业域内的图像块级评价，重点比较不同网络结构的识别性能与部署代价。所有对比模型采用相同的折划分索引、输入数据、训练配置与评价指标，使模型间差异建立在一致的实验条件之上。

=== 训练协议

所有对比模型采用统一的双任务训练框架，输入为 $224 times 224$ 像素 RGB 图像。骨干网络输出的共享特征经自适应全局平均池化后，分别输入脱叶率与吐絮率分类头。两个分类头采用相同结构，均由 128 维全连接层、ReLU 激活函数和任务输出层组成，分别输出 9 类脱叶率等级和 10 类吐絮率等级。各对比骨干均使用相同的双分类头，以减少输出结构差异对模型比较的影响。

模型采用双任务联合优化。设脱叶率与吐絮率的交叉熵损失分别为 $cal(L)_"DE"$ 和 $cal(L)_"BA"$，总损失定义为两者的等权和：

$ cal(L)_"total" = cal(L)_"DE" + cal(L)_"BA". $

因此，两项任务的损失权重均为 1。每一折均使用 Adam 优化器从随机初始化开始训练，不加载 ImageNet 等外部数据集的预训练权重；初始学习率为 $1 times 10^(-3)$，批量大小为 32，不使用权重衰减和学习率调度器。Adam 的一阶与二阶矩估计系数采用 $beta_1 = 0.9$ 和 $beta_2 = 0.999$，数值稳定项为 $epsilon = 10^(-8)$。训练采用 bfloat16 混合精度，以降低显存占用并保持各模型的统一计算精度设置。除骨干结构外，各折的数据索引、输入尺寸、分类头、损失函数和优化参数均保持一致，以保证模型比较的公平性。

=== 评价方案

脱叶率与吐絮率分别为 9 类和 10 类单标签多分类任务，因此本研究基于混淆矩阵定义评价指标。设任务 $t in {"DE", "BA"}$ 的类别数为 $K_t$，混淆矩阵 $C^t$ 中的元素 $C_(i j)^t$ 表示真实类别为 $i$、预测类别为 $j$ 的样本数，则任务 $t$ 的准确率定义为

$ text("Accuracy")_t = frac(sum_(k=1)^(K_t) C_(k k)^t, sum_(i=1)^(K_t) sum_(j=1)^(K_t) C_(i j)^t). $ <eq_acc>

对第 $k$ 类，其精确率、召回率和 F1 分数分别定义为

$ P_(t,k) = frac(C_(k k)^t, sum_(i=1)^(K_t) C_(i k)^t), quad R_(t,k) = frac(C_(k k)^t, sum_(j=1)^(K_t) C_(k j)^t), $ <eq_prec>

$ "F1"_(t,k) = frac(2 P_(t,k) R_(t,k), P_(t,k) + R_(t,k)). $ <eq_f1>

为降低类别不均衡对总体指标的掩盖，本研究在每个任务内部对各类别等权平均：

$ text("Macro-Precision")_t = frac(1, K_t) sum_(k=1)^(K_t) P_(t,k), quad text("Macro-Recall")_t = frac(1, K_t) sum_(k=1)^(K_t) R_(t,k), $ <eq_recall>

$ text("Macro-F1")_t = frac(1, K_t) sum_(k=1)^(K_t) "F1"_(t,k). $

综合性能表中的 Accuracy、Precision、Recall 与 F1 均为脱叶率和吐絮率两项任务对应指标的算术平均。对于任一指标 $M$，双任务平均值定义为

$ M_("avg") = frac(M_("DE") + M_("BA"), 2). $

Precision、Recall 和 F1 均先在各任务内部按类别进行宏平均，再对两项任务取平均。由于多数样本集中在若干中间等级，Accuracy 可能掩盖模型对少数类和边界类的识别不足。因此，本研究同时报告 Macro-Precision、Macro-Recall 与 Macro-F1。

为了分析模型在端侧部署中的资源消耗，本研究将参数数量、计算复杂度（以 GMACs 计）以及 PC 端与移动端推理时间一并纳入评估。MACs（multiply–accumulate operations）表示一次前向推理中乘加运算的数量，GMACs 为其十亿量级表示，用于度量模型计算复杂度。在卷积神经网络（CNN）中，标准卷积层的 MACs 可通过以下公式计算：

$ text("MACs") = K times K times C_("in") times H_("out") times W_("out") times C_("out") $ <dq_macs>

其中，$K$ 表示卷积核的尺寸，$C_("in")$ 与 $C_("out")$ 分别为输入与输出通道数，$H_("out")$ 与 $W_("out")$ 为输出特征图的高与宽。
整模型的总乘加运算量可通过对所有层的 MACs 求和得到：

$ text("MACs")_("total") = sum_(l=1)^L text("MACs")_l $ <sq_macs_all>

为方便对比与展示，通常将总 MACs 转换为 十亿次乘加运算（GMACs），其换算公式如下：

$ text("GMACs") = frac(text("MACs")_("total"), 10^9) $

本研究联合使用分类指标、参数量、GMACs 和实机时延评价模型。分类指标反映识别效果，后三项分别衡量模型规模、理论计算量和实际部署效率。

本研究将端侧推理时间单独纳入评价，是因为农业无人机与移动终端场景中的“可用模型”必须同时满足两个条件：一是识别精度足以支撑双指标判断，二是推理时延不能破坏作业节拍。许多在 PC 端表现优秀的骨干网络，在移动平台上可能因内存访问模式、算子实现和模型规模等原因出现明显时延膨胀。因此，只有把分类指标、参数量、GMACs 与真实设备推理时间联合起来，才能对模型的工程可部署性做出有效判断。

=== 对比实验

本研究从分类性能、参数量、GMACs 和 DJI 遥控器实测时延四个方面比较不同模型，评价其在资源受限设备上的部署潜力。

#figure(
  {
    set text(size: 7.5pt)
    three-line-table(all_metrics)
  },
  caption: [不同模型在脱叶率与吐絮率双任务上的综合性能。Accuracy、F1、Precision 和 Recall 为每折两项任务对应指标的算术平均，表中报告五折交叉验证的均值 $plus.minus$ 标准差；参数量、MACs 和推理时间为确定性部署指标。],
)<allmetric>

本研究首先在统一的五折交叉验证协议下测试不同模型的识别性能。如@allmetric 所示，在经典模型中，DenseNet-121 取得最高的双任务平均 Accuracy（$0.94 plus.minus 0.01$）、Macro-F1（$0.94 plus.minus 0.01$）、Macro-Precision（$0.95 plus.minus 0.01$）和 Macro-Recall（$0.94 plus.minus 0.01$）。ResNet-18 的 Accuracy 为 $0.89 plus.minus 0.04$，Macro-Precision 为 $0.91 plus.minus 0.04$；InceptionV3 与 ViT-S 的 Accuracy 分别为 $0.79 plus.minus 0.04$ 和 $0.77 plus.minus 0.04$。

在轻量级模型方面，ShuffleNetV2 的 Accuracy 和 Macro-F1 均为 $0.92 plus.minus 0.02$，Macro-Precision 为 $0.93 plus.minus 0.02$，表现出较强的识别性能。EfficientNet 的 Accuracy 为 $0.88 plus.minus 0.04$，LeViT-128 为 $0.86 plus.minus 0.05$。MobileNetV2 与 MobileNetV3-Small 的 Accuracy 分别为 $0.74 plus.minus 0.01$ 和 $0.13 plus.minus 0.00$，表明不同轻量结构在当前任务上的表现差异较大；这些结果仅反映统一实验协议下的经验差异，不将单一模型的低分直接归因于轻量化程度。

RTCMNet 的 Accuracy、Macro-F1、Macro-Precision 和 Macro-Recall 均为 $0.93 plus.minus 0.02$，与性能最高的 DenseNet-121 接近，并高于当前实验中的 SCTNet（Accuracy 为 $0.86 plus.minus 0.13$）。最终采用的 RTCMNetB1 含 0.37 M 参数、0.19 GMACs，在保持较高双任务平均性能的同时显著降低了模型规模和计算开销，体现出面向资源受限设备的精度–效率权衡。

#figure(
  block(image("model_params.png")),
  caption: "模型参数量、识别性能与推理时间对比。（a）准确率、参数量与推理时间之间的关系；（b）DJI 遥控器平台上的推理时间。",
)<model_params>

如@model_params 所示，本研究构建的 RTCMNet 在分类性能与运算效率之间实现了较好的平衡。DenseNet-121 的参数量为 6.63 M，移动端推理时间为 1084 ms；最终采用的 RTCMNetB1 仅需 0.37 M 参数，约为 DenseNet-121 的 5.6%，双任务平均 Accuracy 为 $0.93 plus.minus 0.02$，与 DenseNet-121 的 $0.94 plus.minus 0.01$ 接近，且在 DJI 遥控器上的推理时延仅为 32 ms。

与 MobileNetV2 相比，RTCMNet 参数量更小、GMACs 更低，推理时间由 62 ms 降至 32 ms，双任务平均 Accuracy 则由 $0.74 plus.minus 0.01$ 提高至 $0.93 plus.minus 0.02$。MobileNetV3-Small 虽具有较低推理时延，但其 Accuracy 为 $0.13 plus.minus 0.00$；这一结果说明具体轻量结构与训练任务之间的适配性需要结合精度和部署指标共同评价。
ShuffleNetV2 与 EfficientNet 也表现出较好的准确率和较快的推理速度，但它们的参数量明显高于 RTCMNet：ShuffleNetV2 的参数量为 5.10 M，约为 RTCMNetB1（0.37 M）的 13.8 倍，EfficientNet 则约为 3.82 M。在计算资源受限的无人机等边缘设备中，更大的模型规模会增加存储与部署成本，不利于灵活集成。
如@model_params (b) 所示，RTCMNet 在 DJI 遥控器上的推理时延低于 SCTNet 等轻量网络，表明其能够满足当前设备上的低时延推理需求。该结果为脱叶率与吐絮率的现场快速识别提供了设备端依据。

DenseNet-121 的分类指标最高，但移动端时延也最大；部分轻量网络推理较快，但识别性能较低。RTCMNet 在准确率、参数量、计算量和端侧时延之间取得了较均衡的结果。

=== 脱叶率分类实验结果

#figure(
  image("de_res.png"),
  caption: "不同模型的脱叶率等级分类结果。",
)<defaliation_res>

@defaliation_res 给出了不同模型在脱叶率等级分类任务上的 Accuracy、Precision、Recall 和 F1；其数据处理和评价协议与吐絮率任务一致。
实验结果表明，DenseNet-121 在所有模型中表现最佳，其准确率、精确率、召回率和 F1 分数均达到 0.95，体现了该模型在复杂任务中的较强能力。需注意，ShuffleNetV2 的性能相比出现了下降，ResNet-18 紧随其后，其性能略低于 ShuffleNetV2，但仍保持较高的分类精度。
轻量化模型中，ShuffleNetV2 展现了较好的性能，准确率和 F1 分数均接近 ResNet-18，同时参数量远小于全尺寸模型，说明其在权衡模型复杂度与性能方面具有优势。
EfficientNet 和 SqueezeNet 的表现略低于 ShuffleNetV2，但仍在一定程度上满足了任务需求。相比之下，MobileNetV3-Small 的性能较差，其准确率仅为 0.13。这可能是由于该模型设计过于精简，参数量与计算复杂度不足以支撑复杂田间特征的充分学习。
同样，MobileNetV2在该任务中的表现也低于预期，其准确率仅为0.85，表明模型需要更多优化以适应复杂的田间场景。

RTCMNet 的综合指标优于多数轻量基线，而模型规模显著小于 DenseNet-121，体现了识别性能与部署效率之间的平衡。

脱叶率识别的主要难点在于，模型不仅需要识别叶片数量减少这一总体趋势，还需要区分由光照、视角、背景穿透或局部缺株造成的伪稀疏现象。RTCMNet 在该任务上的结果表明，多尺度卷积注意力设计有助于提高对田间细粒度表型特征的辨识能力。

=== 吐絮率分类实验结果

#figure(
  image("bo_res.png"),
  caption: "不同模型的吐絮率等级分类结果。",
)<boll_opeening_res>

@boll_opeening_res 给出了不同模型在吐絮率等级分类任务上的 Accuracy、Precision、Recall 和 F1。DenseNet-121 的四项指标最高；RTCMNet 的综合表现仅次于 DenseNet-121，并优于大部分轻量模型，其 Precision 优势较为明显。

吐絮率等级主要由白絮暴露比例、铃壳开裂状态和冠层遮挡关系等视觉线索共同决定，背景高亮区域和尺度变化可能造成干扰。RTCMNet 的结果与其多尺度特征设计相一致，但各模块的具体贡献仍以消融实验为依据。

较高的 Precision 有助于减少高吐絮等级的误报。在当前作业域内，这一特性可用于筛选接近采收条件的区域，并为后续田间复核提供候选位置。

=== 消融实验

#figure(
  three-line-table(ablation_study),
  caption: "RTCMNet 消融实验。比较不同层配置、注意力类型和注意力头数下的识别准确率、端侧推理时间与计算量。",
)<ablation_study>

为分析注意力类型和网络规模的影响，本研究以 Conv-Former Block@xu2024sctnet 替换 MSCA Block，并调整层数与头数。@ablation_study 比较了各配置的准确率、端侧时延和计算量。

采用 CF、层配置为 1,1,1、头数为 8 时，模型 Accuracy 为 0.89，端侧时延为 22 ms，计算量为 0.37 GMACs。将 CF 配置扩展至 1,2,2 和 16 头后，Accuracy 仅升至 0.90，时延和计算量则分别增至 205 ms 和 6.75 GMACs。采用 MSCA 后，1,1,1 配置的 Accuracy 为 0.91，时延为 29 ms，计算量为 0.16 GMACs；1,2,2 和 8 头配置的 Accuracy 进一步达到 0.942，时延为 31 ms，计算量为 0.19 GMACs。继续增加至 16 头后，Accuracy 降至 0.92，而时延和计算量分别增至 113 ms 和 0.68 GMACs。综合上述结果，最终选用 1,2,2 和 8 头的 RTCMNetB1。

消融结果显示，扩大层数和头数并未带来持续的准确率提升，却明显增加了端侧时延和计算量。RTCMNetB1 因而构成当前实验中较合适的精度—效率折中配置，与移动端部署目标一致。

== 讨论 <pdis>

=== 骨干网络特征分析

#figure(
  image("model_feature.png"),
  caption: "不同骨干网络的输出特征可视化。",
)<model_feature>

为比较不同骨干网络的特征分布，本研究向已训练模型输入相同样本，提取骨干输出，并使用 t-SNE（t-Distributed Stochastic Neighbor Embedding）将高维特征降至三维进行可视化。

从可视化结果来看，在经典卷积网络中，DenseNet-121 由于其密集连接结构，展现出较为紧密且均匀的特征分布，表明其对棉花田间图像的特征提取能力较强。相对应的 MobileNetV3-Small 学习到的特征在高维空间中分布稠密聚集，下游分类器难以区分稠密特征，这可能是其分类性能偏低的原因。在基于 Vision Transformer 的网络中，LeViT-128 同样提取到了更均匀分布的特征，而 ViT-S 未学习到有效的特征提取参数，这从特征空间角度解释了 LeViT-128 相较于 ViT-S 的性能优势。

RTCMNet 的特征分布相对均匀，与其较高的分类指标呈现一致趋势；部分低性能轻量模型则出现较明显的特征重叠。该可视化用于直观补充不同骨干的特征组织差异，各模块的性能贡献仍以消融实验结果为依据。
