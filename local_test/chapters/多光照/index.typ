#import "../../../template.typ": three-line-table
= 低光实时脱叶率吐絮率识别方法

== 引言

在前两章基础上，本研究进一步构建适用于多光照条件的统一识别模型，重点讨论跨域鲁棒性、端侧效率和系统可扩展性。

上一章完成了低光成对数据的合成与统计对齐验证（训练对生成流程见@fig:lowlight_pairgen），为本研究提供了可控训练数据基础。基于该数据基础，本研究将低光增强模块纳入全天候识别链路：先通过轻量增强网络在低色偏敏感的色彩空间中恢复低照度图像的结构与颜色信息，再在增强后的输入上进行脱叶率与吐絮率联合识别，从而提升弱光场景下的端侧稳定性。

全天候多光照数据集的融合与均衡规则见@chap:dataset，本章重点介绍增强模型结构、损失设计与整链路效果评估。

== 自适应低光图像增强模型构建

#figure(
  image("../低光合成/network.png", width: 100%),
  caption: [
    所提出的 $L^3$-AgriUAVNet 网络结构示意图。(a) 整体网络架构：输入图像先经 HVI 色彩空间变换映射至 HV 极坐标空间，由 RFDN-S 主干提取深层特征，GMod 全局调制分支预测仿射参数并调制深层特征，再经特征融合与重建后通过逆 HVI 变换输出 RGB 图像；(b) RFDB 残差特征蒸馏块结构，通过逐阶段通道蒸馏在低开销下提炼特征；(c) ESA 增强空间注意力模块结构，通过降采样建模大感受野空间注意力图以强化纹理与边缘响应；(d) GMod 全局调制模块结构，利用全局平均池化与 MLP 预测逐通道仿射参数增量。[图待更新为期刊版]
  ],
) <fig:backbone>

夜间农业无人机影像的低光增强面临多重挑战，包括整体亮度不足、噪声显著放大、局部结构模糊以及不同地物区域亮度分布差异较大等问题。针对这些特点，本研究采用轻量化且可解释的增强网络 $L^3$-AgriUAVNet（A Lightweight Framework for Low-Light UAV-Based Agricultural Monitoring，如 @fig:backbone 所示），在有限算力预算下恢复关键纹理与结构信息，为后续脱叶率与吐絮率识别提供更稳定的输入。

网络输入首先经过 HVI 色彩空间变换（Horizontal/Vertical-Intensity，引自 CIDNet@yan2025cidnet）：RGB 图像被映射到由色相与强度构成的 HV 极坐标表示，其中色相 $h = "atan2"(v_v, v_h) / (2 pi)$，$v$ 为可学习的强度分量。该变换采用可微的 HSV–RGB 往返映射实现，使增强过程在低色偏敏感的空间中进行，从而抑制 RGB 空间中直接回归易产生的颜色伪影；增强结果经逆变换重新回到 RGB 空间。

色彩空间变换后的表示先经一个 $3 times 3$ 卷积（head）提取浅层特征 $F_0$，其通道数与主干宽度一致；随后 $F_0$ 被送入特征提取主干。主干采用 RFDN 的小型化版本 RFDN-S：深度为 $N = 2$ 个 RFDB（残差特征蒸馏块），宽度 $C = 48$，蒸馏率为 0.25，即每个块蒸馏出 $d = 12$ 个通道，剩余 $R = 36$ 个通道继续参与提炼；块末端接入 ESA（Enhanced Spatial Attention）以增强空间判别能力。此外，本研究另设基于深度可分离卷积（DWConv）的变体 $L^3$-AgriUAVNet-DW（0.07 M 参数），供算力极端受限的平台选用。

为稳定曝光与白平衡，网络设置全局调制分支 GMod：先通过全局平均池化（GAP）将浅层特征 $F_0$ 压缩为通道描述向量，再经 MLP（48 → 64 → 96）预测逐通道仿射参数增量 $Delta g, b$，对深层特征进行调制：

$ F = F_("deep") dot.circle (1 + Delta g) + b $

其中 $dot.circle$ 表示逐元素乘法。输出采用 $1 + Delta g$ 的恒等初始化形式，使训练初期 $g approx 1, b approx 0$，从而稳定优化过程并减少颜色与曝光震荡。最后，HVI 变换分支将浅层特征 $F_0$ 与调制后的深层特征融合并重建，经逆 HVI 变换输出增强后的 RGB 图像。

=== HVI 色彩空间变换

低光增强在 RGB 空间中直接进行时，亮度与颜色高度耦合，容易产生色偏与亮度伪影。CIDNet@yan2025cidnet 提出的 HVI（Horizontal/Vertical-Intensity）色彩空间将 HSV 的色相平面极坐标化：色相由水平/垂直两个分量表示，其极坐标形式为 $h = "atan2"(v_v, v_h) / (2 pi)$，这使红色区域在坐标上保持较小距离，从而消除 HSV 中红色坐标断裂引入的红色伪影；强度分量 $v$ 引入可学习的压缩函数，将低光区域压缩以抑制黑色噪声伪影。HVI 与 RGB 之间的往返映射完全可微，可直接嵌入网络前端与输出端。本研究将增强主干的主要计算置于 HVI 空间中进行，使亮度恢复与颜色校正解耦，增强结果再经逆变换映射回 RGB 供下游识别使用。

=== RFDN-S 主干与 ESA

主干采用 RFDN@liu2020residual 的小型化配置 RFDN-S，以较低计算开销增强局部纹理与结构细节表达。RFDB 的核心思想是在块内逐阶段蒸馏信息更集中的通道子集，同时对剩余通道进行轻量变换以持续提炼特征。具体地，在蒸馏率 0.25 下，每个 RFDB 逐阶段从输入特征中蒸馏出 $d = 12$ 个通道，剩余 $R = 36$ 个通道经卷积变换继续参与下一阶段提炼；各阶段蒸馏得到的特征在通道维拼接后经 $1 times 1$ 卷积融合回 $C = 48$ 通道，并与块输入做残差相加以稳定训练。

每个 RFDB 末端接入 ESA（Enhanced Spatial Attention）模块@liu2020residual。ESA 先用 $1 times 1$ 卷积降低通道数，再通过步幅卷积与池化操作缩小空间尺寸以扩大有效感受野，在低分辨率特征上进行轻量卷积建模空间上下文，最后上采样恢复分辨率并升维，经 Sigmoid 得到空间注意力图，对块输出进行逐元素门控。该设计在保持主干开销可控的同时，提升了模型对叶缘、行间纹理等关键结构区域的响应能力。对于算力极端受限的平台，本研究还提供将标准卷积替换为深度可分离卷积的 DWConv 变体 $L^3$-AgriUAVNet-DW，参数量仅为 0.07 M。

=== 全局调制模块

夜间 UAV 图像的退化不仅表现为局部暗区细节丢失，还表现为不同图像批次之间整体亮度与白平衡基线的波动，仅依靠局部卷积难以稳定建模这种全局分布偏移。为此，本研究设置全局调制模块 GMod，使网络能够根据输入图像的整体照度状态自适应调整特征幅值。GMod 先通过全局平均池化将浅层特征 $F_0$ 压缩为通道描述向量，再由两层 MLP（48 → 64 → 96）预测逐通道仿射参数增量 $(Delta g, b)$，并以 $F = F_("deep") dot.circle (1 + Delta g) + b$ 的形式调制深层特征。其中 $1 + Delta g$ 的输出形式保证了恒等初始化（$g approx 1, b approx 0$），使训练初期网络行为接近恒等映射，从而稳定优化过程并减少全局曝光漂移与白平衡波动引起的颜色震荡。

=== 损失函数设计

为缓解低光增强中“变亮但发糊偏色”的问题，本研究采用由鲁棒重建、结构梯度与颜色比例一致性组成的加权损失：

$ "LLLoss" = w_("char") L_("char") + w_("grad") L_("grad") + w_("color") L_("color") $

本研究取 $w_("char") = 1.0$、$w_("grad") = 0.2$、$w_("color") = 0.05$，Charbonnier 中 $epsilon = 10^(-6)$。

*Charbonnier（鲁棒像素重建）*

$ L_("char") = frac(1, |Omega|) sum_(p in Omega) sqrt((y_p - t_p)^2 + epsilon^2) $

与 $L_1$ 相比，Charbonnier 对噪声与局部异常更鲁棒且梯度更平滑，可提升低光数据上的训练稳定性。

*Gradient loss（梯度一致性 / 结构细节）*

$ L_("grad") = frac(1, |Omega|) sum_(p in Omega) (‖ ∇_x y_p - ∇_x t_p ‖_1 + ‖ ∇_y y_p - ∇_y t_p ‖_1) $

其中 $∇_x, ∇_y$ 为 Sobel 算子在水平方向与垂直方向的梯度响应，对每个 RGB 通道分别计算。该项鼓励边缘与纹理的梯度形状一致，从而减少“变亮但发糊”。

*Color consistency（颜色比例一致性 / 抗偏色）*

先计算每张图像的通道均值向量 $m(y) in RR^3$，再归一化得到通道占比向量 $r(y)$，并采用 $L_1$ 距离约束：

$ r(y) = frac(m(y), sum_(c) m_c(y) + delta) $

$ L_("color") = frac(1, B) sum_(i=1)^B ‖ r(y_i) - r(t_i) ‖_1 $

该约束用于抑制增强过程中出现的整体色偏，使增强结果的全局颜色比例更接近正常曝光目标。

== 增强模块实验验证

=== 实验设置

模型训练在一台配备 NVIDIA GeForce RTX 4090（24 GB 显存）的工作站上完成，推理与端侧部署测试在 RTX 5090（AutoDL 平台）上进行，软件环境为 Python 3.12（Ubuntu 22.04）与 PyTorch 2.8.0，CUDA 版本为 12.8。所有模型在相同成对数据上训练与评测，输入输出均为 RGB，像素范围归一化到 [0,1]；训练 patch 尺寸为 $224 times 224$，随机种子固定为 42。优化器采用 Adam，初始学习率为 $10^(-4)$；batch size 按各模型显存占用设定：Restormer 为 4，LLFormer、Retinexformer、Uformer 与 $L^3$-AgriUAVNet 为 16，HVI-CIDNet 为 32，RFDN 与 SCI 为 64；其中 HVI-CIDNet 与 RFDN 训练 100 epoch，其余模型训练 200 epoch，并采用验证集早停策略。测试集包含 3265 个 patch；真实夜间外部验证集包含 308 个 patch，源自 26 张真实夜间 UAV 图像。低光成对数据的构建流程与统计对齐验证见第 5 章，本章不再赘述。本研究报告 PSNR、SSIM、MAE 与 LPIPS 作为评价指标。

=== 综合性能分析

#figure(
  three-line-table(table(
    columns: 7,
    table.header([方法], [参数量(M)], [MACs(G)], [PSNR(dB)↑], [SSIM↑], [MAE↓], [LPIPS↓]),
    [LLFormer], [5.807], [15.507], [26.54], [0.888], [0.0441], [0.103],
    [Restormer], [25.872], [71.820], [26.16], [0.866], [0.0452], [0.105],
    [Retinexformer], [2.028], [15.747], [26.10], [0.891], [0.0450], [0.125],
    [Uformer], [6.192], [18.475], [24.66], [0.817], [0.0522], [0.222],
    [HVI-CIDNet], [1.976], [6.227], [24.30], [0.795], [0.0533], [0.252],
    [RFDN], [0.371], [17.593], [22.53], [0.800], [0.0649], [0.360],
    [SCI], [0.022], [1.493], [21.86], [0.773], [0.0731], [0.289],
    [$L^3$-AgriUAVNet], [0.139], [6.252], [26.24], [0.893], [0.0452], [0.119],
  )),
  caption: [
    不同低光图像增强方法在夜间 UAV 场景下的定量性能对比。表中比较了各方法的模型参数量、乘加运算量（MACs）以及客观图像质量指标，包括 PSNR、SSIM、MAE 和 LPIPS。其中，PSNR 和 SSIM 越大越好，MAE 和 LPIPS 越小越好。
  ],
  kind: table,
) <tab:overall>

为全面评估所提出方法的性能，本研究将对比基线由“轻量方法之间”的比较升级为覆盖重型 Transformer 与轻量方法的“跨复杂度全谱系”比较。其中，LLFormer@wang2023llformer、Restormer@zamir2022restormer、Retinexformer@cai2023retinexformer 与 Uformer@wang2022uformer 为具有代表性的 Transformer 图像重建方法；HVI-CIDNet@yan2025cidnet 为基于 HVI 色彩空间的低光增强网络；RFDN@liu2020residual 为轻量残差特征蒸馏网络；SCI@ma2022sci 为基于自标定光照（Self-Calibrated Illumination）的轻量增强方法。

从@tab:overall 可见，$L^3$-AgriUAVNet 的 SSIM 为 0.893，为全表最优；PSNR 为 26.24 dB，较 Restormer 高 0.09 dB（基于未舍入原始结果，paired bootstrap 95% CI [0.047, 0.130]，Holm 校正 $p = 0.0105$），较 LLFormer 低 0.30 dB，总体处于重型 Transformer 同档水平。与此同时，其参数量仅为 0.139 M，为 LLFormer 的 1/42、Retinexformer 的 1/15、RFDN 的 1/2.7；MACs 为 6.252 G，约为 LLFormer、Retinexformer 与 RFDN 的 40%、40% 与 36%。这表明该方法在远小于主流基线的计算预算下，取得了与重型 Transformer 相当的重建精度。

进一步从指标含义看，PSNR 和 SSIM 更多反映像素重建精度与整体结构相似性，而 LPIPS 更关注深层感知空间中的语义和纹理一致性。对于农业 UAV 低光增强任务而言，仅依赖 PSNR 提升并不足以保证下游识别收益，因为如果模型通过过度平滑换取更高像素一致性，叶缘、铃壳裂缝、棉絮边界和行间纹理等与表型判别直接相关的局部证据反而可能被削弱。$L^3$-AgriUAVNet 在 SSIM 最优的同时将 LPIPS 控制在 0.119，与最优方法（LLFormer 0.103、Restormer 0.105）处于同一水平，表明该网络更倾向于保留对下游语义判别有价值的结构性细节，而不是简单地执行亮度拉伸或纹理均值化处理。这一点与后续下游幼苗检测实验中 mAP 的提升结果相互印证。

从工程部署角度看，表 @tab:overall 反映的是增强质量、模型复杂度与端侧可用性之间的综合权衡。夜间无人机在线作业要求增强模块不能显著挤占后续脱叶率与吐絮率识别及空间聚合的推理预算，因此本研究更关注模型在整体链路中的可部署性。综合来看，$L^3$-AgriUAVNet 在定量重建、感知一致性和模型规模之间具有较好的平衡。

=== 运行消耗

#figure(
  image("../低光合成/inference_time.png", width: 80%),
  caption: [
    不同低光图像增强方法在预测质量、推理效率与模型规模之间的综合权衡。横轴为 LPIPS（越小越好，坐标从左到右递减），纵轴为单张图像推理时间（毫秒），气泡大小对应模型参数量。[图待更新为期刊版]
  ],
) <fig:time>

如@fig:time 所示，重型 Transformer 方法（如 Restormer）在重建质量方面具有一定优势，但推理时间与模型规模较大；轻量方法 SCI 效率最高，但重建质量明显落后。$L^3$-AgriUAVNet 在保持较低 LPIPS 的同时显著降低了推理时间与参数量，其性能分布位于质量–效率–复杂度空间的帕累托前沿，体现出更优的综合权衡能力。

面向真实端侧部署，本研究进一步将各方法导出为 ONNX 模型并在平板设备上实测。$L^3$-AgriUAVNet 的 ONNX 导出体积仅为 0.79 MiB，明显小于 LLFormer（22.62 MiB）、Retinexformer（8.30 MiB）与 RFDN（1.45 MiB）。在 iPad Air 上（ONNXRuntime Web 1.23.2 + WebGPU，$224 times 224$ 输入），$L^3$-AgriUAVNet 单张推理耗时 202 ms（4.95 FPS），较 LLFormer（569 ms / 1.76 FPS）、RFDN（1281 ms / 0.78 FPS）与 Retinexformer（2712 ms / 0.37 FPS）分别快 2.8 倍、6.3 倍与 13.4 倍，说明该方法在无独立 GPU 的端侧平台上即可满足近实时增强需求。

需要指出的是，低光增强模块的运行消耗不能孤立理解，而应放在“增强 + 识别 + 地理映射”的整链路预算中评估。在实际作业中，若增强模块占用过多推理时间，将直接压缩后续双指标识别、空间聚合与处方更新的时间窗口。基于这一约束，$L^3$-AgriUAVNet 将主要计算资源集中在亮度恢复、纹理保留和颜色稳定上，从而更适配端侧部署需求。

此外，气泡图所展示的“参数量较小但感知质量较优”还意味着模型结构具有较好的可压缩性与平台迁移潜力。对于无人机遥控器、嵌入式边缘盒子或轻量 GPU 平台而言，模型规模不仅影响显存占用，还会影响加载时延、缓存命中率以及长时间运行下的热稳定性。因此，小参数量并非单纯的存储优势，而是与稳定实时性、低功耗和系统可靠性直接相关。综合质量与效率分布可以看出，本研究方法更适合作为全天候识别链路中的前端模块，而非仅作为离线图像修复工具。

=== 消融实验

#figure(
  three-line-table(table(
    columns: 3,
    table.header([变体], [PSNR(dB)↑], [ΔPSNR(dB)]),
    [$L^3$-AgriUAVNet（完整）], [26.24], [0],
    [w/o GMod], [23.63], [-2.62],
    [w/o HVI], [24.52], [-1.73],
    [w/o ESA], [25.59], [-0.65],
    [w/ DWConv 变体], [25.70], [-0.54],
    [depth = 1], [24.48], [-1.76],
    [depth = 3], [25.70], [-0.54],
    [depth = 4], [24.43], [-1.81],
    [width = 32], [25.54], [-0.70],
    [width = 64], [24.66], [-1.58],
  )),
  caption: [
    $L^3$-AgriUAVNet 的消融实验结果。以完整模型（PSNR 26.24 dB）为基准，逐项移除或替换关键组件，并扫描主干深度与宽度配置，分析各设计对低光图像增强性能的影响。ΔPSNR 基于未舍入原始结果计算。
  ],
  kind: table,
) <tab:ablation>

从@tab:ablation 可见，以完整模型（PSNR 26.24 dB）为基准，各组件的移除均导致性能下降，其中 GMod 的贡献最大（-2.62 dB）。从模块作用机理上看，GMod 的主要收益在于缓解低光场景中常见的全局曝光漂移与通道间颜色失衡问题。由于夜间 UAV 图像的退化不仅表现为局部暗区细节丢失，还表现为不同图像批次之间整体亮度和白平衡基线的波动，仅依靠局部卷积很难稳定建模这种全局分布偏移。GMod 通过引入全局统计调制，使网络能够根据输入图像的整体照度状态自适应调整特征幅值，因此在移除该模块后性能下降最为显著，说明网络对亮度与颜色基线的恢复高度依赖全局调制。

HVI 色彩空间变换的贡献次之（-1.73 dB），验证了在低色偏敏感空间中进行增强对抑制颜色伪影与亮度伪影的作用。ESA 模块的贡献为 -0.65 dB，其收益集中体现在局部结构恢复上：夜间农田图像中的叶缘、铃壳边界、行间纹理和棉絮轮廓通常同时面临低对比度与噪声污染，若没有显式的空间注意力机制，网络往往会优先拟合大面积低频区域，从而牺牲对高频细节的表达。将标准卷积替换为深度可分离卷积的 DWConv 变体性能下降 0.54 dB，但参数量由 0.139 M 降至 0.07 M，可作为算力极端受限平台的备选方案。

深度与宽度扫描表明，depth = 2 / width = 48 为精度–复杂度最优点：减浅至 depth = 1 或加深至 depth = 4 均导致性能明显下降（分别下降 1.76 dB 与 1.81 dB），加深至 depth = 3 亦无收益（下降 0.54 dB）；收窄至 width = 32 下降 0.70 dB，加宽至 width = 64 反而下降 1.58 dB。这说明在当前数据规模与任务复杂度下，过深或过宽的主干会引入优化困难与过拟合风险，性能提升来源于模块协同设计与合理的复杂度配置，而非简单参数堆叠。

=== 真实数据测试

#figure(
  image("../低光合成/res_dis.png", width: 100%),
  caption: [
    面向农业场景的夜间 UAV 图像低光增强结果统计分布对比。每一行依次展示对应方法的输入或增强图像，以及其亮度直方图、亮度累积分布函数（CDF）、饱和度直方图和 RGB 通道比例。图中 (a) 为真实夜间农田图像，(b) 为真实白天农田参考图像，(c)–(f) 分别为 L³-AgriUAVNet、LLFormer、Retinexformer 和 RFDN 的增强结果。[图待更新为期刊版]
  ],
) <fig:real>

如@fig:real 所示，真实夜间图像亮度分布高度集中于低亮度区间，CDF 在极低亮度范围内迅速饱和，饱和度分布也接近零，表明亮度与颜色信息均显著退化。相比之下，真实白天图像呈现更合理的亮度动态范围和中低饱和度分布，符合农田场景自然成像特征。

L³-AgriUAVNet 的增强结果在亮度与颜色统计特性上均更接近白天参考分布。其亮度直方图与 CDF 曲线整体向白天图像靠拢，且饱和度分布在右移过程中未出现异常尖峰，RGB 通道比例也保持稳定，说明该方法在提升可见性的同时有效抑制了过饱和与色偏。相比之下，LLFormer 与 Retinexformer 在真实夜间数据上易出现局部过曝光，RFDN 在颜色恢复上仍存在一定不足。

在 308 个真实夜间 patch 上的无参考定量评测进一步验证了上述观察。暗像素比例方面，原始输入为 23.74%，所有增强方法均将其降至 0；RGB 通道残差由原始输入的 0.0346 降至本方法的 0.0233，而 RFDN 高达 0.0611，为各方法中最差，说明其存在明显的通道比例失衡。结构与清晰度指标方面，Tenengrad 由 33.16 提升至 102.87，边缘密度由 0.0792 提升至 0.2990，局部对比度由 6.48 提升至 19.89，表明增强显著恢复了边缘与纹理信息。自然度指标方面，NIQE 由原始输入的 8.15 变为本方法的 8.43，BRISQUE 由 52.21 改善至 48.58，说明增强在大幅提升可见性的同时基本维持了图像的自然统计特性，未引入明显的感知失真。

这种差异对农业场景具有实际影响。夜间农田图像中的可判别信息往往集中在冠层表面、行间阴影、棉絮反射和叶片边缘等局部区域之间的亮度与颜色对比上。若增强结果出现通道比例失衡或局部过曝光，模型就更容易将白絮高亮区域与噪声放大的亮斑混淆，或将叶片阴影误判为表型差异。统计分布更接近白天参考，有利于增强图像与下游识别模型在正常曝光域中形成更一致的判别条件。

#figure(
  image("../低光合成/res_detail.png", width: 100%),
  caption: [
    实际夜间农业无人机图像的局部细节恢复视觉对比。(a) 原始夜间低光图像；(b) RFDN 增强结果；(c) $L^3$-AgriUAVNet 增强结果。红色实框标示对比区域，虚线连接对应局部放大细节。[图待更新为期刊版]
  ],
) <fig:res_detail>

如@fig:res_detail 所示，在复杂冠层区域中，RFDN 虽能提升亮度，但在极低照度条件下易出现过度平滑与色偏，导致叶缘与冠层内部结构可分性下降。相比之下，$L^3$-AgriUAVNet 在相近亮度水平下更好保留了纹理连续性与结构边界，体现出更稳定的细节恢复能力。

真实夜间场景中的困难样本通常同时叠加弱纹理、噪声放大、局部模糊和非均匀照明等多种退化因素。对于这类样本，若增强模型仅执行整体曝光提升，往往会同步放大噪声、反光和模糊边界。$L^3$-AgriUAVNet 在局部细节对比中的结果表明，其恢复策略能够较好地保持难例区域的结构可分辨性，这对后续脱叶率与吐絮率等级识别尤为关键。

=== 下游任务增强实验

#figure(
  image("../低光合成/res_task.png", width: 100%),
  caption: [
    用于评估低光增强效果的下游幼苗检测案例研究。将夜间 UAV 影像裁剪为不重叠图像块，并在原始低光输入与各方法增强输入上分别训练同一轻量检测器（YOLOv8n），比较幼苗检测性能差异。[图待更新为期刊版]
  ],
) <fig:res_task>

#figure(
  three-line-table(table(
    columns: 4,
    table.header([输入], [mAP\@0.5], [Precision], [Recall]),
    [Raw low-light], [0.1393], [0.2984], [0.2257],
    [LLFormer 增强], [0.0601], [--], [--],
    [RFDN 增强], [0.1646], [--], [--],
    [$L^3$-AgriUAVNet 增强], [0.1596], [0.3139], [0.2065],
  )),
  caption: [
    低光条件下下游幼苗检测性能对比。YOLOv8n 在相同训练协议下分别使用原始低光输入与各方法增强输入进行训练和评估；LLFormer 与 RFDN 增强输入的 Precision 与 Recall 未单独报告，以“--”标注。
  ],
  kind: table,
) <tab:downstream>

为验证增强模块对语义判别的实际价值，本研究构建了下游幼苗检测案例研究。实验采用 YOLOv8n@jocher2023yolov8 作为端侧轻量检测器：使用 LabelMe 对夜间 UAV 影像中的幼苗目标进行标注，输入尺寸为 $512 times 512$，训练 75 epoch，并在 139 个测试 patch 上评估。实验并非追求最优检测精度，而是考察增强前端是否能恢复有助于农业语义判别的视觉表征。

结果显示（见@tab:downstream），使用原始低光输入时 mAP\@0.5 仅为 0.1393；经 $L^3$-AgriUAVNet 增强后提升至 0.1596，相对提升 14.6%，Precision 与 Recall 分别为 0.3139 与 0.2065，说明增强模块确实提升了可分辨特征质量。RFDN 增强输入的 mAP\@0.5 绝对值略高（0.1646），但其参数量与 MACs 分别为本方法的 2.7 倍与 2.8 倍，端侧部署代价显著更高。值得注意的是，LLFormer 增强后 mAP\@0.5 反而降至 0.0601，表明增强质量与下游收益并非单调相关——在合成数据上取得更高 PSNR 的方法可能因过度平滑丢失幼苗目标的微弱纹理证据，反而损害检测性能。

进一步地，本研究在更强的 YOLOv8s 上重复了上述对比，结果显示增强前端不再带来收益（Raw 输入 mAP\@0.5 为 0.3574，各方法增强后处于 0.3328–0.3382 之间）。这说明增强前端对强检测器的边际效用有限，其收益集中于轻量端侧模型——这一发现与本研究面向端侧部署的定位一致：在算力受限的无人机端侧链路上，轻量增强前端与轻量检测器的组合能够以最小代价换取稳定的下游收益。

== 低光条件下的脱叶率吐絮率实时识别模型训练

在增强前端确定后，低光场景识别训练采用“预训练—微调—难例强化”的三阶段策略。首先在全天候混合数据集上进行监督预训练，以学习对作物冠层与背景差异更具普适性的基础表征；随后在真实低光样本上进行针对性微调，通过缩小域差使模型参数更贴近夜间成像分布；最后引入在线难例挖掘强化边界样本，以提升在低对比度与噪声放大条件下的判别稳定性。与此同时，本研究在训练中加入时间一致性约束，将连续帧的识别结果纳入优化目标，以抑制视频流推理的时序抖动；该约束在无人机低空巡航的连续观测场景中尤为关键。

上述三阶段训练策略的核心目的，是避免模型在低光场景中出现“两类失配”：一类是语义失配，即模型将正常曝光域中学到的判别规则直接迁移到夜间输入，导致对亮度与颜色漂移过于敏感；另一类是优化失配，即若直接用有限夜间样本从头训练，模型容易被噪声和长尾类别主导，难以形成稳定的判别边界。通过“先学习通用表征、再贴近目标域、最后强化困难样本”的渐进式训练路径，可以在保持基础语义稳定性的同时，有针对性地压缩昼夜域差，并增强对弱纹理、模糊边界和低对比度样本的适应能力。这种训练范式本质上对应了农业端侧模型从实验室数据走向真实复杂环境时所必须经历的域适配过程。

== 实验结果评估

在全天候双指标识别任务上，本研究从精度、速度与鲁棒性三个维度评估整链路效果。精度方面，在多光照测试集上，整链路的双指标估计误差相较第三章基线显著降低，说明增强前端能够有效恢复有利于下游判别的结构与颜色线索；速度方面，端侧推理仍维持秒级响应，能够满足作业在线反馈的节拍需求；鲁棒性方面，在阴影、逆光与弱光条件下均可保持相对稳定的输出，误差波动处于可控范围。错误案例分析显示，极端逆光与强风抖动仍会显著扰动输入分布并放大几何与成像噪声，后续可通过多帧融合、快门同步与自适应曝光策略进一步提升稳定性。

从系统视角看，本研究结果表明“增强前端 + 轻量识别模型”的分层架构适用于全天候农业场景。与由单一识别网络直接处理全部光照变化相比，前端增强模块先在输入层稳定亮度、颜色和局部结构，再由下游网络执行双指标识别，更便于分别优化、部署和诊断。

实验结果同时表明，全天候鲁棒性来源于数据构建、模型结构、损失设计和训练策略的共同作用。若缺少低光合成数据的分布对齐，增强模块难以获得稳定监督；若缺少轻量注意力与全局调制，模型难以同时处理局部细节恢复与全局曝光漂移；若缺少后续真实低光微调与难例强化，识别模型仍可能在域外场景中出现明显波动。

当然，错误案例也提示了当前方案的适用边界。当场景同时出现极端逆光、强反射、快速运动模糊和局部遮挡时，增强模块与识别模块之间的误差仍可能累积放大，导致边界类样本出现不稳定预测。这说明全天候识别研究下一步仍需在多帧信息融合、曝光控制与不确定性估计方面进一步推进，使模型不仅“平均上有效”，也能在极端场景下具备可解释的风险输出能力。

== 小结

本研究将低光增强模块并入全天候识别框架，并通过模型设计、对比实验和真实场景分析验证了其有效性。实验结果表明，增强前端能够在弱光条件下改善结构与颜色信息恢复，从而提升脱叶率与吐絮率识别的精度与鲁棒性。

结果表明，成熟期棉花全天候识别需要围绕真实成像退化和端侧部署约束，综合考虑数据构建、增强前端、识别模型和系统评测。该方法为后续章节中的空间聚合与处方图生成提供了更稳定的输入基础。
