#import "../../../template.typ": three-line-table

== 引言

前述章节分别在受控数据划分下验证了传统机器学习基线（第 3 章）、低光增强方法（第 4 章）与轻量级双指标识别模型（第 5 章），但实验室条件下的指标表现并不等同于真实生产环境中的可用性。大田场景中存在光照与天气变化、非冠层背景干扰、田块尺度差异以及连续作业下的时序波动，识别流程能否在实际生产中稳定反映棉花脱叶吐絮进程，仍需在真实棉田中进行端到端检验。同时，从单幅影像识别到田块尺度应用，还需衔接影像采集、图像块推理、地理映射和结果服务。

为此，本章将前述章节构建的低光增强与双指标识别流程应用于生产棉田，在正常光照与低光两类场景下开展大田验证，考察预测结果能否反映成熟进程及田块内部差异。本章其余内容安排如下：6.2 节介绍实验区域；6.3 节分析正常光照下的双指标空间分布与时序变化；6.4 节比较低光场景增强前后的影像与识别结果；6.5 节分析正常光照识别与低光串联流程的速度和部署代价；6.6 节为本章小结。

== 实验区域

本章在新疆石河子北泉双泉集三分厂的生产棉田检验双指标识别流程。试验田为当地农户自耕地，气候、品种、脱叶催熟处理与常规航测安排同第 2 章（见@chap:dataset）。通过连接无人机采集、图像块识别和地理映射，观察预测结果能否反映成熟进程及田块内部差异。

@fig:field_lowlight_area 中，黄色边界为前文采集双指标数据的 L1 棉田。低光试验仍在该目标作业域内进行，主要考察时相与光照变化。左上角浅蓝色部分为低光采集区，邻近道路、林带和地块边界，影像包含非冠层背景与局部阴影。

#figure(
  image("图-低光脱叶吐絮实验区.png", width: 50%),
  caption: [低光脱叶率与吐絮率实验区示意图。黄色边界为 L1 棉田范围，左上角浅蓝色区域为低光图像采集区。],
) <fig:field_lowlight_area>

正常光照试验分析日间自然光照下的双指标时空分布，低光试验比较增强前后的预测变化。两者使用同一田块空间参照，便于对照不同阶段与光照下的结果。

本章时相沿用第 2 章的 D 记号（见@chap:dataset），即喷施脱叶剂当日为 D0，其后第 $n$ 天记为 D$n$。

== 正常光照场景验证

=== 影像处理与空间映射流程

日间验证选取 L1、L2 两块棉田在催熟作业后不同日期的无人机影像。影像经筛选、分块后输入 RTCMNet，获得脱叶率与吐絮率等级，再按图像块位置生成空间分布图，用于比较不同日期的成熟状态和田块内部差异。

=== 空间分布

@fig:field_daytime 展示了 L1 催熟后第 2、8、18 天（D2、D8、D18）和 L2 第 0、10、21 天（D0、D10、D21）的识别结果。圆点表示图像块位置，颜色由绿至红对应预测等级由低至高；DE 为脱叶率等级（0--7 级），BA 为吐絮率等级（4--9 级）。L1 从 D2 的低等级逐步转为 D18 的较高等级，L2 从 D0 到 D21 也呈现相同的整体上升趋势。预测结果在田块内形成连续区域，随时间变化的方向与催熟进程一致。

#figure(
  image("图-大田.png", width: 100%),
  caption: [日间条件下 L1 和 L2 棉田的脱叶率与吐絮率空间识别结果。地块、催熟后天数和指标依次标注为 L1/L2、D 和 DE/BA；DE 表示脱叶率等级，BA 表示吐絮率等级。],
) <fig:field_daytime>

=== 时序演化与田间现象分析

L1 主体区域的双指标随时间升高，边缘到 D18 仍保留较低脱叶率等级。现场观察发现，边缘通风与光照较好，植株长势旺、叶量较大，可能使相同施药条件下的脱叶响应较慢。L1 左上部还存在与滴灌压力不足有关的低密度区域，植株间空间较大，预测分布也不同于主体。空间图保留了这些局部差异，但其成因判断仍需结合田间调查。

L2 下边缘紧邻树木，无人机喷施时需保留避障距离，边界药剂覆盖相对不足；该区域在后续时相中表现为较慢的脱叶与吐絮进程。左上角缺口种有经济树木，其空间响应也不同于棉田主体。预测的边界滞后与局部异常位置与现场观察相符，可据此确定需要复核的区域。

日间田间实验完成了影像采集、图像块识别和空间映射。脱叶率与吐絮率均随时间上升，但局部区域的变化并不同步：部分区域叶片脱落较慢，棉铃开裂已进入较快推进阶段。多时相双指标分布可用于定位持续滞后区、组织现场复核和分析催熟作业效果。

== 低光场景验证

=== 低光影像采集与增强流程

为进一步验证前述低光增强方法在真实成熟期棉田中的适用性，本研究分别于脱叶催熟作业后第 3 天（D3）和第 20 天（D20）的日落时分，在 L1 地块左上部的低光图像采集区开展无人机影像采集。两个时相分别代表催熟初期叶片大量保留和催熟后期叶片脱落、棉絮集中暴露的典型冠层状态。与日间条件相比，日落时太阳高度角较低，棉花冠层内部及植株行间形成较大范围阴影，原始影像整体亮度下降，暗部对比度受到压缩，叶片、枝条、棉絮与地表背景之间的局部边界更难分辨，增加了脱叶率与吐絮率的视觉识别难度。

D3 与 D20 沿第 2 章的固定航线采集，保持飞行高度、航向、拍摄间隔和相机设置不变。每个时相有 140 幅航点影像，各裁剪一个中心 $224 times 224$ 像素图像块，因此每个时相对应 140 个空间样本。原始低光输入（LOW）与增强输入（EN）由同一裁剪块生成，位置、范围和尺寸一致，仅增强处理不同。

裁剪块经@chap:lowlight 训练完成的 $L^3$-AgriUAVNet 处理，得到增强图像。本次采集缺少严格配准的正常曝光参考，因此主要通过同一图像增强前后的视觉对比，观察阴影和非均匀照明区域的变化。

#pagebreak(weak: true)

=== 不同催熟阶段的低光增强效果

#figure(
  image("图-脱叶吐絮增强d3-对比.png", width: 72%),
  caption: [催熟作业后第 3 天日落时分采集的棉田低光图像及其增强结果。上下两行为不同采样视野，左列为原始低光图像，右列为 $L^3$-AgriUAVNet 增强图像。],
) <fig:field_lowlight_d3>

如@fig:field_lowlight_d3 所示，两组原始图像均存在明显的局部阴影。冠层暗部的绿色叶片与枝条亮度接近，白色棉絮邻近结构的对比度也较低。经 $L^3$-AgriUAVNet 处理后，阴影区域亮度提高，叶片层次、冠层间隙和植株轮廓更加清晰，棉絮与周围叶片的明暗差异也得到增强。上下两组视野呈现相近的变化。

D20 的棉叶已大量脱落，棉絮、植株间隙和地表背景的可见比例增大。相较 D3，图像同时包含高反射棉絮、暗色枝条、残余绿叶与裸土，亮度跨度更大，增强时需要兼顾暗部恢复和高亮区域细节。

#figure(
  image("图-脱叶吐絮增强d20-对比.png", width: 72%),
  caption: [催熟作业后第 20 天日落时分采集的棉田低光图像及其增强结果。上下两行为不同采样视野，左列为原始低光图像，右列为 $L^3$-AgriUAVNet 增强图像。],
) <fig:field_lowlight_d20>

如@fig:field_lowlight_d20 所示，上排视野中的棉絮在原始低光图像内仍具有较高亮度，暗部枝条、残余叶片及棉絮间隙则不易辨认。增强后，冠层内部结构和棉田背景更加清晰。部分棉絮接近高亮饱和，表明后期高吐絮率场景需要兼顾暗部提亮与高亮保持。下排视野以稀疏冠层、残余绿色植被和地表背景为主，增强后绿色区域、行间结构与土壤差异更加明显。

D20 影像还呈现出较明显的方向性模糊，可能与日落时曝光时间延长及无人机平台运动有关。增强处理提高了整体亮度和局部对比度，但采集阶段产生的模糊仍然保留。因此，低光采集还需综合控制曝光参数、飞行速度和平台稳定性。

脱叶率判断涉及残叶、枝条、冠层孔隙和裸地等结构，吐絮率则更多依赖棉絮与叶片、棉铃及背景的差异。D3 与 D20 图像表明，增强提高了这些线索的可见度。下节将增强结果输入 RTCMNet，进一步比较双指标预测的变化。

=== 增强前后的空间识别结果

将 D3 日落时分的原始低光图像和对应增强图像分别输入 RTCMNet，并依据采样位置生成脱叶率与吐絮率空间分布。选取相邻时相 D2 的日间识别结果作为田块成熟状态的背景参照，并在 L1 地块中标出低光采集区。

#figure(
  image("图-脱叶吐絮增强d3.png", width: 100%),
  caption: [低光增强前后的 D3 脱叶率与吐絮率空间识别结果。两侧分别为 L1 地块 D2 日间吐絮率（BA）和脱叶率（DE）分布，红框标示低光采集区；中间上行为 D3 原始低光图像的识别结果（LOW），下行为经 $L^3$-AgriUAVNet 增强后的识别结果（EN），黑框与虚线标示局部对应区域。],
) <fig:field_lowlight_d3_prediction>

如@fig:field_lowlight_d3_prediction 所示，原始低光输入下的 D3 吐絮率预测整体偏向较高等级，局部高等级点较为分散。增强后，预测等级向中低区间回落，空间分布更接近 D2 日间参照区域的主体状态。

D3 脱叶率在增强前后均以低等级为主，符合催熟作业后第 3 天叶片仍大量保留的阶段特征。增强后部分样本由 1 级调整为 0 或 2 级。吐絮率预测分布的调整较明显，脱叶率则保持低等级为主，表明两个任务头对增强输入的响应不同。

采用相同流程分析 D20 影像，并以相邻时相 D18 的日间结果作为催熟后期的背景参照。D18 与 D20 相隔两天，该参照用于比较催熟后期的总体状态和局部空间分布。

#figure(
  image("图-脱叶吐絮增强D20.png", width: 100%),
  caption: [低光增强前后的 D20 脱叶率与吐絮率空间识别结果。两侧分别为 L1 地块 D18 日间吐絮率（BA）和脱叶率（DE）分布，红框标示低光采集区；中间上行为 D20 原始低光图像的识别结果（LOW），下行为经 $L^3$-AgriUAVNet 增强后的识别结果（EN），黑框与虚线标示局部对应区域。],
) <fig:field_lowlight_d20_prediction>

如@fig:field_lowlight_d20_prediction 所示，D20 吐絮率在原始低光和增强输入下均以中高等级为主，与 D18 日间地块已进入高吐絮阶段的总体状态一致。增强后，高等级预测仍在采集区主体范围内连续分布，黑框所示局部点的预测有所回落。

D20 脱叶率预测呈现出更明显的空间差异。原始低光输入下，部分区域集中在低至中等级；增强后，采集区上部黑框内仍保持较低等级，中下部则出现连续的中高等级响应。该采集区位于 L1 地块左上部，日间多时相结果和现场调查均显示，该区域受滴灌压力及水分分布不均影响，植株密度、长势和脱叶进程存在空间差异。增强后的等级分布与这些田间观测相对应。D3 和 D20 两个时相中，不同区域和两个任务头均呈现出差异化的预测调整。

#figure(
  three-line-table(table(
    columns: 7,
    table.header([时相], [输入], [$n$], [脱叶率等级], [吐絮率等级], [脱叶率熵], [吐絮率熵]),
    [D3], [LOW], [140], [$1.34 plus.minus 0.96$], [$7.46 plus.minus 0.73$], [0.51], [1.22],
    [D3], [EN], [140], [$0.54 plus.minus 0.88$], [$5.19 plus.minus 1.81$], [0.96], [1.05],
    [D20], [LOW], [140], [$2.07 plus.minus 1.43$], [$7.82 plus.minus 0.98$], [1.05], [1.03],
    [D20], [EN], [140], [$3.20 plus.minus 2.45$], [$8.19 plus.minus 1.14$], [1.51], [1.40],
  )),
  caption: [低光增强前后 RTCMNet 双指标预测分布的描述性统计。LOW 与 EN 为同一空间位置的原始低光和增强输入；等级栏为均值 $plus.minus$ 标准差，熵以 bit 计，用于描述预测等级分布的离散程度，不代表识别精度。],
  kind: table,
) <tab:field_lowlight_prediction_stats>

@tab:field_lowlight_prediction_stats 汇总了空间位置一一对应的 140 个样本。D3 中，增强后脱叶率和吐絮率的平均预测等级分别下降 $0.80$ 和 $2.27$ 级；D20 中，两者分别上升 $1.13$ 和 $0.37$ 级。D20 脱叶率等级的标准差由 $1.43$ 增至 $2.45$，等级熵由 $1.05$ 增至 $1.51$，预测等级的离散程度增大。这一分布与日间识别结果及现场水分调查揭示的空间异质性相对应。D3 脱叶率和 D20 吐絮率的等级熵也有所升高，D3 吐絮率的熵略有下降，表明增强对两个任务头及不同成熟阶段的影响方向不同。

== 识别速度测试

=== 测试环境与计时方法

田间催熟效果监测需要连续处理多个航点的图像，并及时输出脱叶率与吐絮率结果。正常光照下可直接使用 RTCMNet，低光条件下则需先增强再识别。增加一个前处理模型是否会抵消轻量识别的速度优势，需要通过完整串联测试判断。本节在同一台计算机上测试 RTCMNet、LLFormer 和 $L^3$-AgriUAVNet + RTCMNet，以考察单模型与串联流程的实际执行代价。其中 LLFormer 为低光增强模型，用于提供较高复杂度增强前端的耗时参照，其输出并不包含脱叶率与吐絮率预测。

测试设备搭载 Intel Core i7-12650H CPU、32 GB DDR5 内存及 NVIDIA GeForce RTX 4060 显卡，操作系统为 Windows 11。各配置使用固定随机输入，预热 5 次后计时 15 次。串联测试将 $L^3$-AgriUAVNet 的输出作为 RTCMNet 的输入，每次记录完整的两阶段推理。本节仅采用 CPU 测试结果：Web CPU 使用 WASM，原生 CPU 使用 CPUExecutionProvider。输入为 float32 张量，形状为 $1 times 3 times 224 times 224$，批量大小为 1，原生服务使用 ONNX Runtime 1.29.0。

为统一模型选择、输入配置和计时操作，本研究开发了推理测试软件，界面见@fig:inference-benchmark-interface。软件提供单模型测试与低光增强串联两种模式，可设置输入张量形状、批量大小、预热次数和计时次数，并展示原生推理服务的连接状态及运行库版本。本节按测试记录报告平均耗时、分位数及波动范围，FPS 保留界面显示值。

#figure(
  image("inference_benchmark_interface.png", width: 55%),
  caption: [本研究开发的推理测试软件界面，展示模型选择、测试模式切换与输入参数配置。],
) <fig:inference-benchmark-interface>

=== 单模型与串联流程的速度比较

#figure(
  block(breakable: false, three-line-table(table(
    columns: (1.35fr, 1.15fr, 0.8fr, 0.8fr, 0.8fr, 0.8fr, 0.65fr),
    table.header([模型或流程], [执行配置], [均值], [P50], [P90], [P95], [FPS]),
    [RTCMNet], [Web CPU], [362.9], [363.9], [371.1], [381.7], [2.8],
    [RTCMNet], [原生 CPU], [56.253], [55.262], [59.747], [59.916], [17.8],
    [LLFormer], [Web CPU], [1595.3], [1595.3], [1609.1], [1610.6], [0.6],
    [LLFormer], [原生 CPU], [323.9], [323.0], [334.6], [335.9], [3.1],
    [$L^3$ + RTCMNet], [Web CPU], [709.9], [708.5], [727.1], [740.0], [1.4],
    [$L^3$ + RTCMNet], [原生 CPU], [167.6], [165.5], [177.5], [180.4], [6.0],
  ))),
  caption: [同机单模型与串联推理速度。耗时单位为 ms；P50、P90、P95 为测试界面报告的分位数。$L^3$ 表示 $L^3$-AgriUAVNet。],
  kind: table,
) <tab:field_inference_speed>

@tab:field_inference_speed 显示，原生 CPU 上 RTCMNet 的平均耗时为 56.253 ms，加入低光增强后为 167.6 ms，约为直接识别的 2.98 倍。增强前端确实增加了处理时间，但完整串联仍保持约 6.0 次/s 的处理能力。对于逐航点或图像块级监测，这一结果表明当前计算机无需 GPU 加速即可执行低光双指标预测；是否满足连续视频处理，则还需根据目标帧率及其他环节耗时判断。

更关键的是，轻量串联的额外开销并未超过本次测试中较高复杂度增强模型的单独执行开销。原生 CPU 上，串联流程相对 LLFormer 的平均耗时由 323.9 ms 降至 167.6 ms，减少 48.3%，对应约 1.93 倍的处理速度。Web CPU 上，耗时由 1595.3 ms 降至 709.9 ms，减少 55.5%，对应约 2.25 倍的处理速度。

上述比较的意义在于：轻量方案已经完成“增强 + 双指标识别”，其耗时仍低于 LLFormer 仅完成增强的耗时。若采用 LLFormer 作为低光前端，随后还需接入识别模型才能完成相同任务。因此，本次记录支持轻量串联在推理效率上的优势，而“模型数量增加”并不必然意味着总体部署代价更高。不过，本次尚未直接测试 LLFormer + RTCMNet，不能将两个独立测试的耗时之和当作该组合的实测结果，也不能将这一结论推广至所有大型模型。

=== 串联耗时组成与执行稳定性

#figure(
  block(breakable: false, three-line-table(table(
    columns: (1.5fr, 1fr, 1fr, 1fr),
    table.header([执行配置], [增强阶段（ms）], [识别阶段（ms）], [串联均值（ms）]),
    [Web CPU], [351.347], [358.533], [709.9],
    [原生 CPU], [68.861], [98.704], [167.6],
  ))),
  caption: [完整串联测试中的两阶段耗时。增强输出传入识别模型；分阶段耗时之和与串联均值的微小差异来自显示精度。],
  kind: table,
) <tab:field_cascade_stages>

@tab:field_cascade_stages 表明，原生 CPU 上增强与识别分别占串联耗时的约 41.1% 和 58.9%。串联中的识别阶段为 98.704 ms，高于 RTCMNet 独立测试的 56.253 ms，说明独立计时结果不能直接代表连续执行时的阶段开销。两次测试的输入来源和执行上下文不同，张量衔接、内存访问、会话调度等因素均可能参与这一差异。评价应用流程应优先采用 167.6 ms 的完整串联结果，而非将增强阶段与独立识别时间拼接估算。

#figure(
  block(breakable: false, three-line-table(table(
    columns: (1.4fr, 1.25fr, 1fr, 1fr, 1fr),
    table.header([模型或流程], [执行配置], [最小值], [最大值], [标准差]),
    [RTCMNet], [Web CPU], [350.8], [381.7], [7.399],
    [RTCMNet], [原生 CPU], [52.221], [60.153], [2.589],
    [LLFormer], [Web CPU], [1571.4], [1610.6], [11.817],
    [LLFormer], [原生 CPU], [311.7], [338.5], [7.540],
    [$L^3$ + RTCMNet], [Web CPU], [699.5], [740.0], [10.476],
    [$L^3$ + RTCMNet], [原生 CPU], [153.5], [185.2], [8.660],
  ))),
  caption: [各配置在 15 次计时中的耗时波动，单位为 ms。],
  kind: table,
) <tab:field_inference_variability>

平均耗时之外，分位数和波动范围有助于判断短时等待是否稳定。原生 CPU 串联的 P95 为 180.4 ms，标准差为 8.660 ms，变异系数约为 5.2%；其最大耗时 185.2 ms 仍低于 LLFormer 的最小耗时 311.7 ms。Web CPU 上同样如此，串联最大值 740.0 ms 低于 LLFormer 最小值 1571.4 ms。因而，本次速度差异并非只体现在均值上。但每组仅计时 15 次，P95 接近样本上端，尚不足以刻画长时间运行中的极端延迟或热稳定性。

原生 CPU 串联比 Web CPU 约快 4.24 倍，显示运行环境对最终速度有明显影响。浏览器方案便于通过网页提供交互入口，原生 CPU 则在本次配置下更适合集中处理多个图像块。两者的具体差距还受到运行库、线程及算子实现等因素影响，不能完全归因于浏览器本身。

=== 模型规模与田间部署意义

模型规模与实际延迟反映不同的资源需求。为进一步解释串联方案的部署代价，@tab:field_pipeline_cost 汇总了前述章节的参数量与计算量，并列出两个轻量模型相加后的总量。

#figure(
  block(breakable: false, three-line-table(table(
    columns: (2fr, 1fr, 1fr),
    table.header([模型或流程], [参数量（M）], [计算量（GMACs）]),
    [RTCMNet], [0.370], [0.190],
    [$L^3$-AgriUAVNet], [0.139], [6.252],
    [$L^3$-AgriUAVNet + RTCMNet], [0.509], [6.442],
    [LLFormer], [5.807], [15.507],
    [ViT-S], [21.660], [4.600],
  ))),
  caption: [模型与串联流程的理论开销。采用前述章节 $224 times 224$ 输入下的统计设置；串联项为两模型之和，不包含模型外处理。],
  kind: table,
) <tab:field_pipeline_cost>

串联方案共含 0.509 M 参数、6.442 GMACs，分别为 LLFormer 的约 8.77% 和 41.54%。即使承担额外的双指标识别任务，其权重规模和理论运算量仍较低，与本次 CPU 延迟优势相呼应。在相同权重精度下，更少的参数有利于减小模型存储与分发负担。

与第 5 章的 ViT-S 相比，串联参数量仅为其约 2.35%，但计算量高于 ViT-S 的 4.600 GMACs。目前数据可得出结论：采用面向任务的轻量增强与轻量识别组合，可以在有限计算资源下兼顾低光预处理与双指标输出，避免加重部署负担。

速度优势还需与图像质量和下游任务表现共同判断。第 4 章中，$L^3$-AgriUAVNet 的 PSNR 为 26.24 dB，略低于 LLFormer 的 26.54 dB，而 SSIM 为 0.893，高于后者的 0.888。这说明轻量方案取得速度收益时仍保持了接近的恢复质量。

若以 140 个输入图像块顺序处理为例，按原生 CPU 均值估算，直接识别约需 7.88 s，轻量串联约需 23.46 s，而 LLFormer 单独增强约需 45.35 s。该估算仅反映模型推理时间，实际任务还需计入影像读取、裁剪、张量转换及地理结果整理。对按航点复核和批量生成成熟状态图的应用，约二十余秒完成这一规模的低光模型推理具有实际可用性，也说明无需将所有影像都送入复杂增强网络。正常光照可直接识别，低光影像按场景启用增强。

本次测试将此前分散在不同设备上的模型计时推进到同机完整串联验证。结果支持在当前配置下优先采用轻量增强与识别组合：它在 CPU 上完成两阶段处理的耗时低于 LLFormer 单独增强，同时具有较小的参数规模。这里的低成本主要体现为较小的模型存储与推理资源需求。

== 小结

本章将双指标识别与低光增强方法应用于生产棉田，将图像块预测与航点位置、采集时间关联，形成田块成熟状态的空间表达，并通过同机推理测试考察处理效率。正常光照下，脱叶率与吐絮率等级随催熟进程整体上升，但局部变化并不同步，部分边界区域与田块主体存在差异。将两项指标映射到相同位置，有助于区分叶片脱落滞后与棉铃吐絮滞后，为定位差异区域、安排现场复核和判断后续作业时机提供参考。日落影像实验完成了 $L^3$-AgriUAVNet 与 RTCMNet 的串联，增强后的预测调整随任务和催熟阶段变化，表明光照适配需要结合具体冠层状态与识别任务评价。

识别速度测试进一步说明了这一流程在农业批量监测中的应用潜力。在 i7-12650H 原生 CPU 上，RTCMNet 单次双指标识别平均耗时为 56.253 ms，低光增强与识别完整串联为 167.6 ms，分别对应约 17.8 次/s 和 6.0 次/s。串联流程已包含增强与双指标输出，其耗时仍比 LLFormer 单独增强的 323.9 ms 减少 48.3%；Web CPU 上也保持相同优势，耗时减少 55.5%。这说明通过轻量模型分工处理低光恢复与表型识别，可以在扩展光照适用范围的同时控制处理开销，串联流程仍可比本次较高复杂度单模型更快。

对田间应用而言，较低延迟有利于缩短影像采集后的结果等待时间，使多航点数据能够较快转化为需要复核的区域线索。以 140 个图像块顺序处理估算，原生 CPU 上直接识别约需 7.88 s，低光串联约需 23.46 s，均仅计模型推理时间。这一处理速度有利于在催熟作业窗口内完成批量分析并及时组织现场复核。流程实际由 CPU 执行，也便于利用已有计算机部署，减少对专用 GPU 推理环境的依赖。正常光照采用直接识别、低光条件采用增强后识别，还可按场景分配计算资源。上述优势主要体现在处理时间和部署资源需求，具体设备费用与能耗节约仍需实测。

本章结果共同支持了从冠层影像到田块状态表达的流程可执行性及当前设备上的处理效率。空间预测可辅助田间调查，但低光棉田缺少同步人工真值，尚不能量化增强带来的双指标精度增益；短时随机输入测速也不能替代完整农业作业评价。后续应结合独立地块的人工调查，记录影像读取、裁剪、推理和结果制图的全流程耗时，并开展持续运行测试，以进一步确定实际监测精度、反馈速度与长期部署稳定性。
