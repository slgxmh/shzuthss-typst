#import "../../../template.typ": three-line-table

= 实验数据集及理论基础 <chap:dataset>

== 多源检测公开数据集

=== 图像分类常用数据集

公开数据集为计算机视觉模型提供了可复用的“通用视觉先验”，也是不同方法之间进行公平比较的事实标准。以大规模图像分类数据集 ImageNet 为代表，其通过层级化类别体系与规模化标注，显著推动了深度网络在表征学习上的发展，并成为后续检测、分割等任务常用的预训练来源@deng2009imagenet。

在目标检测方向，PASCAL VOC 与 MS COCO 被广泛用于模型训练与评测。VOC 以相对简洁的类别体系与统一评测协议（如 mAP）推动了检测算法的标准化比较@everingham2010pascal；COCO 则进一步引入更复杂的场景构成、更丰富的目标尺度与遮挡情况，并同时提供边界框与实例分割标注，从而更贴近真实应用分布并对模型鲁棒性提出更高要求@lin2014coco。对于语义分割与场景理解，Cityscapes 等像素级精标数据集提供了高质量的密集标注，为方法在细粒度边界与类别一致性方面的改进提供了统一基准@cordts2016cityscapes。

从数据集构成角度看，上述通用数据集通常由以下要素共同决定其可用性与可比性：

1. *数据源与场景覆盖*：采集平台与成像条件决定了分辨率、视角、光照与背景复杂度，从而影响模型能否学到稳定可迁移的纹理与结构特征。
2. *类别体系与标注粒度*：分类标签、边界框、实例掩膜与像素级语义标注对应不同监督信号强度，也对应不同任务对数据规模与一致性的要求。
3. *划分策略与评测协议*：训练/验证/测试的独立划分、指标定义（如 mAP、IoU 等）与公开基准共同构成“可复现实验”的关键前提。
4. *统计分布与长尾问题*：类别不均衡、尺度分布与遮挡比例会显著影响模型学习到的决策边界，并在跨域迁移时放大分布偏移风险。

通用视觉数据集的价值不仅来自样本规模，也来自稳定的类别定义、标注协议和评测方式。借鉴这一经验，本研究在构建棉花数据集时同步规定采集、标注、数据划分和评价指标，使后续模型在一致条件下进行比较。

农业影像与通用对象识别数据存在明显差异。脱叶率和吐絮率反映连续变化的生理与管理状态，没有固定轮廓和清晰类别边界，而是通过叶片减少、白絮外露和冠层疏密等变化共同表现。因此，数据集除规定类别外，还需明确调查方法、采样尺度、观测时相和等级边界。

=== 植物表型公开数据集

与通用视觉数据集相比，植物表型数据集往往具有更强的领域约束与更复杂的数据结构：一方面，表型任务通常面向特定作物、特定生育期或特定管理措施，导致公开数据集的场景覆盖更碎片化；另一方面，表型指标既包含分类/检测式离散标签，也包含株高、冠层结构、叶面积指数等连续性状，并常与多源传感信息（RGB、多光谱/高光谱、深度或 LiDAR）耦合，从而对数据组织与对齐提出更高要求@tian2020uav_crop_monitor @xu2019multispectral。

在农业病害与健康诊断方向，PlantVillage 等公开数据集通过标准化采集与大规模图像样本为移动端病害识别研究提供了基础资源@hughes2015plantvillage。面向田间表型与结构性状估计，研究更强调“多时相 + 多传感 + 多尺度”的联合建模。例如，基于 UAV 的高通量表型研究通常利用时间序列影像捕捉冠层结构与生长动态，并结合多光谱指数、点云或深度信息提升对结构性状与成熟状态的可观测性@ye2023timeseries @fan2025rgb_lidar。针对棉花等大田作物，多源遥感与机器学习结合的研究也表明，多传感融合能够在复杂背景与光照变化下提高表型诊断的稳定性@zhao2025fusion。

植物表型数据可按任务分为离散识别、连续估计和时序状态监测三类。病害识别和果实计数属于离散任务；株高、生物量和叶面积指数通常采用连续估计；生长过程与管理状态监测则关注多日期条件下的变化。本研究属于第三类，需要保持航点影像、观测日期和双指标调查记录之间的对应关系。

植物表型样本通常同时包含影像、位置、日期和人工调查结果。对于 UAV 高通量表型数据，同一地块还可能对应 RGB、多光谱、深度或点云数据，使用前需完成空间配准、时间对应和尺度匹配。本研究将 UAV 影像、航点坐标、观测时相以及脱叶率和吐絮率等级关联起来，使每个图像样本均可追溯到相应调查记录。

从数据集构成角度看，植物表型类公开数据集通常需要显式描述并规范以下组成部分，才能支撑可复现实验与跨工作比较：

1. *多源数据载荷*：传感器类型（RGB/多光谱/高光谱/LiDAR/深度）、谱段配置、辐射定标与曝光策略决定了可用信息维度与噪声形态@tian2020uav_crop_monitor。
2. *时空组织与配准*：航线与重叠度、地理配准、地块/小区（plot）层级组织，以及跨时相对齐方式直接影响“同一目标在不同时刻/不同传感器下”的可比性@ye2023timeseries。
3. *标注对象与尺度*：表型标注可以发生在像素、植株、行带、地块或小区尺度；离散分级标签与连续性状回归标注需要统一定义测量流程与误差来源，并明确是否存在专家一致性校验。
4. *切分策略与评测目标*：除常规随机切分外，更应关注跨地块、跨年份、跨品种与跨光照的切分与外推评测，以刻画模型在真实生产条件下的泛化能力。
5. *偏差与可迁移性*：作物品种、管理措施、土壤背景、杂草与阴影等因素会引入系统性偏差；公开数据集在发布时应尽量给出元数据与统计描述，以支持后续的域适配与误差分析@xu2019multispectral。

上述要素分别影响信息范围、样本可比性、任务定义和评价结果。只有统一数据组织与评测协议，模型间的性能差异才具有明确解释基础。

从本研究的需求出发，公开数据集分析带来三点直接启发。第一，面向成熟期棉花双指标监测的数据集需要显式纳入多时相信息，因为脱叶率与吐絮率本质上是过程性指标；第二，数据集应尽量保留真实大田背景，而不应过度清洗场景，以便更接近实际生产环境；第三，评测不能仅采用单次随机切分，而应结合地块、日期和光照条件等元信息理解结果。这三点直接影响了本研究后续棉花数据集与低光数据集的组织方式。

== 构建棉花脱叶率与吐絮率数据集

本研究以田间棉花样本为对象，构建脱叶率与吐絮率双标签识别数据集。为提升数据管理效率并保证实验可复现性，本研究采用统一的 HDF5 文件组织样本、标签与切分索引，支撑后续批量训练与多模型公平对比。

=== 实验区域

本次实验于新疆石河子北泉双泉集三分厂进行，如@data_area 所示。研究区属温带大陆性气候，年均降水量较低，水资源相对匮乏，昼夜温差显著，这些条件对棉花生长周期及脱叶剂作用均有重要影响。实验选用的棉花品种为新陆早 83 号，该品种在当地种植较为广泛。实验所用脱叶剂由噻苯敌草隆和乙烯利组成，用量分别为 15 克/亩和 40 克/亩。实验田为当地农户自耕农田，而非专门试验田，以尽量保持研究条件与实际生产环境一致。

#figure(
  image("data_area.png"),
  caption: "试验区概况。（a）试验区位于石河子；（b）L1 与 L2 两块试验田；（c）L2 试验田航线及参数；（d）L1 试验田航线及参数；（e）棉花样本标记；（f）催熟作业过程中的棉花；（g）达到采收标准的棉花。",
) <data_area>

=== 图像采集

数据采集工作从2024年9月24日开始至10月4日结束，共进行26期数据收集，每期包括L1和L2两块试验田的可见光数据。在数据采集过程中，采用了大疆Mavic3多光谱版无人机。这款无人机专为农业应用设计，具有高精度遥感能力。实验航线由大疆Pilot2软件自动生成， 如@fly_param 所示。研究时间覆盖了棉花生长的关键阶段，为实验提供了连续且全面的数据支持。

#figure(
  three-line-table(
    table(
      columns: 3,
      [参数名称], [参数值], [参数说明],
      [地面分辨率], [1cm/pixel], [手动指定分辨率],
      [航线高度], [27.3m], [由地面分辨率自动计算的航线高度],
      [航向重叠率], [60%], [平坦地面的验设置],
      [旁向重叠率], [70%], [平坦地面的经验设置],
      [飞行速度], [2.7m/s], [为了更好获得清晰的图像],
    ),
  ),
  caption: "UAV 航线与影像采集参数。",
) <fly_param>

=== 专家标注

本研究采取人工调查的方式进行脱叶率与吐絮率的统计。为确保数据的代表性和准确性，在试验田内均匀布置样本点。在每个样本点选取三株棉花作为样本，记录绿叶数量、总棉铃数量和开放棉铃数量，用于计算吐絮率和脱叶率。如 @eq_boll 与 @eq_de 所示。

$ "BA"_t = frac(B_t, B_0) times 100 % $ <eq_boll>
$ "DE"_t = (1 - frac(L_t, L_0)) times 100 % $ <eq_de>

其中，$"BA"_t$ 和 $"DE"_t$ 分别表示第 $t$ 天的吐絮率和脱叶率，$B_t$ 为第 $t$ 天开放棉铃数，$B_0$ 为施药初始棉铃总数，$L_t$ 为第 $t$ 天残留叶片数，$L_0$ 为施药初始叶片数。

=== 图像预处理

在完成数据收集后，本研究依据人工调查结果筛选无人机获取的图像，并为其提供相应的分类标签标注。为提高标注效率与准确性，本研究自主开发无人机数据解析软件，用于读取 MRK 文件中的位置信息，实现图像与地理坐标匹配，并辅助标注分类标签。为增强模型对复杂田间环境的适应性并确保结果可靠性，本研究对数据进行预处理：1）随机旋转和镜像以增强方向鲁棒性；2）随机裁剪为 224×224 以增强尺度鲁棒性；3）剔除田块边缘图像以减少非代表性样本；4）仅进行均值化处理以避免特征分布过于单一。根据田间调查结果，脱叶率划分为 9 个等级、吐絮率划分为 10 个等级。

=== 数据集制作

预处理后数据集示例如@fig:dataset_num 所示。数据集以预处理后的 224×224 图像块为划分单位，按照 5:1:4 的比例划分为训练集、验证集与测试集，并通过固定随机种子保证不同实验批次间可比性。其中，训练集用于调整模型参数，验证集用于优化超参数，测试集用于评估模型的最终性能。

本研究面向特定生产区域内固定航线条件下的棉花成熟状态监测，当前评价协议旨在验证所提方法在目标作业域内的识别可行性，并在统一的数据与训练条件下比较不同模型的相对性能及部署代价，而非估计跨区域、跨年份或跨品种的普适泛化能力。连续棉田影像具有较高的类内相似性与空间连续性，这是固定航线农业监测任务的客观属性。所有对比模型均采用相同的数据划分索引、训练配置和评价指标，因此，该协议可支持统一域内条件下的方法比较；相应结果应解释为目标作业域内的图像块级评价，不宜外推为跨域条件下的绝对性能。

#figure(
  image("dataset_num.png"),
  caption: "棉花双指标数据集示例与等级分布。（a）图像样本及脱叶率标签 D、吐絮率标签 B；（b）脱叶率标签（de_label）与吐絮率标签（ba_label）的样本分布。",
)<fig:dataset_num>

受棉花真实催熟进程及观测时间窗口影响，数据集中实际出现的标签范围并未覆盖全部理论等级。施用脱叶催熟剂时，棉花已具有一定的自然吐絮水平，因此首次观测的吐絮率并非从零开始，实际样本主要分布在 4--9 级；另一方面，受植株生理状态、田间环境和药剂作用差异影响，观测期末仍存在一定叶片残留，脱叶率未达到理论最高等级，实际样本分布在 0--7 级。该分布反映了试验期内棉花成熟状态的真实变化过程，并非数据遗漏或人为删除类别。

数据文件包含三个核心数据集，如@tab:hdf5-core 所示。

#figure(
  three-line-table(table(
    columns: 4,
    [字段名], [含义], [维度], [类型],
    [`images`], [图像数据], [`(N, 3, 224, 224)`], [`float32`],
    [`de_labels`], [脱叶率标签], [`(N,)`], [`int8`],
    [`ba_labels`], [吐絮率标签], [`(N,)`], [`int8`],
  )),
  caption: "HDF5 数据文件核心字段说明。",
) <tab:hdf5-core>

其中，$N$ 表示样本总数。图像采用通道优先（$C, H, W$）格式，能够被常见机器学习框架直接读入，减少数据转置和额外预处理带来的工程开销。

== 构建大田低光增强数据集

=== 实验区域

本次实验在新疆沙湾市周边的大田农业区开展。研究区属温带大陆性气候，年均降水量较低（125.9 至 207.0 毫米），水资源相对匮乏且昼夜温差显著。实验数据采集于实际生产田块（农户自耕地），而非专门试验田，因此能够更真实地反映大田场景中常见的地表背景变化、作物行间结构与管理差异。

#figure(
  image("area.png", width: 90%),
  caption: [
    研究区域示意图 (a) 研究区域在新疆沙湾市的地理位置；(b) 开阔大田实验区的无人机正射影像，其中 L1、L2 和 L3 分别表示不同作物地块；(c)–(e) 分别为 L1（小麦）、L2（棉花）和 L3（玉米）的地面实景图像。研究区域均为露天大田环境，具有典型的农业种植结构与自然光照条件。
  ],
) <fig:lowlight_area>

=== 图像采集

正常曝光数据采集于 2025 年 5 月 6 日至 6 月 12 日，覆盖作物生长的不同时期与田间外观变化。为增强数据多样性与代表性，从多期航测数据中均匀选取三种典型大田作物（小麦、棉花与玉米）的无人机影像，最终获得 1306 张原始 UAV 图像作为低光成对数据集的数据源。此外，另选取 26 张真实田间夜间低光图像，作为后续低光退化参数校准与统计一致性分析的外部分布参照。

本研究仅给出低光数据来源与采集安排的总体说明。关于低光成对样本如何生成、退化参数如何设置，以及合成数据与真实夜间分布如何进行一致性验证，统一在第 5 章中展开。

== 卷积神经网络相关理论基础

卷积神经网络（Convolutional Neural Network, CNN）通过局部连接、参数共享和层级特征提取处理图像数据。LeNet、AlexNet、VGG 和 ResNet 等网络推动了卷积结构在视觉识别中的应用@lecun1998lenet @krizhevsky2012alexnet @simonyan2014vgg @he2016resnet。本节介绍后续模型设计所涉及的卷积、感受野、残差连接和轻量化算子。

=== 卷积算子与参数共享

在深度学习框架中，二维卷积层通常实现的是“互相关（cross-correlation）”操作。给定输入特征图 $bold(x) in RR^(H times W times C_("in"))$，卷积核 $bold(w) in RR^(K times K times C_("in") times C_("out"))$，输出通道 $k$ 在空间位置 $(i, j)$ 的响应可写为：

$ y_(i,j,k) = sum_(c=1)^(C_("in")) sum_(u=1)^(K) sum_(v=1)^(K) w_(u,v,c,k) · x_(i+u, j+v, c) + b_k. $

在 stride 为 $s$、padding 为 $p$ 的情况下，输出空间尺寸为：

$ H_("out") = floor((H + 2p - K)/s) + 1,quad W_("out") = floor((W + 2p - K)/s) + 1. $

参数共享使同一卷积核在全图复用，并形成平移等变性。该性质适合目标位置不固定、局部纹理重复出现的田间影像。

=== 感受野、下采样与多尺度表征

卷积堆叠逐步扩大感受野，浅层侧重边缘与纹理，深层形成更抽象的语义特征。池化或步幅卷积可降低计算量，但过度下采样会损失脱叶率与吐絮率识别所需的细粒度信息。多尺度融合可联合浅层细节与深层语义，用于处理冠层尺度变化和局部遮挡。

=== 非线性、归一化与训练稳定性

非线性激活函数用于提升网络的表示能力。ReLU 及其变体因计算简单、梯度传播稳定而成为主流选择，并在深层网络训练中表现出明显优势@nair2010relu。

Batch Normalization（BN）对 mini-batch 特征进行标准化并引入可学习仿射参数，可改善训练稳定性@ioffe2015batchnorm。部署时，BN 可折叠到卷积权重中，减少额外推理开销。

=== 优化方法与正则化

CNN 的训练通常通过最小化经验风险实现。对分类任务，常用交叉熵损失；对回归任务，常用 $L_1/L_2$ 损失或其鲁棒变体。优化方面，SGD（含 momentum）仍是高性能基线；Adam 等自适应优化方法在收敛速度与超参数敏感性方面更具工程友好性，但在泛化上需结合任务与数据分布进行选择@kingma2014adam。

为缓解过拟合，常用正则化策略包括权重衰减（$L_2$ 正则）、数据增强与 Dropout。Dropout 通过随机失活神经元抑制共适应，可在一定程度上提升泛化能力@srivastava2014dropout。参数初始化同样影响训练稳定性，Xavier/Glorot 初始化从方差传播角度给出了深层网络可训练性的实践准则@glorot2010xavier。

=== 深度结构、残差连接与轻量化卷积

残差连接采用“恒等映射 + 残差”形式改善深层网络的梯度传播和训练稳定性@he2016resnet。

深度可分离卷积可显著降低 MACs 与参数量，是 MobileNet 等轻量骨干的核心算子@howard2017mobilenets。剪枝、量化和蒸馏还可进一步降低部署开销，但其实际收益需通过目标设备时延验证@cheng2017compression_survey。

=== 注意力机制与特征重标定

注意力机制根据输入对不同空间位置、通道或尺度的特征赋予权重，使网络突出与任务相关的响应。该方法由序列建模扩展到视觉任务后，形成了自注意力、空间注意力和通道注意力等主要类型@vaswani2017attention。

自注意力通过位置间相似性建立长程依赖，Vision Transformer 和 Swin Transformer 是其典型应用@vaswani2017attention @liu2021swin。轻量注意力则使用卷积、门控或局部统计调节空间与通道响应，计算开销通常更低。考虑到棉田影像的判别信息主要表现为局部纹理、边界和多尺度冠层结构，本研究采用后一路线满足端侧时延约束。

空间注意力生成二维权重图以突出叶缘、棉絮和冠层空隙等位置；通道注意力选择与颜色、纹理和结构有关的通道响应；特征重标定则通过缩放、偏置或全局调制调整特征分布。这些机制为后续 MSCA、ESA 和 GMod 的设计提供了基础。

RTCMNet 中的 MSCA（Multi-Scale Convolutional Attention）通过多尺度卷积和局部归一化生成注意力响应，避免高代价的全局矩阵运算@xu2024sctnet。低光增强网络中的 ESA（Enhanced Spatial Attention）在降采样特征上估计空间权重，以突出纹理和边缘；GMod 则依据全局统计量调节特征幅值与偏置。三个模块分别服务于双指标识别中的多尺度表征、低光结构恢复和全局曝光调节。

卷积网络通过局部连接、参数共享和层级结构提取多尺度特征，并可借助轻量卷积和模型压缩降低端侧开销。这些方法构成 RTCMNet 与低光增强网络的结构基础。

== 图像分类算法相关理论基础

脱叶率与吐絮率识别属于监督学习的多类别图像分类问题。农业田间影像具有光照变化大、背景干扰强、类别长尾明显和标注成本高等特点。本研究先以 RGB 像素向量和传统分类器建立工程基线，再分析光谱指数、前景分割、形态和纹理等结构化特征方案，并采用端到端深度模型学习图像表征。结构化特征为待验证方案，未作为第 3 章基线实验的输入@jia2013feature @bishop2006prml @lecun2015deep。

以下从数据表征、分类算法和评测协议三个方面介绍后续实验所需的理论基础@hastie2009esl。

=== 数据表征与预处理：从光谱到图像

遥感影像的“光谱信息”可理解为对不同波段反射率/辐射亮度的采样。对多光谱/高光谱数据，可将每个像素或 ROI 表征为 $bold(r) in RR^B$ 的谱向量，其中 $B$ 为波段数。光谱分析的基本思想是利用地物在波段响应上的差异构建判别特征，并在统计意义上将噪声与非稳定因素（照度变化、阴影、传感器噪声）尽可能从特征中“剥离”。

工程上，预处理通常包括辐射定标与归一化（减小曝光差异）、去噪平滑与谱形态增强（突出吸收峰或边缘特征），以及区域统计（用均值/分位数等统计量减少局部遮挡影响）。Savitzky--Golay 滤波常用于平滑与求导，以在抑制噪声的同时保留谱形状信息；其本质是在局部窗口内用低阶多项式做最小二乘拟合@savitzky1964sg。进一步的预处理方法（如标准正态变量、散射校正、导数与去趋势等）可用于削弱乘性/加性干扰并改善谱间可比性@rinnan2009preprocess。

对 UAV-RGB 场景，“光谱”更多体现为可见光通道的组合与统计，例如可见光植被指数（ExG/VARI/NGRDI）、白度/绿度统计、对阴影敏感的亮度分布描述，以及经分割得到的 ROI 结构统计。这类“近似光谱”表征在缺乏严格反射率定标时仍可作为有效判别先验，并能为深度模型提供可解释的中间变量。

=== 传统机器学习：工程基线与特征工程

传统机器学习路线强调“特征工程 + 监督分类器”。其优势在于：在小样本条件下可通过先验设计降低学习难度，并能输出与表型对应的可解释特征贡献；其不足在于对复杂背景与强非线性模式的表征能力受限，且在跨域偏移下容易失效@jia2013feature @hastie2009esl。

==== 特征构建、降维与特征选择

当输入维度较高（尤其是高光谱场景），直接在原始谱向量上训练分类器容易受到“维数灾难”与样本不足影响。典型策略是通过降维或特征选择构建更低维、信息更集中的表征空间。PCA 通过对协方差矩阵做特征分解得到正交主成分，实现最大方差方向上的线性投影；LDA 则在监督信息下最大化类间散度并最小化类内散度，从而提升可分性@jolliffe2002pca @fisher1936lda。

遥感分类中常见的结构化特征包括：

1. *指数与波段比值*：植被指数、颜色指数、归一化差分等。
2. *统计特征*：均值、方差、分位数、偏度等，用于刻画分布形态。
3. *纹理与结构特征*：GLCM、LBP、连通域、形态学指标等，用于补充光谱特征对结构变化的不足。

在样本有限或需追溯误差来源时，优先构建“少而精”的可解释特征往往更稳健，也更便于开展跨地块误差分析与特征贡献解释@jia2013feature。

==== 监督分类器

在得到结构化特征 $bold(x) in RR^d$ 后，可采用多种传统分类器进行建模：kNN 以距离度量为基础、结构简单但对尺度敏感@cover1967knn；Logistic Regression 属于线性判别模型，常与正则化结合用于高维小样本@hastie2009esl；SVM 通过最大间隔原理获得鲁棒决策边界，并可借助核函数处理非线性可分问题@cortes1995svm；Random Forest 通过集成降低方差并给出特征重要性，但在长尾分布下可能偏向多数类@breiman2001rf；Gradient Boosting 与 XGBoost 通过逐步拟合残差构造强分类器，常在结构化特征上取得较高性能@friedman2001gbdt @chen2016xgboost。

=== 深度学习：端到端表征学习与迁移范式

深度学习路线以端到端表征学习为核心，通过多层非线性映射自动学习从像素/谱向量到语义标签的判别特征，在复杂背景、遮挡与光照变化场景下具有更强的表达能力@lecun2015deep。但其训练通常需要更大规模、更高一致性的标注数据，并对算力与部署平台提出更高要求，因此在农业场景中常与迁移学习、轻量化结构与数据增强策略结合使用@krizhevsky2012alexnet @howard2017mobilenets。

==== 经典 CNN 架构与结构演进

从结构演进看，AlexNet 首次在大规模数据集上验证深层 CNN 的可行性并显著提升分类性能@krizhevsky2012alexnet；VGG 通过“统一的小卷积核堆叠”展示了深度带来的收益并成为特征提取基线@simonyan2014vgg；Inception 系列以多分支结构实现多尺度特征提取并在计算效率上做权衡@szegedy2015inceptionv3；ResNet 通过残差连接显著改善深层网络可训练性，成为现代视觉网络的关键范式@he2016resnet；DenseNet 通过密集连接增强特征复用，提升梯度流动并减少冗余参数@huang2017densenet。对于资源受限部署，MobileNets 等轻量骨干通过深度可分离卷积降低 MACs，常作为端侧模型的结构基础@howard2017mobilenets。

==== Transformer 与全局建模

除 CNN 外，Vision Transformer（ViT）等方法将自注意力机制引入图像建模，通过全局 token 交互建立长程依赖，在大规模预训练条件下表现出强大的表征能力@dosovitskiy2020vit。对农业遥感任务而言，Transformer 的价值往往体现在对大范围上下文与全局结构的建模能力，但其部署开销与数据规模需求需要结合端侧约束谨慎评估。

==== 损失函数、优化与泛化增强

深度分类模型通常采用交叉熵损失进行训练，并使用 SGD+Momentum 或 Adam 等优化器。Momentum 有助于加速收敛并改善鞍点附近的优化行为@sutskever2013sgd；Adam 在收敛速度与超参数敏感性方面更具工程友好性@kingma2014adam。在类别不均衡场景，可采用类别重加权或 focal loss 等方法抑制易分类样本的梯度主导效应，从而提升少数类学习强度@he2009imbalanced @lin2017focal。

数据增强与正则化对农业场景尤为关键。除常规的旋转/裁剪/颜色扰动外，mixup 与 CutMix 通过在输入空间进行样本混合或局部替换，提升模型对噪声与分布偏移的鲁棒性，并在一定程度上缓解过拟合@zhang2018mixup @yun2019cutmix。

==== 迁移学习与预训练

由于农业领域标注成本高且跨地块分布偏移显著，迁移学习是深度模型落地的常用范式：先在大规模通用数据集上预训练骨干网络，再在目标任务上微调，从而降低对大规模标注的依赖并提升收敛稳定性@deng2009imagenet。

=== 评测协议：类别不均衡与跨域泛化

脱叶率与吐絮率往往按区间离散为多等级标签，实际数据易出现长尾分布。若仅报告 Accuracy，模型可能通过预测多数类获得较高表面成绩而忽视少数类识别能力。因此，宏平均指标（Macro-F1、Macro-Recall 等）更能反映类别不均衡场景下的整体鲁棒性。本研究在基线评估中优先关注 Macro-F1，并建议结合混淆矩阵与错误案例分析定位失效等级区间@he2009imbalanced。

在评测协议上，单次随机切分容易引入偶然性。除常规 train/val/test 切分外，更建议采用多随机种子重复实验或交叉验证，以报告均值与方差并提高结论可信度；对于面向生产的遥感模型，进一步应采用跨地块、跨年份或跨光照条件的切分方式进行外推测试，以刻画域偏移条件下的泛化能力边界@kohavi1995cv。

综上，本研究理论基础从传统机器学习与深度学习两条路线展开：前者以 RGB 像素向量实验提供工程参照，并以结构化特征方案提供后续误差诊断的候选变量；后者提供端到端表征能力与复杂场景下的识别潜力。后续章节在统一数据与评测协议下报告相应实验，并在端侧约束下讨论其工程可部署性。

== 小结

本章依据真实生产田块的航测与人工调查流程构建棉花双指标数据集。数据处理包括固定航线采集、人工真值统计、MRK 位置信息解析、方向与尺度增强、边缘样本剔除和均值化，并建立脱叶率与吐絮率的分级标签体系。该数据集用于后续传统分类基线、RTCMNet 和田间流程验证。
