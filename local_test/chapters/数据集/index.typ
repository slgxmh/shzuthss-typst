#import "../../../template.typ": three-line-table

= 基础工作与相关理论 <chap:dataset>

== 构建棉花脱叶率与吐絮率数据集

本研究以田间棉花样本为对象，构建脱叶率与吐絮率双标签识别数据集。为提升数据管理效率并保证实验可复现性，本研究采用统一的 HDF5 文件组织样本、标签与切分索引，支撑后续批量训练与多模型公平对比。

=== 实验区域

本次实验于新疆石河子北泉双泉集三分厂进行，如@data_area 所示。研究区属温带大陆性气候，年均降水量较低，水资源相对匮乏，昼夜温差显著，这些条件对棉花生长周期及脱叶剂作用均有重要影响。实验选用的棉花品种为新陆早 83 号，该品种在当地种植较为广泛。实验所用脱叶剂由噻苯敌草隆和乙烯利组成，用量分别为 15 克/亩和 40 克/亩。实验田为当地农户自耕农田，而非专门试验田，以尽量保持研究条件与实际生产环境一致。

#figure(
  image("data_area.png"),
  caption: "试验区概况。（a）试验区位于石河子；（b）L1 与 L2 两块试验田；（c）L2 试验田航线及参数；（d）L1 试验田航线及参数；（e）棉花样本标记；（f）催熟作业过程中的棉花；（g）达到采收标准的棉花。",
) <data_area>

=== 图像采集

数据采集于 2024 年 9 月 24 日至 10 月 4 日进行，共完成 26 期航测，每期均采集 L1 和 L2 两块试验田的可见光影像。采集设备为大疆 Mavic 3 多光谱版无人机，航线由大疆 Pilot 2 软件自动生成，参数见@fly_param。连续航测记录了观测期内棉花脱叶与吐絮状态的变化。

#figure(
  three-line-table(
    table(
      columns: 3,
      [参数名称], [参数值], [参数说明],
      [地面分辨率], [1cm/pixel], [手动指定分辨率],
      [航线高度], [27.3m], [由地面分辨率自动计算的航线高度],
      [航向重叠率], [60%], [平坦地面的经验设置],
      [旁向重叠率], [70%], [平坦地面的经验设置],
      [飞行速度], [2.7m/s], [减少运动模糊，保持图像清晰],
    ),
  ),
  caption: "UAV 航线与影像采集参数。",
) <fly_param>

=== 专家标注

脱叶率与吐絮率通过人工调查获得。试验田内均匀布置样本点，每点选取三株棉花，记录绿叶数、总棉铃数和开放棉铃数，按@eq_boll 和@eq_de 计算两项指标。

$ "BA"_t = frac(B_t, B_0) times 100 % $ <eq_boll>
$ "DE"_t = (1 - frac(L_t, L_0)) times 100 % $ <eq_de>

其中，$"BA"_t$ 和 $"DE"_t$ 分别表示第 $t$ 天的吐絮率和脱叶率，$B_t$ 为第 $t$ 天开放棉铃数，$B_0$ 为施药初始棉铃总数，$L_t$ 为第 $t$ 天残留叶片数，$L_0$ 为施药初始叶片数。

=== 航点影像数据管理与样本构建工具

多期 UAV 航测产生的定位记录与影像文件数量较多，且同一航点包含可见光及多个光谱波段。若完全依靠人工逐文件整理，容易出现航点与影像对应关系错误，也不利于保存样本筛选依据和重复执行数据处理任务。为此，本研究开发了 UAV 航点影像数据管理工具，并结合配套 Python 脚本建立从原始航测数据到模型输入样本的数据构建流程。该工具采用 Tauri 桌面应用框架，后端使用 Rust 实现文件解析与数据组织，前端基于 React/TypeScript 构建交互界面，并利用 OpenLayers 展示航点空间位置。

工具首先读取 DJI 飞行任务生成的 MRK 文件，提取影像编号、经度和纬度等信息；随后依据航点编号和文件命名规则扫描影像目录，将同一航点的可见光影像以及 G、R、RE、NIR 波段影像组织为一条样本记录。虽然本文的棉花双指标识别以可见光影像为主要数据源，但多波段关联功能保留了原始航测数据的对应关系，也便于后续开展多源信息研究。

#figure(
  image("software_main.png"),
  caption: "UAV 航点影像数据管理工具界面。",
) <fig:uav-data-manager>

地图界面按编号显示各航点，支持单点选择和矩形框选。研究人员查看影像预览后，根据田块边界、清晰度和目标完整性决定样本取舍。MRK 路径与已选航点编号等状态可保存到工作区，便于恢复中断的筛选任务，也使各批次的处理范围可以复查。

筛选完成后，工具导出源影像目录、输出目录、中心裁剪比例、入选航点编号及波段文件名。配套 Python 脚本按任务记录并行裁剪影像，汇总人工调查表中的双指标标签，再执行随机旋转、水平镜像、224×224 像素中心裁剪和归一化，将图像及标签写入 HDF5 文件。航点编号连接定位记录、原始影像、调查结果和模型样本，便于追溯处理过程。工具功能与数据流程分别见@tab:uav-data-tool-modules 和@fig:uav-data-pipeline。

#figure(
  three-line-table(table(
    columns: (1.1fr, 1.5fr, 2.8fr),
    [处理环节], [实现方式], [主要作用],
    [定位记录解析], [桌面工具], [解析 MRK 文件，提取航点编号及经纬度],
    [航点影像关联], [桌面工具], [按航点组织可见光及 G、R、RE、NIR 波段影像],
    [空间筛选与复核], [桌面工具], [地图选点、矩形框选、影像预览及样本取舍],
    [工作区管理], [桌面工具], [保存数据路径和入选航点，支持任务恢复],
    [裁剪任务导出], [桌面工具], [输出入选航点、影像文件及裁剪参数],
    [样本生成与封装], [Python 脚本], [完成批量裁剪、标签汇总、HDF5 封装],
  )),
  caption: "UAV 航点影像数据管理与样本构建工具的功能组成。",
) <tab:uav-data-tool-modules>

#figure(
  image("software_flow.png", width: 100%),
  caption: "基于自主开发工具的 UAV 航点影像筛选与样本构建流程。",
) <fig:uav-data-pipeline>

=== 数据集概况

完成航点影像筛选与标签关联后，首先按照导出任务中设定的比例对原始影像进行中心裁剪，以保留固定航点影像的中心区域并减少相邻区域干扰。输入模型前对图像进行归一化处理，使不同批次样本具有一致的数据尺度。根据田间人工调查结果，脱叶率划分为 8 个等级，吐絮率划分为 6 个等级。

预处理后数据集示例如@fig:dataset_num 所示。数据集以预处理后的 224×224 图像块为划分单位，按照 5:1:4 的比例划分为训练集、验证集与测试集，并通过固定随机种子保证不同实验批次间可比性。其中，训练集用于调整模型参数，验证集用于优化超参数，测试集用于评估模型的最终性能。

本研究的评价范围是特定生产区域、固定航线条件下的图像块识别。所有对比模型使用相同的划分索引、训练配置和评价指标，以比较目标作业域内的识别性能与部署代价。连续棉田影像在空间上相关、外观上相似，因此图像块级划分不能检验跨区域、跨年份或跨品种的泛化能力；相关结果应限定在当前采集与划分条件下解释。

#figure(
  image("dataset_num.png"),
  caption: "棉花双指标数据集示例与等级分布。（a）图像样本及脱叶率标签 D、吐絮率标签 B；（b）脱叶率标签（de_label）与吐絮率标签（ba_label）的样本分布。",
)<fig:dataset_num>

受棉花真实催熟进程及观测时间窗口影响，数据集中实际出现的标签范围并未覆盖全部理论等级。施用脱叶催熟剂时，棉花已具有一定的自然吐絮水平，因此首次观测的吐絮率并非从零开始，实际样本主要分布在 4--9 级；另一方面，受植株生理状态、田间环境和药剂作用差异影响，观测期末仍存在一定叶片残留，脱叶率未达到理论最高等级，实际样本分布在 0--7 级。该分布反映了试验期内棉花成熟状态的真实变化过程，并非数据遗漏或人为删除类别。

数据文件包含三个核心数据集，如@tab:hdf5-core 所示。

#figure(
  three-line-table(table(
    columns: 4,
    [字段名], [含义], [维度], [类型],
    [`images`], [图像数据], [`(N, 3, 224, 224)`], [`float32`],
    [`de_labels`], [脱叶率标签], [`(N,)`], [`int8`],
    [`ba_labels`], [吐絮率标签], [`(N,)`], [`int8`],
  )),
  caption: "HDF5 数据文件核心字段说明。",
) <tab:hdf5-core>

其中，$N$ 表示样本总数。图像采用通道优先（$C, H, W$）格式，能够被常见机器学习框架直接读入，减少数据转置和额外预处理带来的工程开销。

== 构建大田农业低光增强数据集

=== 实验区域

本次实验在新疆沙湾市周边的大田农业区开展。研究区属温带大陆性气候，年均降水量较低（125.9 至 207.0 毫米），水资源相对匮乏且昼夜温差显著。实验数据采集于实际生产田块（农户自耕地），而非专门试验田，因此能够更真实地反映大田场景中常见的地表背景变化、作物行间结构与管理差异。

#figure(
  image("area.png", width: 90%),
  caption: [
    研究区域示意图 (a) 研究区域在新疆沙湾市的地理位置；(b) 开阔大田实验区的无人机正射影像，其中 L1、L2 和 L3 分别表示不同作物地块；(c)–(e) 分别为 L1（小麦）、L2（棉花）和 L3（玉米）的地面实景图像。研究区域均为露天大田环境，具有典型的农业种植结构与自然光照条件。
  ],
) <fig:lowlight_area>

=== 图像采集

正常曝光数据采集于 2025 年 5 月 6 日至 6 月 12 日，覆盖作物生长的不同时期与田间外观变化。为增强数据多样性与代表性，从多期航测数据中均匀选取三种典型大田作物（小麦、棉花与玉米）的无人机影像，最终获得 1306 张原始 UAV 图像作为低光成对数据集的数据源。此外，另选取 26 张真实田间夜间低光图像，作为后续低光退化参数校准与统计一致性分析的外部分布参照。

本节介绍低光数据的来源与采集安排。成对样本生成、退化参数设置及与真实夜间分布的统计比较见第 5 章。

== 相关工作原理

=== 图像的光谱与特征工程

遥感光谱信息反映地物在不同波段的反射率或辐射亮度。对多光谱、高光谱数据，可将像素或 ROI 表示为谱向量 $bold(r) in RR^B$，其中 $B$ 为波段数。光谱分析利用波段响应差异构建判别特征，并通过预处理减轻照度、阴影与传感器噪声的影响。工程上，预处理通常包括辐射定标与归一化、去噪平滑与谱形态增强，以及区域统计。Savitzky--Golay 滤波常用于平滑与求导，其本质是在局部窗口内用低阶多项式做最小二乘拟合@savitzky1964sg；标准正态变量、散射校正、导数与去趋势等方法可用于削弱乘性/加性干扰并改善谱间可比性@rinnan2009preprocess。在 UAV-RGB 影像中，可利用的信息主要是可见光通道组合与统计量，包括 ExG、VARI、NGRDI 等指数，白度和绿度统计、亮度分布，以及分割后 ROI 的结构统计。缺少反射率定标时，这些量仍可作为分类特征，但应结合曝光与背景条件解释其变化。

传统机器学习路线强调“特征工程 + 监督分类器”，在小样本条件下可通过先验设计降低学习难度，并输出与表型对应的可解释特征贡献；其不足在于对复杂背景与强非线性模式的表征能力受限，且在跨域偏移下容易失效@jia2013feature @hastie2009esl。当输入维度较高时，直接在原始向量上训练分类器容易受到“维数灾难”与样本不足影响，典型策略是通过降维或特征选择构建更低维的表征空间：PCA 通过对协方差矩阵做特征分解实现最大方差方向上的线性投影，LDA 则在监督信息下最大化类间散度并最小化类内散度@jolliffe2002pca @fisher1936lda。遥感分类中常见的结构化特征包括指数与波段比值、统计特征（均值、方差、分位数、偏度等），以及纹理与结构特征（GLCM、LBP、连通域、形态学指标等）。

在得到结构化特征 $bold(x) in RR^d$ 后，可采用多种传统分类器进行建模：kNN 以距离度量为基础、结构简单但对尺度敏感@cover1967knn；Logistic Regression 属于线性判别模型，常与正则化结合用于高维小样本@hastie2009esl；SVM 通过最大间隔原理获得鲁棒决策边界，并可借助核函数处理非线性可分问题@cortes1995svm；Random Forest 通过集成降低方差并给出特征重要性，但在长尾分布下可能偏向多数类@breiman2001rf；Gradient Boosting 与 XGBoost 通过逐步拟合残差构造强分类器，常在结构化特征上取得较高性能@friedman2001gbdt @chen2016xgboost。

=== 卷积网络的发展与应用

卷积神经网络（Convolutional Neural Network, CNN）通过局部连接、参数共享和层级特征提取处理图像数据。LeNet、AlexNet、VGG 和 ResNet 等网络推动了卷积结构在视觉识别中的应用@lecun1998lenet @krizhevsky2012alexnet @simonyan2014vgg @he2016resnet；Inception 系列以多分支结构实现多尺度特征提取@szegedy2015inceptionv3，DenseNet 通过密集连接增强特征复用@huang2017densenet。对于资源受限部署，MobileNets 等轻量骨干通过深度可分离卷积降低 MACs，常作为端侧模型的结构基础@howard2017mobilenets。除 CNN 外，Vision Transformer 将自注意力机制引入图像建模，在大规模预训练条件下表现出强大的全局表征能力@dosovitskiy2020vit，但其部署开销与数据规模需求需要结合端侧约束谨慎评估。

在深度学习框架中，二维卷积层通常实现的是“互相关（cross-correlation）”操作。给定输入特征图 $bold(x) in RR^(H times W times C_("in"))$，卷积核 $bold(w) in RR^(K times K times C_("in") times C_("out"))$，输出通道 $k$ 在空间位置 $(i, j)$ 的响应可写为：

$ y_(i,j,k) = sum_(c=1)^(C_("in")) sum_(u=1)^(K) sum_(v=1)^(K) w_(u,v,c,k) · x_(i+u, j+v, c) + b_k. $

在 stride 为 $s$、padding 为 $p$ 的情况下，输出空间尺寸为：

$ H_("out") = floor((H + 2p - K)/s) + 1,quad W_("out") = floor((W + 2p - K)/s) + 1. $

参数共享使同一卷积核在全图复用，并形成平移等变性，适合目标位置不固定、局部纹理重复出现的田间影像。卷积堆叠逐步扩大感受野，浅层侧重边缘与纹理，深层形成更抽象的语义特征；池化或步幅卷积可降低计算量，但过度下采样会损失脱叶率与吐絮率识别所需的细粒度信息，多尺度融合可联合浅层细节与深层语义。

非线性激活函数用于提升网络的表示能力，ReLU 及其变体因计算简单、梯度传播稳定而成为主流选择@nair2010relu。Batch Normalization（BN）对 mini-batch 特征进行标准化并引入可学习仿射参数，可改善训练稳定性，部署时还可折叠到卷积权重中@ioffe2015batchnorm。残差连接采用“恒等映射 + 残差”形式改善深层网络的梯度传播@he2016resnet；深度可分离卷积可显著降低 MACs 与参数量，是轻量骨干的核心算子@howard2017mobilenets；剪枝、量化和蒸馏还可进一步降低部署开销，但其实际收益需通过目标设备时延验证@cheng2017compression_survey。

CNN 通常通过最小化经验风险训练。分类任务常用交叉熵损失，含动量的 SGD 与 Adam 均为常用优化器@kingma2014adam；类别不均衡时，类别重加权或 focal loss 可减轻易分类样本对梯度的主导作用@he2009imbalanced @lin2017focal。常用正则化策略包括权重衰减、数据增强与 Dropout@srivastava2014dropout；mixup 与 CutMix 通过样本混合提升模型对噪声与分布偏移的鲁棒性@zhang2018mixup @yun2019cutmix。由于农业领域标注成本高且跨地块分布偏移显著，迁移学习是深度模型落地的常用范式：先在大规模通用数据集上预训练骨干网络，再在目标任务上微调@deng2009imagenet。

=== 注意力机制的主要类型与轻量化

注意力机制根据输入对不同空间位置、通道或尺度的特征赋予权重，使网络突出与任务相关的响应。该方法由序列建模扩展到视觉任务后，形成了自注意力、空间注意力和通道注意力等主要类型@vaswani2017attention。

自注意力通过位置间相似性建立长程依赖，Vision Transformer 和 Swin Transformer 是其典型应用@vaswani2017attention @liu2021swin。轻量注意力则使用卷积、门控或局部统计调节空间与通道响应，计算开销通常更低。考虑到棉田影像的判别信息主要表现为局部纹理、边界和多尺度冠层结构，本研究采用后一路线满足端侧时延约束。

空间注意力生成二维权重图以突出叶缘、棉絮和冠层空隙等位置；通道注意力选择与颜色、纹理和结构有关的通道响应；特征重标定则通过缩放、偏置或全局调制调整特征分布。这些机制为后续 MSCA、ESA 和 GMod 的设计提供了基础。RTCMNet 中的 MSCA（Multi-Scale Convolutional Attention）通过多尺度卷积和局部归一化生成注意力响应，避免高代价的全局矩阵运算@xu2024sctnet；低光增强网络中的 ESA（Enhanced Spatial Attention）在降采样特征上估计空间权重，以突出纹理和边缘；GMod 则依据全局统计量调节特征幅值与偏置。三个模块分别服务于双指标识别中的多尺度表征、低光结构恢复和全局曝光调节。

== 小结

本章介绍了棉花双指标数据集和农业低光数据集的采集与组织方法。棉花影像通过 MRK 航点记录关联人工调查标签，经筛选、裁剪、几何增强和归一化后形成分级识别样本；低光数据则包括正常曝光源影像与真实夜间校准影像。在此基础上，本章说明了光谱特征工程、卷积网络与注意力机制的相关原理，为后续实验统一术语与方法定义。
