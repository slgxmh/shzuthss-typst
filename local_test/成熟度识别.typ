= 面向棉花成熟度遥感表型的轻量化识别方法

== 问题建模与挑战分析

第二章已从农艺和数据层面对成熟期棉花脱叶率与吐絮率的遥感表型进行了定义。本章进一步讨论如何在真实田间 UAV RGB 图像上建立可部署的识别模型。与通用图像分类任务不同，棉花成熟度识别并不只是从图像中区分若干静态对象类别，而是对催熟后冠层状态进行等级判别，其视觉证据往往表现为叶片覆盖度变化、白絮暴露比例变化以及冠层结构稀疏化等连续过程性差异。因此，模型不仅要具备足够的表征能力，还必须能够在复杂背景、弱特征和端侧部署约束下保持稳定性能。

=== 棉花成熟度识别任务定义

设输入样本为由 UAV 获取并经预处理后的棉株冠层图像块 $x in RR^(3 times H times W)$，其中 $H$ 和 $W$ 分别表示图像高和宽，3 表示 RGB 三通道。本文的目标不是输出连续值回归结果，而是将脱叶率与吐絮率分别映射为离散成熟等级，以便与数据集标注和后续作业决策流程保持一致。设脱叶率等级空间为 $cal(D) = {0,1,...,K_d-1}$，吐絮率等级空间为 $cal(B) = {0,1,...,K_b-1}$，则识别模型可表示为

$ f_theta(x) = (hat(d), hat(b)), quad hat(d) in cal(D), hat(b) in cal(B) $

其中，$theta$ 表示模型参数，$hat(d)$ 和 $hat(b)$ 分别为模型预测的脱叶率等级与吐絮率等级。与分别训练两个独立模型相比，本文采用共享骨干网络并在末端设置双任务预测头的方式进行联合建模。其基本考虑在于，脱叶率与吐絮率虽然对应不同农艺过程，但二者在冠层颜色、枝叶遮挡、白絮暴露和整体稀疏化等方面共享大量视觉证据。共享骨干有助于提高有限样本条件下的特征利用效率，并降低端侧部署时的重复计算开销。

从模型输出形式看，本章将问题定义为双任务多分类，而不是双回归任务。一方面，当前数据标注体系本身以等级区间表达成熟状态，采用分类建模更符合标签组织方式；另一方面，在真实生产场景中，等级判断往往比高精度连续值更具可执行性，例如是否达到催熟复核阈值、是否接近机采适期等。因此，本章所研究的轻量化识别方法，是在统一数据协议和等级标签定义下开展的双任务联合分类问题。

=== 农业场景下的关键挑战

棉花成熟度识别的第一类难点来自复杂背景干扰。成熟期棉田图像中通常同时包含裸土、残膜、滴灌带、阴影、缺株区以及行间空隙等背景因素，这些区域在颜色和纹理上可能与目标表型形成干扰。例如，亮色残膜和强反光地表容易与吐絮区域混淆，阴影和背景穿透又可能被误判为叶片脱落后的冠层稀疏区。若模型缺乏足够的空间结构建模能力，容易学习到与成熟状态无关的背景共现关系。

第二类难点来自目标尺度变化和视角不稳定。UAV 低空成像虽然具备高分辨率优势，但在实际作业中仍会受航高微小波动、姿态变化和样本裁剪尺度差异影响。对吐絮率而言，白絮目标通常呈现为数量较多、分布离散的小尺度亮斑；对脱叶率而言，判别依据更多来自冠层整体结构和叶片覆盖比例变化。这意味着模型需要同时建模局部细节和全局上下文，而单尺度轻量骨干往往难以兼顾二者。

第三类难点在于表型特征弱且类别边界连续。脱叶率和吐絮率等级之间并不存在完全清晰的视觉断点，尤其在相邻等级之间，差异更多表现为比例变化而非结构突变。吐絮率任务中，少量白絮刚开始暴露时与背景高亮区域很容易混淆；脱叶率任务中，局部失叶、遮挡和群体长势差异又会削弱等级边界的稳定性。因此，模型需要具备对细粒度差异和多尺度上下文的敏感性。

第四类难点来自实时性与部署要求。成熟度监测的实际应用场景包括便携终端、无人机遥控器和边缘侧设备，这些平台的算力、内存和能耗预算均明显低于服务器环境。重型网络虽然可能获得较高离线精度，但其参数量、计算量和端侧推理时延通常难以满足在线监测需求；过度追求轻量化又可能导致特征表达不足。因而，现有模型往往难以同时满足识别精度与实时部署效率的双重要求，这正是本章提出轻量化识别方法的直接动因。

== 方法总体框架

针对上述问题，本文提出面向棉花成熟度遥感表型的轻量级识别模型 RTCMNet（Real-Time Cotton Maturity Network）。该模型以“共享骨干 + 多尺度卷积注意力增强 + 双任务分类头”为总体结构，在保证较低参数规模和推理开销的前提下，提高对复杂农田场景中细粒度成熟表型的建模能力。

=== 模型整体架构

RTCMNet 的整体流程可概括为三部分。首先，输入的 UAV RGB 图像经过浅层卷积 stem 和若干卷积块，提取基础的边缘、颜色和局部纹理特征；其次，在中高层阶段引入面向农业场景设计的多尺度卷积注意力模块，对冠层结构、白絮暴露和背景抑制相关的上下文信息进行增强；最后，将共享骨干得到的高级语义特征送入两个任务特异预测头，分别输出脱叶率和吐絮率的分类结果。

从数据流角度看，该模型并未将两个任务完全割裂处理，而是在主干部分共享特征提取，仅在预测阶段分离为两个分支。这种设计兼顾了任务共享性与任务差异性：共享骨干用于提取冠层公共表征，双分支预测头则分别学习脱叶率与吐絮率的判别边界。模型整体不依赖复杂的 Transformer 全局自注意力矩阵运算，而是以卷积和轻量注意力为基本构件，更适合端侧部署。

=== 设计思路

RTCMNet 的设计遵循三个原则。第一，以轻量化为基本前提。本文所面向的不是服务器离线推理，而是资源受限终端下的实时识别，因此模型在设计上首先要控制参数量和计算量，避免高昂的端侧部署成本。第二，以多尺度特征增强为核心补偿机制。成熟度识别同时依赖局部小尺度纹理和整体冠层结构，单纯依赖浅层卷积或极简骨干容易丢失任务关键信息，因此需要在轻量结构中引入能够增强多尺度上下文的模块。第三，以农业任务适配为导向。模型结构不追求通用视觉任务上的复杂范式移植，而强调对棉田背景复杂、白絮离散、小目标多和等级边界连续等特征的针对性适配。

基于以上思路，本文选择卷积主干而非重型 Transformer 作为基础结构，一方面是因为卷积在局部模式提取和端侧部署上更成熟，另一方面是因为棉花成熟度任务的关键信息仍主要体现在局部纹理与结构组合上。在此基础上，通过引入多尺度卷积注意力模块，提高模型对不同尺度冠层结构和细粒度白絮特征的感知能力，使轻量化与表征能力之间形成相对平衡。

== 轻量化特征提取网络设计

=== 骨干网络设计

RTCMNet 的骨干网络由浅层 stem、多个卷积块以及中高层特征增强阶段组成。浅层部分主要负责从 RGB 输入中提取低级视觉信息，包括边缘、颜色梯度和局部纹理；中层部分逐步扩大感受野，建立对枝叶排列、冠层稀疏度和白絮分布的更抽象表示；高层部分则结合注意力增强模块，进一步提升对成熟状态相关特征的聚焦能力。

在结构安排上，骨干网络采用逐步下采样与通道扩展的方式构建层级表示，使模型在有限计算预算下获得较大的有效感受野。与直接堆叠重型卷积层不同，RTCMNet 更强调“以较少层数获取足够判别特征”，避免不必要的参数堆叠。这样的设计对于成熟度识别任务尤为重要，因为该任务所需的不是极端复杂的语义抽象，而是在保持空间结构信息的同时准确区分相邻等级。

=== 轻量化策略

本文在骨干网络中采用轻量化卷积模块作为基本单元，通过减少冗余通道计算和控制网络深度，降低模型的总参数量和乘加运算量。其基本思想不是简单压缩任意已有大模型，而是在设计阶段就围绕端侧部署需求构建结构，即优先保留对农业表型识别有效的局部建模能力，并尽量避免对全局矩阵运算和高维投影的依赖。

除基本卷积结构外，轻量化策略还体现在以下两个方面。其一，模型采用共享骨干服务于双任务输出，减少了分别训练与部署两个独立网络带来的重复开销。其二，特征增强模块被控制在中高层阶段使用，而不是在全网络中大规模堆叠，以避免过度增加时延。由此，RTCMNet 并非追求最小参数量本身，而是在保证主要判别信息被有效建模的前提下，实现更合理的复杂度配置。

=== 计算复杂度分析

对卷积网络而言，模型复杂度通常可由参数量和乘加运算量两个维度描述。设某卷积层输入通道数为 $C_"in"$，输出通道数为 $C_"out"$，卷积核大小为 $K times K$，输出特征图空间尺寸为 $H_"out" times W_"out"$，则该层参数量为

$ P = K^2 times C_"in" times C_"out" $

对应乘加运算量可近似写为

$ "MACs" = H_"out" times W_"out" times K^2 times C_"in" times C_"out" $

整模型的总运算量则为各层运算量之和。对端侧部署而言，参数量影响存储和加载成本，MACs 影响前向推理理论计算开销，而真实推理时间还会受算子实现和硬件访存模式影响。本文在后续实验中同时报告参数量、GMACs 和真实设备推理时延，以更全面地评价模型部署代价。

== 面向农业场景的特征增强模块

=== 模块设计动机

尽管轻量卷积骨干能够以较低成本提取局部纹理和边缘信息，但仅依赖常规卷积仍不足以稳定识别成熟期棉花的细粒度表型。其根本原因在于，脱叶率和吐絮率的视觉证据同时分布在不同尺度上：吐絮率更多依赖小尺度高亮白絮区域及其局部聚集关系，脱叶率则更依赖冠层孔隙、叶片覆盖度和整体结构疏密变化。若模型只能感知单一尺度特征，容易在复杂背景和相邻等级之间产生混淆。

此外，农业场景中的关键目标往往并不位于图像中心，也不具备规则形状。成熟期棉株的白絮分布具有离散、小尺度和不规则聚集的特点，残留叶片则与枝条、阴影和背景穿透交织出现，这要求模型不仅能提取局部纹理，还要能够在有限计算开销下整合邻域上下文关系。基于此，本文在骨干网络中引入多尺度卷积注意力模块，用于增强不同尺度上的关键响应，同时抑制与成熟状态无关的背景干扰。

=== 模块结构

本文采用的特征增强模块为 MSCA（Multi-Scale Convolutional Attention）。其核心思想是利用多个尺度的卷积分支近似建模注意力权重分布，以替代标准自注意力中代价较高的 Query、Key、Value 全局矩阵运算。具体而言，输入特征首先经过归一化处理，再分别送入不同感受野的卷积分支，各分支在局部邻域内提取尺度不同的空间依赖关系；随后，将多分支输出进行归一化和聚合，并通过残差连接与输入特征融合，得到增强后的表示。

若记输入特征图为 $X$，第 $s$ 个尺度分支为 $text("Conv")_s(\cdot)$，则可将模块抽象表示为

$ A(X) = sum_(s=1)^S op("softmax")(text("Conv")_s(op("BN")(X))) $

$ Y = W_o A(X) + W_r(X) $

其中，$A(X)$ 表示多尺度注意力响应的聚合结果，$W_o$ 为输出映射，$W_r(X)$ 为残差分支映射。相较于标准自注意力，该模块不显式构造全局位置对相似度矩阵，因此参数量与运算量更可控；相较于单尺度卷积注意力，其多分支结构能够同时关注局部细节和更宽邻域上下文，更适合处理棉田冠层中尺度变化明显的视觉模式。

从任务适配性看，MSCA 的作用主要体现在两个方面。一方面，它增强了对白絮局部高亮纹理与枝叶结构边界的识别能力，有助于提升吐絮率分级性能；另一方面，它通过更大尺度的上下文感受能力，加强了对冠层整体稀疏度和空间组织模式的建模，有助于脱叶率识别。也就是说，该模块的收益并非抽象地来自“加了注意力”，而是来自对成熟度任务中多尺度农业表型的针对性刻画。

== 预测头设计

=== 多分支结构

在骨干网络输出共享高级特征后，RTCMNet 设置两个相互独立的分类分支，分别完成脱叶率和吐絮率等级预测。每个预测头由全局平均池化、轻量全连接层和输出分类层组成。全局平均池化用于将空间特征汇聚为紧凑的语义向量，减少参数规模并降低过拟合风险；全连接层则进一步完成任务特异判别边界的学习。

采用双分支结构的原因在于，尽管两项任务共享大量底层视觉证据，但其最终判别关注点并不完全一致。脱叶率更关注绿色叶片残留、冠层孔隙和整体结构稀疏化，吐絮率更关注白絮暴露程度和局部亮色目标分布。若完全共享分类器，容易使一项任务的判别边界干扰另一项任务；而通过共享骨干、分离预测头，则可以在不显著增加部署成本的前提下兼顾共享性与差异性。

=== 损失函数

由于本文将两项任务建模为分类问题，因此每个任务采用交叉熵损失进行优化。设脱叶率预测损失为 $L_d$，吐絮率预测损失为 $L_b$，则总损失函数定义为

$ L = lambda_d L_d + lambda_b L_b $

其中，$lambda_d$ 和 $lambda_b$ 为两项任务的损失权重。在实际训练中，若两个任务具有相近的重要性，可令 $lambda_d = lambda_b = 1$。该设计使模型在共享特征学习过程中同时接受两项任务监督，有利于提升骨干特征对成熟状态整体变化的敏感性。

需要指出的是，在成熟度等级识别场景中，类别不均衡可能导致模型过度偏向高频等级。为此，训练阶段还可结合类别重加权或采样平衡策略，提高少数等级样本对梯度更新的贡献。后续实验评价中也不只报告 Accuracy，而将 F1 等指标一并纳入，用于更真实反映模型在各等级上的综合表现。

== 模型复杂度与部署分析

=== 参数量与 FLOPs

从模型规模看，RTCMNet 采用了以轻量卷积为主、局部引入多尺度注意力增强的结构，因此其参数规模明显低于常见重型骨干网络。根据后续实验结果，RTCMNet 的参数量约为 0.35M，GMACs 约为 0.9，远低于 DenseNet121、EfficientNet 等模型。该结果说明，RTCMNet 在设计上避免了大规模冗余通道和高开销全局建模操作，能够在较低复杂度下完成双任务识别。

从理论计算角度看，这一复杂度水平更适合嵌入式端侧设备。尤其在需要持续处理连续 UAV 图像流的场景中，较低的参数量和运算量有助于降低模型加载时间、内存占用和长期运行能耗。

=== 推理时间

理论复杂度并不总能完全反映真实设备上的运行效率，因此本文还关注模型在实际终端上的推理时间。实验表明，RTCMNet 在 DJI 遥控器平台上的单次推理时间约为 32 ms，显著低于 DenseNet121 等重型模型。相比之下，DenseNet121 虽然具有较高识别精度，但移动端推理时间达到秒级，难以满足实时应用需求。

这一结果表明，RTCMNet 的轻量化设计不仅体现在参数和 MACs 上，也能够转化为真实端侧时延收益。对于机载或便携终端而言，这一点比离线实验中的微小精度提升更具实际意义。

=== 部署可行性

综合参数规模、理论复杂度和真实推理时间可以看出，RTCMNet 更适合部署于资源受限的农业终端设备。首先，其模型规模较小，便于在嵌入式平台完成加载与更新；其次，其推理时延较低，能够适应田间作业中对实时反馈的要求；再次，双任务共享骨干的设计减少了重复计算，使同一模型即可同时输出脱叶率与吐絮率结果，降低了系统集成复杂度。

需要强调的是，部署可行性并非只取决于模型能否运行，还取决于其在复杂场景下的稳定性。RTCMNet 的目标并不是在服务器环境获得最高绝对精度，而是在真实农业应用约束下实现“足够高的识别性能 + 可接受的端侧代价”的平衡。

== 实验设计与评价指标

=== 数据集与实验设置

本章实验所用数据来自第二章构建的成熟期棉花脱叶率与吐絮率 UAV RGB 数据集，覆盖不同地块、不同日期和不同成熟阶段。实验在统一的数据划分和标签定义下进行，以保证不同模型比较的公平性。由于数据具有真实生产场景下的复杂背景和类别分布不均衡特征，因此该实验更接近模型在实际应用中的表现边界，而非理想化受控环境结果。

为验证 RTCMNet 的有效性，本文选取了多种具有代表性的深度学习模型作为比较对象，包括 DenseNet121、ResNet18、MobileNetV2、ShuffleNetV2、EfficientNet 以及若干轻量或端侧友好模型。比较目标不仅包括识别精度，还包括模型参数量、计算复杂度和端侧推理时间，从而综合评价不同模型的实际适用性。

=== 评价指标

本文采用 Accuracy、Precision、Recall 和 F1 作为主要分类评价指标，并结合参数量、GMACs 和真实设备推理时延评价部署性能。Accuracy 用于描述整体分类正确率，但在类别不均衡场景下容易高估多数类表现；Precision 和 Recall 分别从误报和漏报角度刻画模型性能；F1 则综合反映两者平衡情况，更适合评价成熟度等级识别中对边界类和少数类的判别能力。

除分类指标外，本文同时报告模型规模与运行效率相关指标。这是因为对农业端侧场景而言，单纯比较精度并不足以判断模型是否可用，只有将识别性能与部署代价结合起来，才能获得更符合应用实际的结论。

== 实验结果分析

=== 与现有方法对比

在统一实验协议下，RTCMNet 展现出较强的综合性能。已有结果表明，DenseNet121 在识别精度上达到较高水平，Accuracy 和 F1 均可达到约 0.94，但其参数量超过 20M，端侧推理时间约为 1084 ms，明显难以用于实时农业应用。与之相比，RTCMNet 在 Accuracy 和 F1 上达到相近水平，Precision 和 Recall 亦保持稳定，同时仅使用约 0.35M 参数和 0.9 GMACs，在 DJI 遥控器上的推理时间约为 32 ms，体现出更优的精度-效率平衡。

与典型轻量模型相比，RTCMNet 也表现出更适合当前任务的结构优势。MobileNetV2 虽然具有较好的轻量化特性，但在当前成熟度识别任务上的综合精度仍低于 RTCMNet，说明单纯依赖通用移动端骨干并不足以充分表征棉田成熟期的细粒度表型。ShuffleNetV2 和 EfficientNet 同样能够在一定程度上兼顾精度和速度，但其模型规模通常仍高于 RTCMNet，且在复杂背景和相邻等级区分方面未显示出明显优势。由此可见，RTCMNet 的收益并不只是来自“模型更小”，而是来自结构与任务之间更高的一致性。

从任务角度看，RTCMNet 在脱叶率与吐絮率两个任务上均表现稳定。对吐絮率识别而言，多尺度卷积注意力有助于增强对白絮局部高亮纹理和空间聚集关系的建模；对脱叶率识别而言，该结构又能通过更大感受野捕捉冠层整体稀疏化和绿色覆盖变化。这也是其能够在双任务场景下保持较高综合性能的关键原因。

=== 消融实验

为验证 MSCA 模块和网络配置对性能的具体贡献，本文进一步进行了消融实验。结果表明，在相近网络规模下，以 MSCA 代替普通卷积注意力或更复杂但不够轻量的注意力结构，能够在较小代价增加的前提下提升识别准确率。尤其当网络层数和模块配置处于适中水平时，模型在准确率、推理时间和计算量之间取得了更优平衡。

消融实验还表明，模型性能提升并非简单来自参数堆叠。随着网络层数或注意力头数继续增加，推理时间和复杂度显著上升，而精度提升并不稳定，部分情况下甚至出现轻微下降。这说明在当前数据规模和任务条件下，过高模型容量会引入冗余建模，并不一定转化为有效识别收益。相反，采用与农业表型任务更匹配的多尺度卷积注意力结构，才是提升模型性能的主要来源。

这一结果对本研究具有重要意义。它说明 RTCMNet 的性能优势不是依赖重型网络常见的“大模型换高精度”策略，而是在严格的端侧约束下，通过更合理的结构设计获取到的。这与本章面向可部署应用的目标是一致的。

== 本章小结

本章围绕成熟期棉花脱叶率与吐絮率双指标遥感识别任务，提出了轻量级识别模型 RTCMNet，并从任务定义、结构设计、复杂度控制、部署可行性和实验验证等方面进行了系统论述。研究结果表明，成熟度识别的关键并不只是增加模型规模，而在于如何在复杂农业场景下以有限计算预算有效建模多尺度冠层结构和细粒度表型差异。

在统一数据与评测协议下，RTCMNet 通过共享骨干、多尺度卷积注意力和双任务预测头，实现了较好的精度-效率平衡。对比实验和消融实验共同表明，该模型能够在保持较低参数量和较快端侧推理速度的同时，获得与部分重型模型相当的综合识别性能。上述结果为后续低光增强和全天候成熟度识别研究提供了稳定的正常光照基线与模型基础。
